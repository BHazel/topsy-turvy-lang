# DEVELOPMENT.md
## Topsy Turvy Language — Technical Reference for Coding Agents

* **Last Updated:** 2026-07-03 — Operetta Touring Theatre Phase 1 engine contract (`TOURING_THEATRE_PLAN.md` §10), plus a naming/structure review pass. Phase 0's 4 hello-world exports are superseded by the full, frozen v1 contract (a breaking change to Phase 0's signatures, acceptable since it was an explicit spike) — only the contract version (`1`) survives verbatim. **9 exports** in `operetta/BWHazel.TopsyTurvy.Embedded/NativeExports.cs`, C# method names PascalCase per `AGENTS.md` with only the `[UnmanagedCallersOnly(EntryPoint = "topsyturvy_*")]` string keeping the exported C symbol (renamed from the placeholder `tt_*` prefix to `topsyturvy_*` for clarity): `ApiVersion`, `CreateSession`/`DestroySession` (opaque `GCHandle`-backed session lifecycle), `AnalyseSource` (diagnostics-with-spans, replacing the placeholder `AnalysisSummary`), `GetHover`/`GetCompletions` (wrap `HoverMarkdownBuilder`/`SymbolTable`/`KeywordData`, mirroring `HoverHandler`/`CompletionHandler`'s logic — `PhraseContext.cs` deliberately duplicates `CompletionHandler.GetPhraseContext` since Embedded cannot reference the OmniSharp-dependent `LanguageServer` project; both sides now carry a `<b>Constraint:</b>` doc note pointing at each other), `FormatSource` (wraps `SourceFormatter.FormatSource`), `ExecuteProgramme` (callback-driven — see below), `CancelExecution`, `GetLastError`, `FreeBuffer`. **`topsyturvy_tokens` deliberately deferred** — no existing reusable scanner (`ReplHighlighter` only emits ANSI markup via its own hard-coded keyword+colour list, independent of the authoritative `KeywordData` — a pre-existing third keyword list, out of scope to fix here, flagged separately); may be adopted later without a contract change per §5.4. **Span encoding**: `AnalyseSource`'s diagnostics use 1-indexed line/column matching `SourceSpan`/`SourceLocation` exactly; `GetHover`/`GetCompletions`'s `line`/`column` parameters are 0-indexed instead, matching the LSP convention `SymbolTable.ExtractWordAt`/`CompletionHandler.GetPhraseContext` already use — the two conventions are documented explicitly in both the XML docs and `topsyturvytoolchain.h` so they aren't conflated. **Project layout**: the Embedded project is now organised by utility rather than flat, per the "folder mirrors namespace" convention — `NativeInterop/` (`NativeSession`, `NativeCallbacks`, `BufferedNativeIO`, `NativeImportResolver`, `SessionRegistry`, `EmbeddedJsonContext`, namespace `BWHazel.TopsyTurvy.Embedded.NativeInterop`) and `Analysis/` (`PhraseContext`, `AnalysisResult`, `DiagnosticInfo`, `HoverResult`, `CompletionResult`, `CompletionItemPayload`, namespace `BWHazel.TopsyTurvy.Embedded.Analysis`), with `NativeExports.cs` at the project root. **All types are `public`** (not `internal` + `InternalsVisibleTo`) — simpler and preferred absent a compelling reason otherwise; the `AssemblyInfo.cs`/`InternalsVisibleTo` attribute was removed. **Session/IO plumbing**: `NativeSession` (holds a `TopsyTurvyEnvironment`, a per-execution `CancellationTokenSource`, `LastError`, and `NativeCallbacks`); `NativeCallbacks` (a `public readonly unsafe struct` holding the two `delegate* unmanaged<...>` callback pointers — passed as two separate parameters to `CreateSession`, not a struct-by-value, per the "no structs by value" Android-reuse guidance; its `context` parameter doc was expanded to explain it's a caller "userdata" pointer the library never dereferences, only passes back so the caller can recover which session a callback invocation belongs to); `BufferedNativeIO : ITopsyTurvyIO` (mirrors `BufferedWebIO`'s pre-seeded stdin queue, but fires the `output_line` callback synchronously instead of buffering to a list) and `NativeImportResolver` (bridges `resolve_import` to `InterpreterExecutionOptions.SourceFileResolver`) both extract their previously-bare `512`-byte stackalloc-vs-heap threshold into a documented `MaxStackAllocBytes` constant. **`SessionRegistry`** (new): a real bug caught by testing, not anticipated in the plan — calling `GCHandle.FromIntPtr`/`.Free()` on a garbage or already-freed handle is *undefined runtime behaviour*, not a reliably catchable managed exception, and crashed the test host outright; `SessionRegistry` tracks currently-valid handles in a `HashSet<nint>` so `DestroySession`/`TryGetSession` can safely reject invalid/double-destroyed handles *before* ever touching a `GCHandle` API on a value they haven't vouched for. **`ExecuteProgramme` status codes** — a second real bug: `Interpreter.Execute` does not throw for cancellation or runtime errors, it catches both internally and returns them as diagnostics in the `DiagnosticCollection` result (confirmed by a standalone probe against the core `Interpreter`, unrelated to Embedded); the original `catch (OperationCanceledException)`/`catch (Exception)` design was therefore dead code for those two cases. Fixed to inspect `cancellationTokenSource.IsCancellationRequested` and `runtimeDiagnostics.HasErrors` after `Execute` returns (`0` success / `1` parse fail / `2` typecheck fail / `3` exception or runtime-error diagnostic / `4` cancelled). **Tests**: reused the existing `BWHazel.TopsyTurvy.Tests` project rather than a new one (`AllowUnsafeBlocks` + a `ProjectReference` to `Embedded` added), following the `Cli/OptionsResolvers/` in-process-unit-test precedent; new `Embedded/NativeExportsTests.cs` (incl. the mandatory surrogate-pair fixture — an emoji in a `YARN` literal, asserting identical diagnostic line/column against an ASCII stand-in of the same UTF-16 length) and `Embedded/TestCallbackCapture.cs`. `[UnmanagedCallersOnly]`-attributed methods cannot be called directly even in-process (`CS8901`) — every test obtains a `delegate* unmanaged<...>` via `&NativeExports.Method` first. 852 → 864 `BWHazel.TopsyTurvy.Tests`. **Apple naming**: `TopsyTurvy.xcodeproj` remains the umbrella project (like a `.sln`); the app target is renamed `Theatre` (`Theatre.app`, bundle ID `com.bwhazel.operetta.Theatre`, source folder `apps/apple/TopsyTurvy/Theatre/`, entry point `TheatreApp.swift`) and the XCFramework/module is renamed `TopsyTurvyToolchain` (`import TopsyTurvyToolchain`, built by `scripts/build-embedded-xcframework.sh` into `Frameworks/TopsyTurvyToolchain.xcframework`) so the umbrella/app/framework names read distinctly. `BWHazel.TopsyTurvy.Embedded.csproj`'s `AssemblyName` was also renamed, `topsyturvy` → `topsyturvytoolchain` (so the shipped native binary, `topsyturvytoolchain.dylib`, matches the `TopsyTurvyToolchain.xcframework`/module name it lives inside — every other project in the solution keeps `AssemblyName` equal to its project name, but this is the one artifact a Swift/Xcode developer actually links against, hence the deliberate exception); `scripts/build-embedded-xcframework.sh` needed no change, since it already resolves the dylib filename dynamically rather than hard-coding it. `topsyturvy.h`/`module.modulemap` renamed to `topsyturvytoolchain.h` to match (include guard `TOPSYTURVY_H` → `TOPSYTURVYTOOLCHAIN_H`); `AGENTS.md`'s Native Export sync table updated to the new filename. **Swift**: the XCTest bundle target is renamed `TopsyTurvyToolchainTests` (folder `apps/apple/TopsyTurvy/TopsyTurvyToolchainTests/`, own `Frameworks`/`Embed Frameworks` phases linking the XCFramework independently of the app target) and split one-type-per-file (`TopsyTurvyToolchainTests.swift` the `XCTestCase` only, `TestCallbackCapture.swift`, `AnalysisResult.swift`, `DiagnosticInfo.swift`) — the two C-callback targets stay top-level *functions*, not static methods, since Swift only permits forming a `@convention(c)` function pointer from a free function or a literal closure, never a static method (a build error caught this during the split). All 3 cases pass in Simulator (`xcodebuild test`); `Theatre` app target still builds. **Build/CI**: `scripts/build-embedded-xcframework.sh` promoted from ad hoc to permanent (`embedded-xcframework` Makefile target; new `apple-app` ("Apple Platforms App") job in `.github/workflows/build-test.yml`, `runs-on: macos-latest` — the first macOS CI runner in this repo — running the Embedded xUnit subset, the XCFramework build, and `xcodebuild test` against a Simulator destination). The job's `xcodebuild test -scheme` argument was caught pointing at the pre-rename scheme name (`TopsyTurvyTests`) after the Xcode test target was renamed to `TopsyTurvyToolchainTests` — fixed to match; this would otherwise have failed the very first time the job ran. `AGENTS.md` gained a "Native Export" synchronisation-point table. Verified: zero trim/AOT warnings on both RIDs (4.1 MB/slice, still well under the ~30 MB budget), full `operetta` solution builds clean, 864/864 `BWHazel.TopsyTurvy.Tests` green. Contract frozen at v1 per the Phase 1 exit gate; Phase 2 (app shell, editor bake-off, real UI) is out of scope here.
* **Last Updated (previous):** 2026-07-03 — Operetta Touring Theatre Phase 0 feasibility spike (`TOURING_THEATRE_PLAN.md` §10): proves the interpreter can be embedded as a Native AOT XCFramework and called from Swift. New `operetta/BWHazel.TopsyTurvy.Embedded/` project (`AssemblyName=topsyturvy`) references exactly the five core libraries (`Ast`, `Parser`, `TypeChecker`, `Analysis`, `Runtime`), all five of which gained `IsAotCompatible=true`; `PublishAot`/`PublishAotUsingRuntimePack`/`NativeLib=Shared`/`InvariantGlobalization`/`StripSymbols` publish it as a `.dylib` for `ios-arm64` and `iossimulator-arm64` with zero trim/AOT warnings on both slices (Superpower, the one third-party dependency in the referenced graph, cleared cleanly) — each slice is ~3.6 MB, well under the ~30 MB budget. Four `[UnmanagedCallersOnly]` exports in `NativeExports.cs`: `tt_api_version`, `tt_analyse` (parses + type-checks, returns a JSON `AnalysisSummary` via source-generated `System.Text.Json` — `EmbeddedJsonContext` — not reflection-based serialisation, the second AOT unknown this spike clears), `tt_execute` (parses, type-checks, interprets with a new no-op `NoOpTopsyTurvyIO`, returns a status code), and `tt_free` (releases `tt_analyse`'s returned buffer); every export wraps in try/catch and returns a sentinel on failure, per the FFI exception-wall principle in `TOURING_THEATRE_PLAN.md` §4.1 — treated as non-negotiable from this first spike rather than deferred. New `scripts/build-embedded-xcframework.sh` publishes both RIDs and combines them via `xcodebuild -create-xcframework` into `apps/apple/TopsyTurvy/Frameworks/TopsyTurvyEngine.xcframework` (dylib path resolved dynamically per RID, not hard-coded, anticipating .NET 11's `lib`-prefix change). The previously-empty `apps/apple/TopsyTurvy/TopsyTurvy.xcodeproj` shell gained its first real target: a minimal SwiftUI app (hand-rolled — manual framework embed + hand-written `topsyturvy.h`/`module.modulemap`, no SwiftPM binary target yet) whose `ContentView` calls `tt_api_version()` via `import TopsyTurvyEngine`. Verified end-to-end: `xcodebuild build` for the iOS Simulator destination succeeds, and the built app installs, launches, and renders "Topsy Turvy engine contract version: 1" in `iPhone 17` (arm64) Simulator. **Physical device confirmed too**: built and ran on a connected iPhone 17 Pro Max (Developer Mode enabled on-device, automatic provisioning via `xcodebuild -allowProvisioningUpdates -allowProvisioningDeviceRegistration` registered the device and regenerated the profile), installed and launched via `xcrun devicectl`, same "Topsy Turvy engine contract version: 1" confirmed on screen — the Phase 0 exit gate (§10) is now fully met. Full `operetta` solution and `BWHazel.TopsyTurvy.Tests` build/pass unaffected (852/852). Repo hygiene: `.gitignore` gained `xcuserdata/`, `*.xcuserstate`, and the generated `TopsyTurvyEngine.xcframework` path (regenerated on demand by `scripts/build-embedded-xcframework.sh`, same treatment as `bin/`/`obj/` — never hand-edited, never committed); only the hand-authored Swift sources, hand-written `topsyturvy.h`/`module.modulemap`, and the `.pbxproj`/`.xcworkspacedata` project files are tracked under `apps/apple/`. Phase 1 (full FFI contract, session handles, callbacks, Makefile/CI packaging) is deliberately out of scope for this spike.
* **Last Updated (previous):** 2026-07-03 — VS Code extension: four new UtopIR preview commands, `.topsy`-only for this first pass (`extensions/vscode/topsy-turvy/`). `topsy-turvy.pourUtopIr`/`viewUtopIrAstPromptbook`/`pourDotNetCil` mirror the existing `cuePreProcessor`/`viewAstPromptbook` `execFileAsync` + new-tab pattern exactly (`sorcerer <file> --emit <utopir|utopir-ast|dotnet-cil> --tiptoe`, unconditional `--tiptoe`). `topsy-turvy.prepareDotNetAssembly` prompts for an output path via `showSaveDialog`, then runs `sorcerer <file> --target dotnet --output <path>` (no `--tiptoe` — full panel/colour output, since this one runs visibly in the terminal rather than being captured) as a `vscode.Task` in the shared terminal panel (visible progress, since compiling can take a moment) rather than `execFileAsync`, and reports completion via a notification (success: "Open File Location" button via `revealFileInOS`; failure: points at the terminal). All four gated by one new master toggle, `topsy-turvy.enableUtopirPreviewCommands` (boolean, default `false` — off until UtopIR support is out of preview), referenced directly as `config.topsy-turvy.enableUtopirPreviewCommands` in every new menu contribution — the extension's first `when`-clause-gated (not just behavior-gated) setting; no `setContext` plumbing needed since VS Code resolves `config.*` in `when` clauses natively. Every `editor/title` (toolbar) entry, old and new, now carries an explicit `navigation@N` priority (`@1`–`@10`) rather than a bare `"navigation"` group, added after a report of toolbar buttons landing in the overflow menu unpredictably — this only controls default ordering (VS Code has no per-extension way to force a button to stay off the overflow menu; that's user-controlled via right-click hide/show/reorder on the toolbar itself). See the `extensions/vscode/topsy-turvy/` file inventory entry below for full detail. Verified: `npm run check-types`/`lint`/`compile` all clean; pre-existing `prettier --check` warnings on unrelated files confirmed present before this change (not introduced by it).
* **Last Updated (previous):** 2026-07-02 — Pluggable temporary variable naming in the UtopIR transformer, exposed via a new `--config varFormat` CLI key — the first genuinely recognized `--config` key. **Transformer**: naming logic extracted from `TopsyTurvyToUtopIRTransformer`'s three temp-generating call sites (arithmetic flattening, explicit `AS IT WERE` cast, implicit widening cast) into a new `ITemporaryVariableNameFormatter` abstraction (`operetta/BWHazel.TopsyTurvy.UtopIR.Transformer/VariableNameFormatters/`), supplied via constructor injection (`TopsyTurvyToUtopIRTransformer(ITemporaryVariableNameFormatter? formatter = null)`). The formatter owns composing the name entirely — the transformer only supplies raw ingredients (mnemonic/keyword + operand labels) via `CreateName(params string[] parts)`, it doesn't pre-join them. `IncrementingIntVariableFormatter` (the new default) — LLVM-SSA style `_0`, `_1`, `_2`, ..., ignoring the parts. `InstructionDetailVariableFormatter` — the original descriptive scheme (`_sum_a_b`), strengthened with per-part sanitisation (`.` → `p`, other non-`[a-zA-Z0-9_-]` → `-`, guarding against a future float/string literal producing an invalid identifier) and de-duplication (a repeated composed name gets a `_2`, `_3`, ... suffix, closing the name-clash risk the old implementation had). **CLI**: `--config varFormat:verbose|numeric` (case-insensitive) recognized by `UtopIrResolver`, `UtopIrAstResolver`, `DotNetCilOptionsResolver` via new `VariableFormatConfig`/`VariableNameFormat` — an invalid value throws `ToolchainConfigException` (a real error; every other `--config` key everywhere else is still unconditionally unrecognized, so this is the first case that can actually happen), an unrecognized key still just warns. `UtopIrResolver`'s `TOptions` changed from `NoOptions` to `VariableNameFormat` directly; `JsonEmitOptions` gained a third `Format` property (used by `UtopIrAstResolver`, ignored by `AstResolver`); new `CilTargetOptions(CilEmitOptions EmitOptions, VariableNameFormat Format)` wraps rather than modifies `CilEmitOptions`, keeping the transform-stage concern out of the `UtopIR.Emitters.Cil` project. `varFormat:verbose` given alongside `.utopir` input (no transform step) is reported as ignored via a warning (`numeric` is not warned about there, since it's indistinguishable from the unset default). New tests: `UtopIR.Tests/Transformer/InstructionDetailVariableFormatterTests.cs`/`IncrementingIntVariableFormatterTests.cs` (6); existing `TopsyTurvyToUtopIRTransformerTests` pinned to `InstructionDetailVariableFormatter` explicitly, since its assertions target descriptive names no longer produced by default; four new `Cli.E2ETests/SorcererCommandTests` covering both formats, the invalid-value error, and the `.utopir`-input warning — 852/852 `Tests`, 169/169 `UtopIR.Tests`, 100/100 `Cli.E2ETests`. Manually verified via the built binary: default numeric names, `--config varFormat:verbose` producing descriptive names for both `--emit utopir` and `--emit dotnet-cil`, an invalid value erroring out, an unrecognized key warning-and-continuing, and the `.utopir`-input ignored-warning.
* **Last Updated (previous):** 2026-07-02 — `sorcerer` command rearchitected around per-format/target resolvers, plus new aliases and rounded panels. **Design**: every `--emit`/`--target` string value now has its own `IEmitterOptionsResolver<TOptions>` implementation under the new `operetta/BWHazel.TopsyTurvy.Cli/OptionsResolvers/` directory, each owning three responsibilities — `GetAllowedExtensions()`/the default `CanEmit(filename)` (extension validation), `Apply(baseOptions, config, tiptoe) : TOptions` (merges `--config` on top of CLI-derived base options), and `Emit(filename, options, tiptoe) : int` (reads the file itself and performs the actual work — preprocessing, parsing, transforming, serializing, or compiling). `SorcererCommandBuilder` shrank from ~600 lines to a thin, uniform wrapper: check the file exists (universal), parse `--config`, switch on the `<str>` to instantiate the matching resolver (and, for the two `DotNetCilOptionsResolver`-backed cases, call its static `BuildEmitOptions(filename)`/`BuildTargetOptions(filename, output)` factory to build the base options), then drive it through `RunResolver<TOptions>` (`CanEmit` → `Apply` → `Emit`, with `ToolchainConfigException` caught and reported as a user error). Six resolvers: `PreprocessResolver`/`UtopIrResolver` (plain-text output, `TOptions` = new `NoOptions` empty record), `AstResolver`/`UtopIrAstResolver` (JSON output, `TOptions` = new `JsonEmitOptions(bool Abridged, bool Chromatic)` record — these two flags come from dedicated CLI options, not `--config`), `DotNetCilOptionsResolver` (backs both `--emit dotnet-cil` and `--target dotnet`, `TOptions` = `CilEmitOptions`). **Unrecognized `--config` keys are a non-fatal warning, not an error** — every resolver's `Apply` reports any key it doesn't recognize via the new `PanelHelper.ReportUserWarning` (panel or `--tiptoe` STDERR) and otherwise ignores it, then proceeds; `ToolchainConfigException`/`Apply`'s failure path is reserved for an invalid *value* on a key the resolver does recognize (none exist yet, so nothing throws today). This went through two iterations mid-session — first a hard error for any key, then relaxed to warn-and-continue per explicit direction, since an unrecognized key shouldn't block an otherwise-valid operation the way a malformed `key:value` token (still rejected before dispatch) or a genuinely bad value should. New `ToolchainOperations` class (`operetta/BWHazel.TopsyTurvy.Cli/ToolchainOperations.cs`, public, at the project root — not nested under `OptionsResolvers/`) holds `ParseAndCheck`/`GetUtopIrProgram` (parsing) and `WriteJson`, logic shared by more than one resolver; `PanelHelper` itself gained `ReportUserError`/`ReportUserWarning` (moved from an earlier internal `ResolverSupport` class, since removed — error/warning reporting belongs with the rest of the panel-writing logic, not a resolver-specific helper). **New CLI surface**: `--emit`/`-e` gains `--pour`/`-p`; `--target`/`-t` gains `--philtre`/`-f`; new repeatable `--config`/`-g` (aliases `--tablet`/`-l`) taking `key:value` pairs, with malformed entries (no `:`) rejected before dispatch. **Panels**: every Spectre.Console panel across the CLI now uses `Border = BoxBorder.Rounded` (`PanelHelper.cs`'s panels — including a new yellow-bordered `WriteWarning`, whose message text is now also wrapped in `[yellow]...[/]` markup, not just the border — and the three ad-hoc ones in `Repl/ReplSession.cs`), matching the REPL's already-rounded `TableBorder.Rounded` tables. New unit tests, all in `BWHazel.TopsyTurvy.Tests/Cli/OptionsResolvers/` (genuinely unit tests, not E2E — `BWHazel.TopsyTurvy.Tests/Cli/` already references the `Cli` project directly for in-process testing): `DotNetCilOptionsResolverTests` (7), `PreprocessResolverTests` (3), `AstResolverTests` (3), `UtopIrResolverTests` (3), `UtopIrAstResolverTests` (3) — 819 → 843 `BWHazel.TopsyTurvy.Tests`. **Real bug caught while writing these tests**: several resolver test classes redirect the process-global `Console.Out`/`Console.Error` to capture output; since xUnit runs different test classes in parallel by default, this caused cross-test output contamination (one test asserting on another's captured text) until a `[CollectionDefinition("ConsoleCapture", DisableParallelization = true)]` + `[Collection("ConsoleCapture")]` was added across all five affected classes. Manually verified end-to-end via the built binary: universal file-not-found check, `CanEmit`-driven extension rejection, an unrecognized `--config` key producing a yellow warning panel (rich mode) / `Warning: ...` line (`--tiptoe`) while the operation still succeeds, malformed `--config justfoo` still rejected outright, and a full `--target dotnet` compile-and-run round trip.
* **Last Updated (previous):** 2026-07-02 — Grammar alignment following UtopIR parser review: `specifications/UtopIR.ebnf`'s `Identifier` production (`Identifier = [a-zA-Z], { [a-zA-Z0-9_] }`) was itself inconsistent with the rest of the spec — its own inline comment and `UtopIR.md`'s worked examples already used `£_`-prefixed temporary register names (e.g. `£_sum_Peer1_Peer2`) that the strict letter-led rule couldn't parse. Corrected to `Identifier = ( "_" | [a-zA-Z] ), { [a-zA-Z0-9_-] } ;` — leading letter *or* underscore, plus hyphens now allowed mid-identifier (safe for UtopIR, unlike Topsy Turvy, since arithmetic uses keyword mnemonics rather than symbolic operators, so there is no ambiguity to guard against). `UtopIR.md` §3.2/§3.2.2 updated to match: identifiers now reference UtopIR's own grammar rather than "any valid name supported in Topsy Turvy" (inaccurate — no reserved-word filtering applies), and the `£_` temporary-register prefix is now described purely as a transformer naming convention, not a grammar requirement. `BWHazel.TopsyTurvy.UtopIR.Parser.Lexer.Identifier` already matched this (implemented ahead of the spec catching up — see the previous entry below); no parser code changes were needed, only the spec/grammar files and the parser's own doc comments referencing the old "known discrepancy" framing (now removed, since it is no longer a discrepancy).
* **Last Updated (previous):** 2026-07-02 — New `BWHazel.TopsyTurvy.UtopIR.Parser` project (Superpower-based, mirrors `BWHazel.TopsyTurvy.Parser`'s house style) completes the UtopIR toolchain: hand-written or previously-emitted `.utopir` source text can now be parsed back into a `UtopIRProgram`, not just produced from Topsy Turvy source. Files: `Lexer.cs` (atomic terminals — `Variable`, `StringLiteral`/`CharacterLiteral`, `FloatLiteral`/`IntegerLiteral`/`BoxIntegerLiteralValue`, `BooleanLiteral`, `Comment`, `Identifier`, `Keyword(string)`), `OperandParser.cs` (`Type`, `ArithmeticOp`, `Literal`, `Operand`), `InstructionParser.cs` (one parser per instruction form plus the reusable `InstructionSequence` combinator), `UtopIRDiagnostic.cs`/`UtopIRParseResult.cs`/`UtopIRSyntaxException.cs`/`UtopIRParser.cs` (public entry point: `TryParse`/`Parse`). Deliberately dependency-free of `BWHazel.TopsyTurvy.Ast` (only references `UtopIR.Ast`), and — confirmed with the user — the UtopIR AST gets no `Span` property in this work (deferred to whenever LSP/syntax-highlighting support for UtopIR is actually built; diagnostics get line/column directly from Superpower's own `Result<T>.ErrorPosition`). **Nesting-ready design**: `InstructionParser.InstructionSequence(TextParser<Unit>? terminator = null)` is a single recursive combinator (not a flat C# line-loop) so that when UtopIR later grows nested block instructions (functions/classes, reusing the same instruction forms per the user's design intent), a body's `InstructionSequence` can be invoked recursively with a different terminator — additive, not a rewrite. **Real bug found during implementation, not anticipated in the original plan**: `specifications/UtopIR.ebnf`'s `Identifier = [a-zA-Z], { [a-zA-Z0-9_] }` requires a leading letter, but `TopsyTurvyToUtopIRTransformer`'s auto-generated temporary register names (e.g. `£_sum_Peer1_Peer2`) start with an underscore — the strict grammar as written cannot parse real transformer output. `Lexer.Identifier` was relaxed to accept a leading underscore (`Character.Letter.Or(Character.EqualTo('_'))`), documented in an XML `<remarks>` block as a known EBNF-vs-toolchain discrepancy, not a deliberate grammar extension. Two other real bugs caught by a live scratchpad smoke test before formal unit tests were written: (1) `Lexer.Whitespace`/`WhitespaceRequired` initially used `Character.WhiteSpace` (matches `\n`/`\r` too), which greedily consumed the newlines the line-oriented grammar needs explicitly — fixed to `Character.In(' ', '\t')`; (2) a static-field-initialization-order bug (`Variable`'s initializer referenced `Identifier`, declared later in the file) — fixed by reordering. CLI wiring: `SorcererCommandBuilder.GetUtopIrProgram(filename, tiptoe)` now dispatches on extension (`.topsy` → existing parse/type-check/transform path; `.utopir` → `UtopIRParser.TryParse`, reporting `UtopIRDiagnostic`s via the existing `ReportUserError`/`PanelHelper` pattern on failure) — the former "UtopIR source files are not yet supported" hard-rejection is gone; `--emit utopir-ast`/`--emit dotnet-cil`/`--target dotnet` all now genuinely accept `.utopir` input. New tests: `BWHazel.TopsyTurvy.UtopIR.Tests/Parser/` (`LexerTests`, `InstructionParserTests`, `UtopIRParserTests` incl. a UtopIRCodeGenerator/UtopIRParser round-trip test — 163/163 UtopIR.Tests total, was 120); `BWHazel.TopsyTurvy.UtopIR.E2ETests/UtopIrSourcePipelineIntegrationTests.cs` (hand-written `.utopir` source parsed → `CilEmitter` → loaded/invoked/exit-code-asserted assembly, incl. a `were`-cast widening case — 7/7 UtopIR.E2ETests total, was 4); `SorcererCommandTests` updated (the three former "not yet supported" tests now assert genuine success; one new test asserts a malformed `.utopir` file produces a real syntax-error diagnostic rather than crashing — 23/23 `SorcererCommandTests`, 96/96 `Cli.E2ETests` total). Manually smoke-tested via the built CLI binary against the worked example from `specifications/UtopIR.md`: `sorcerer prog.utopir --emit utopir-ast --tiptoe`, `--emit dotnet-cil --tiptoe`, and default `--target dotnet` followed by actually running the produced `.dll` (`dotnet prog.dll`, exit code `72` for `42 + 30`, matching the worked example). **Known, deliberately out-of-scope gap**: no grammar support for narrow/unsigned-typed literal syntax — a hand-written `.utopir` file appointing a bare int-boxed literal to a narrower/unsigned-typed variable (e.g. `£x = welcome pirate` / `£x = appoint 500000`) can still produce invalid IL; this is the same class of "declared type vs. literal's own CLR type" gap already fixed once for the Topsy-Turvy-source path (`TransformDeclaration`/`TransformAssignment`'s `CastOperandIfNeeded`) but not present when authoring UtopIR directly.
* **Last Updated (previous):** 2026-06-30 — Three post-implementation fixes to the programme return value feature: (1) **CLI exit code not propagating**: `PerformCommandBuilder.HandlePerform` was returning hard-coded `0` on success, overriding the interpreter's exit code; fixed by adding `int ExitCode` to `ProgramExecutionResult`, threading it from `interpreter.ExitCode` through `ProgramRunner.Run(exitCode)` to `PerformCommandBuilder` which now `return result.ExitCode`; `ProgramRunner` no longer sets `System.Environment.ExitCode` directly. (2) **Visual node kind wrong**: `CreateProgrammeReturnNode` and the `VisualNodeFactory` entry were using `VisualNodeKind.Program`, which caused `VisualNodeWidget` to render the HARK! title/subtitle text-field UI; changed to `VisualNodeKind.Function` (same as `ReturnNode`). (3) **StatementParser and TopsyTurvyParser XML docs**: added `#### Programme Return` and `#### Top-Level Statement` subsections to the "Programme Structure" section (count 2 → 4); updated "Main Entry Points" to describe both `TopLevelStatement` (programme body) and `Statement` (all other contexts); updated `TopsyTurvyParser` programme-body bullet to reference `StatementParser.TopLevelStatement`. Previously: Programme return value: `ProgrammeReturnNode` (new AST node) and `ProgrammeReturnSignalException` (new internal signal) added; `StatementParser` gains `ProgrammeReturn` parser and `TopLevelStatement` combinator (tries `ProgrammeReturn` before `Statement`); `TopsyTurvyParser.ProgramParser` body now uses `TopLevelStatement`; `Interpreter` gains `ExitCode` property (reset to `0` on each `Execute` call, set from caught `ProgrammeReturnSignalException`); `TopsyTurvyCodeGenerator` handles `ProgrammeReturnNode` (emits `AND SO I FIND <expr>`); `VisualGraphBuilder` adds `CreateProgrammeReturnNode`; `VisualGraphToAstConverter` adds `ReconstructProgrammeReturnFactory` and `ReconstructProgrammeReturn`; `VisualNodeFactory` adds `"ProgrammeReturnNode"` entry; `VisualContextMenu.razor` adds programme return menu item under Statement; `SPEC.md` §1 documents `AND SO I FIND` at top level. Test count: 814 → 819 (`BWHazel.TopsyTurvy.Tests`). Previously: Loop step + free start: `LoopNode.Step` (`Expression?`) added; `LoopDefinition` gains `Step` parameter (default `null`); `LoopStepParser` added to `StatementParser` matching optional `BY <expr>` between loop variable and `UNTIL`; `ExecuteAscendingLoop` no longer resets variable to 0 on entry; both ascending/descending loops evaluate `Step` expression each iteration (defaulting to `1` when absent); `TopsyTurvyCodeGenerator` emits ` BY <expr>` when `Step != null`; `VisualGraphBuilder`/`VisualGraphToAstConverter` gain a `Step` DataIn port on loop opener nodes; `VisualNodeFactory` adds `Step` DataIn port to factory-created loop blocks; `BY` added to `KeywordData`, `ReplHighlighter`, TextMate grammar loops group, Monaco tokenizer + completion list; `SPEC.md` §12 updated. Test count: 806 → 814 (`BWHazel.TopsyTurvy.Tests`). Previously: EITHER parser bug fixed: `SingleOrExpression` added to `ExpressionParser`; `PrefixExpression` routes `Operator.Either` through `OR` separator; `OR` added to `KeywordData`, `SourceFormatter`, `ReplHighlighter`, Monaco tokenizer, TextMate grammar; `GRAMMAR.ebnf` updated to split `FixedOpExpression` into `EitherExpression` and `OtherFixedOpExpression`. `dotnet test BWHazel.TopsyTurvy.WebEditor.E2ETests` — 11/11 (was 6/6 — Playwright suite grew). Previously: Visual editor Phase D/E: body-sync bug fixed — all builder-backed `Reconstruct*` methods (loop, conditional, guard, try-catch, switch, function) previously called `ReconstructBodyStatements(original.Body)` which iterated the frozen original AST, ignoring live graph edits; replaced throughout with `WalkFlowStatements`/`WalkBranchBody`. `WalkFlowStatements` gains a `bool skipFirst = true` parameter (with XML doc); `IsBranchHeaderSentinel` determines whether a branch entry node needs `skipFirst = false`. ONCE MORE./THAT WILL DO. both have `flowOut: false` (consistent). `CreateTryCatchBlock` in `VisualNodeFactory` now pre-creates `WITH GRATITUDE` (TryCatchSuccessBranch) and `MODIFIED RAPTURE,` (TryCatchErrorBranch) header nodes at factory time. Function declaration node gains full editing controls: name text field (→SymbolIdentifierNodeName), void checkbox (→NodeLiteralType null/non-null), return type dropdown, "+ Add Parameter" button; each parameter creates a TERM node of `VisualNodeKind.Parameter` (dark blue `#1565c0`) connected via "Param N" DataIn; parameters appear in Symbols panel under a new "Parameters" expansion panel with `(FuncName)` suffix. `VisualSymbolKind.Parameter` added; `VisualSymbolEntry.OwnerName` optional property added. TryCatchOpener gains a "Caught Variable" text field (→SymbolIdentifierNodeName, subtitle `catch: {v}`). SUMMON redesign settled: FlowIn + DataIn "Function" + DataOut "Out" + FlowOut; FunctionReferenceNode (new) created when dragging a function from the Symbols panel — has named DataIn ports per parameter + DataOut "Out"; user connects FunctionReferenceNode DataOut to SUMMON's "Function" DataIn; `ReconstructSummonFromNode` reads argument expressions from FunctionReferenceNode's DataIn ports. `VisualGraphBuilder.CreateFunctionBodySubgraph` now creates TERM nodes (not DataOut ports on the opener) to match the factory layout, so code→visual rebuild populates the Parameters section correctly. `THE PROPS` pre-seeded in `VisualSymbolScanner` as a Variable entry. Symbol panel is resizable via a drag handle: `[JSInvokable] SetPanelWidth(int)` + `window.visualSymbolPanel.startResize` JS. Warning overlay moved to bottom-right (`right: 1rem`). `.visual-node-flow-bottom { gap: 32px }` and `.visual-node-flow-bottom-item { min-width: 56px }` added to app.css. `ExpressionNodeTypes` includes `"FunctionReferenceNode"`. `implement-language-feature.md` step numbering corrected (was: duplicate 3g/3h/3j labels; now: sequential 3a–3l with code generator at 3k). `docs/concepts/analysis.md` Code Generator subsection added with AST-object-tree → source example.
* **Specification Version:** 0.5.0 — `SPEC.md` is the grammar source of truth
* **File Extension:** `.topsy`
* **Baseline:** `dotnet build operetta/BWHazel.TopsyTurvy.slnx --no-incremental` — 0 errors, 0 warnings. `dotnet test operetta/BWHazel.TopsyTurvy.Tests` — 864/864. `dotnet test operetta/BWHazel.TopsyTurvy.Cli.E2ETests` — 100/100. `dotnet test operetta/BWHazel.TopsyTurvy.WebEditor.E2ETests` — 11/11 Playwright (requires PowerShell Core — see §4). `dotnet test operetta/BWHazel.TopsyTurvy.UtopIR.Tests` — 169/169. `dotnet test operetta/BWHazel.TopsyTurvy.UtopIR.E2ETests` — 7/7. `npm run test:grammar` — 4 files/all passing. `npm run test:unit` — 4/4.

| Component | Status |
|---|---|
| AST (`BWHazel.TopsyTurvy.Ast`) | Complete |
| Parser (`BWHazel.TopsyTurvy.Parser`) | Complete |
| Runtime (`BWHazel.TopsyTurvy.Runtime`) | Complete |
| Analysis (`BWHazel.TopsyTurvy.Analysis`) | Complete |
| Integration — all 4 examples pass | Complete |
| LSP server + VS Code extension | Complete |
| CLI (`operetta`) | Complete |
| Web editor (`BWHazel.TopsyTurvy.WebEditor`) | Complete (Blazor WASM) |
| UtopIR AST (`BWHazel.TopsyTurvy.UtopIR.Ast`) | v0.0.1-preview1 — declaration/assignment/arithmetic/return |
| UtopIR Transformer (`BWHazel.TopsyTurvy.UtopIR.Transformer`) | v0.0.1-preview1 — TT AST → UtopIR instruction list |
| UtopIR Analysis (`BWHazel.TopsyTurvy.UtopIR.Analysis`) | v0.0.1-preview1 — UtopIR AST → source text |
| UtopIR CIL Emitter (`BWHazel.TopsyTurvy.UtopIR.Emitters.Cil`) | v0.0.1-preview1 — integer types, arithmetic, `Library`/`Executable` output |
| UtopIR Parser (`BWHazel.TopsyTurvy.UtopIR.Parser`) | v0.0.1-preview1 — Superpower-based `.utopir` source text parser |
| Embedded engine (`BWHazel.TopsyTurvy.Embedded`) | Phase 1 v1 contract frozen — 9 `UnmanagedCallersOnly` exports (`topsyturvy_*`; session lifecycle, analyse/hover/complete/format/execute/cancel/last-error/free), Native AOT `.dylib` for `ios-arm64`/`iossimulator-arm64`; `topsyturvy_tokens` deliberately deferred. Public API, organised into `NativeInterop/`/`Analysis/`. CI-verified via `embedded-apple` job on `macos-latest`; Swift `TopsyTurvyToolchainTests` XCTest bundle (linked against `TopsyTurvyToolchain.xcframework`, consumed by the `Theatre` app target) and 12 in-process xUnit cases both green |

---

## 1. File Inventory

| File / Directory | Role |
|---|---|
| `AGENTS.md` | Project rules and constraints — read before starting any work. |
| `.actrc` | Portable `act` configuration for local workflow testing. |
| `scripts/act.sh` | Wrapper for `act` — detects host architecture and Podman socket, then delegates all arguments to `act`. Run from repo root: `scripts/act.sh push`. |
| `.github/workflows/build-test.yml` | Reusable CI workflow — triggered on `push` and `workflow_call`. Two parallel jobs: `language-toolchain` (full .NET build + unit/CLI E2E/WebEditor E2E tests) and `vscode-extension` (CLI build + extension compile, grammar, unit, and integration tests via `coactions/setup-xvfb`). VS Code integration tests require `libgtk-3-0` and related libraries installed via `apt-get`. |
| `.github/workflows/publish.yml` | Manual publish workflow — calls `build-test.yml`, then cross-compiles for `win-x64`, `linux-x64`, `linux-arm64`, `osx-x64`, `osx-arm64`, producing CLI `.tar.gz` and `.vsix` artifacts per platform. |
| `SPEC.md` | Authoritative language specification v0.5.0. |
| `GRAMMAR.ebnf` | Formal EBNF grammar v0.4.0 — blueprint for the parser. |
| `examples/` | `hello_world.topsy`, `fizzbuzz.topsy`, `fibonacci.topsy`, `pirates_calculator.topsy` — all execute correctly. `examples/language/arrays.topsy` demonstrates array declaration, element access, element assignment, truthiness, and WHILST-loop traversal. |
| `operetta/BWHazel.TopsyTurvy.slnx` | Solution file. |
| `operetta/BWHazel.TopsyTurvy.Ast/` | AST node types. All inherit from `Node` (`required SourceSpan Span`). Also defines `Diagnostic`, `DiagnosticCollection`, `SourceSpan`, `SourceLocation`. `TryCatchNode` carries optional `CaughtValueName` (null = no binding) for `MODIFIED RAPTURE, Name` syntax. `InPlaceCastNode` extends `Statement` directly (no shared `TypeCastNode` base). `ExpressionCastNode` extends `Expression` — the non-mutating cast is an expression, not a statement. `TypeCastNode` has been removed. `ArrayDeclarationNode` — `PRAY WELCOME arr AS A [CONSERVATIVE] LITTLE LIST OF [<size>] <type> [BEING ...]`; carries `int? Size` (pre-allocation count, null = no size). `ArrayIndexNode` extends `Expression` — `VICTIM <index> ON <arr>`. `ArrayElementAssignmentNode` extends `Statement` — `VICTIM <index> ON <arr> IS APPOINTED <value>`. `ArrayLengthNode` extends `Expression` — `RECKONING OF <arr>`; carries only `string ArrayName`. `TernaryExpressionNode` extends `Expression` — `TrueValue SHOULD IT TRANSPIRE THAT Condition OTHERWISE, FalseValue`; carries `TrueValue`, `Condition`, `FalseValue` (all `Expression`). `GuardNode` extends `Statement` — `YEOMAN Condition OTHERWISE, Block UNDER ORDERS.`; carries `Condition: Expression` and `ElseBlock: IReadOnlyList<Statement>`. `AssertNode` extends `Statement` — `THE LAW IS Condition THAT ErrorMessage`; carries `Condition: Expression` and `ErrorMessage: Expression`. `FunctionDefinitionNode` carries `Parameters: IReadOnlyList<TypedParameter>` (each record: `Name`, `LiteralType Type`, `SourceSpan Span`) and `ReturnType: LiteralType?` (`null` = void, i.e. no `TO FIND` clause). `GenerateDocumentationFile` is enabled; XML documentation comments are present on all types. |
| `operetta/BWHazel.TopsyTurvy.Analysis/` | Shared analysis library. `SymbolTable`, `SymbolInfo` (carries `IsConstant`, `Documentation`, `TypedParameters: IReadOnlyList<TypedParameter>?`, and `TypeDisplayName`), `SymbolKind`, `DocumentState`, `DepthAction`. `SourceFormatter` — keyword normalisation + libretto indentation. `SourceAnalyser` — `FindSkipRanges`, `BuildLineOffsets`, `IsInSkipRange`, `FindWordOccurrences`, `CountOccurrences`. `KeywordData` — 96-entry keyword list (authoritative source of truth for LSP/completion — must not be duplicated in `Keywords.cs`; now includes `CONSERVATIVE`, `LIBERAL`, 8 documentation comment tag keywords, `YEOMAN`, `UNDER ORDERS.`, `THE LAW IS`, `CHANCELLOR`, `PIRATE`, `SAUSAGE-ROLL`, `STANDING`, `FOOT`, `STITCH`). `HoverMarkdownBuilder` — Markdown hover strings; shows `(constant)` for `CONSERVATIVE` variables; now also renders documentation comment sections (summary, remarks, parameters, return, exceptions, examples, see-also, deprecation). `DocumentationComment` — record holding parsed documentation comment data. `DocumentationCommentParser` — public static class that parses block comment content into a `DocumentationComment`. `DocumentationCommentParseState` — public sealed class holding all mutable state accumulated during a single `Parse` pass; lives in its own file. `LiteralTypeToDisplayName` is `public static` (used by `ReplSession.WriteArmoury` to display type names in the `:armoury` table). `TopsyTurvyCodeGenerator` — AST → Topsy Turvy source emitter; entry `Generate(ProgramNode) : string`; `WriteStatement` dispatch covers all statement types; `WriteExpression` covers all expression types; `OperatorKeyword` and `TypeKeyword` map enums to source strings. References: Ast, Parser. Referenced by: LanguageServer, Cli, WebEditor. `GenerateDocumentationFile` is enabled; XML documentation **complete** for all types. |
| `operetta/BWHazel.TopsyTurvy.Parser/` | Superpower parser. Entry: `TopsyTurvyParser`. Pre-processors: `CommentsPreProcessor`, `VictorianFlourishPreProcessor`. `SourceMap`/`SourceMapping` for offset tracking (`SourceMapping.TransformedOffset` maps a position in the pre-processed text to its original line/column). Pre-processor output carried in `PreProcessResult(TransformedText, SourceMap)`. `TryParse` returns `ParseResult(ProgramNode?, IReadOnlyList<Diagnostic>)`. `TypeKeyword` lives in `ExpressionParser` (moved from `StatementParser` to avoid a circular initialisation dependency). `ExpressionParser.ExpressionCast` is wired into both `Expression` alternatives and `ExpressionStatementParser`. `StatementParser.TypeCast` now only handles in-place casts. `StatementParser.ArrayDeclaration` parses `PRAY WELCOME arr AS A [CONSERVATIVE] LITTLE LIST OF [<size>] <type> [BEING ...]` — after `AS A` is consumed (4 chars including trailing `A`), the remaining token is `LITTLE LIST OF` not `A LITTLE LIST OF` (see §2); the optional size is parsed with `Ws(Lexer.IntegerLiteral).Select(v => checked((int?)v)).Try().OptionalOrDefault(null)` between the `LITTLE LIST OF` keyword and the type keyword — `checked` combined with the trailing `.Try()` makes an out-of-range array size fail this parser alternative (falling back to "no size given") rather than silently wrapping, now that `Lexer.IntegerLiteral` returns `long` (see below). `Lexer.IntegerLiteral` returns `long` (not `int`) so integer literals of any magnitude parse without wrapping; `Lexer.BoxIntegerLiteralValue(long)` is the single shared place that decides the boxed CLR type (boxed `int` if it fits, otherwise boxed `long`) — used by both `ExpressionParser.LiteralExpression` (to resolve `LiteralType.Integer` vs `.Long`) and `StatementParser.SwitchBody`'s case-literal parsing (so a case value and the value it's compared against always box to the same CLR type, since `object.Equals` on differently-boxed numeric types is otherwise `false` even for equal values, e.g. `2L.Equals(2)` is `false`). `StatementParser.ArrayElementAssignment` parses `VICTIM <index> ON <arr> IS APPOINTED <value>`. `ExpressionParser.ArrayIndexExpression` parses `VICTIM <index> ON <arr>`. `ExpressionParser.ArrayLengthExpression` parses `RECKONING OF <arr>` — wired into both `ExpressionParser.Expression` and `StatementParser.ExpressionStatementParser` (see §2). `PrincipalBlock` accepts `Ws(ArrayDeclaration.Or(Declaration)).Try().Many()` to handle mixed declaration lists. All AST nodes produced by the parser carry real source spans: `TopsyTurvyParser` sets `ParserHelpers.ActiveSourceMap` (thread-local) to the `SourceMap` from the pre-processor before calling any combinator, and clears it in a `finally` block; every LINQ parser chain captures `startOffset` and `endOffset` via `ParserHelpers.CurrentOffset` and calls `ParserHelpers.BuildSpan` at node construction time. `FunctionDefinition` additionally captures per-parameter offsets to populate `FunctionDefinitionNode.ParameterSpans`. `GenerateDocumentationFile` is enabled; XML documentation **complete** for all types. |
| `operetta/BWHazel.TopsyTurvy.Parser/ParserHelpers.cs` | `internal static class` providing shared helpers used across `StatementParser`, `ExpressionParser`, and `TopsyTurvyParser`: `PlaceholderSpan` (the `(0,0)-(0,0)` fallback used in isolated unit tests where no `SourceMap` is active); `ActiveSourceMap` (`[ThreadStatic]` field set by `TopsyTurvyParser` before each parse and cleared in a `finally` block — the communication channel between the entry-point and the static parser combinators); `CurrentOffset` (a zero-consuming parser that returns the current absolute cursor offset); `BuildSpan(startOffset, endOffset)` (converts two pre-processed offsets to a `SourceSpan` via `ActiveSourceMap`, or falls back to `PlaceholderSpan`); `WithOffsets<T>(parser)` (wraps any parser to also return start/end offsets, used for branched `.Select()` chains where inserting `from` clauses would be unwieldy); and `Ws<T>(parser)` (wraps a parser with a required-whitespace prefix). All parser files import it via `using static`. |
| `operetta/BWHazel.TopsyTurvy.Parser/ConditionalBodyInfo.cs` | `public record` carrying the parsed body of a conditional statement — `TrueBlock`, `ElseIfs`, and `ElseBlock`. Replaces the anonymous tuple previously returned by the `ConditionalBody` parser, following the pattern established by `LoopDefinition`. |
| `operetta/BWHazel.TopsyTurvy.Parser/SwitchBodyInfo.cs` | `public record` carrying the parsed body of a switch statement — `Cases` and `DefaultBlock`. Replaces the anonymous tuple previously returned by the `SwitchBody` parser. |
| `operetta/BWHazel.TopsyTurvy.TypeChecker/` | Two-pass type checker. Entry: `TopsyTurvyTypeChecker.Check(ProgramNode)` → `TypeCheckResult`. Pass 1 (`CollectFunctionSignatures`) walks all `FunctionDefinitionNode` instances — including nested ones — and stores a `FunctionSignature` (`IReadOnlyList<LiteralType> ParameterTypes`, `LiteralType? ReturnType`) in the `SemanticModel` to support forward references. Pass 2 (`TypeCheckVisitor`) walks every node carrying three parallel scope stacks: `scopeStack` (variable/parameter name → `LiteralType`, one frame per block), `arrayElementTypeStack` (array name → element `LiteralType`, mirrors `scopeStack` frame structure), `functionReturnTypeStack` (`LiteralType?` per function, `null` = void). The root scope is pre-seeded with `THE PROPS` → `LiteralType.Array` (element type `LiteralType.String`) so the built-in is always resolvable. `InferIdentifier`, `CheckAssignment`, and `CheckInput` emit `Error` diagnostics for undeclared names. Expression casts (`AS IT WERE`) are trusted at compile time — type safety is delegated to the runtime. `TypeCheckResult` exposes `SemanticModel` and `IReadOnlyList<Diagnostic>`; `Success` = no error-severity diagnostics. References: Ast only. Referenced by: Cli, LanguageServer. |
| `operetta/BWHazel.TopsyTurvy.Runtime/` | Tree-walking interpreter. Entry: `Interpreter`. Key types: `TopsyTurvyValue` (now supports `LiteralType.Array` backed by `List<TopsyTurvyValue>`; `Array(List<TopsyTurvyValue>)` factory; `IsTruthy()` returns `true` for non-empty arrays; `CastTo()` allows cast only to `YARN` or `DECREE`; `ToString()` renders `[elem1, elem2, ...]`; factory method `Double()` — previously `Float()`), `TopsyTurvyEnvironment` (tracks constants via `HashSet<string>`; `Declare` accepts optional `isConstant`; `Assign` enforces the constant constraint; `IsConstant(name)` query; `IsConstantInChain(name)` walks enclosing scope chain — required for array element mutation which bypasses `Assign`; `GetVariables()` returns `IReadOnlyDictionary<string, TopsyTurvyValue>` for the current scope), `ITopsyTurvyIO`/`ConsoleIO`. `Interpreter` exposes `Functions` (`IReadOnlyDictionary<string, FunctionDefinitionNode>`) for external inspection (used by `:armoury`). Private helpers: `ResolveStringCharacter` (mirrors `ResolveArrayElement` for YARN indexing); `IntegerTypeRank` (named `private static int` method, extracted from a local function in `GetWidestIntegerType`). Signal exceptions: `BreakSignalException`, `ContinueSignalException`, `ReturnSignalException`, `TopsyTurvyThrowException`. `Execute(program, cancellationToken, options?)` — pass `InterpreterExecutionOptions.ExecutionTimeout` for Blazor WASM timeout (see §2). `ExecuteTryCatch` creates a child scope via `CreateNested()` when `CaughtValueName` is set. `GenerateDocumentationFile` is enabled; XML documentation **complete** for all types. |
| `operetta/BWHazel.TopsyTurvy.Runtime/InterpreterExecutionOptions.cs` | Positional record consolidating the optional `Execute` parameters: `ExecutionTimeout` (`TimeSpan?`), `SourceFilePath` (`string?`), `SourceFileResolver` (`Func<string, string?>?`), `ProgrammeArguments` (`IReadOnlyList<string>?`, default `null`). Pass `null` for any unused option. |
| `operetta/BWHazel.TopsyTurvy.Tests/` | xUnit suite — 852/852 passing. Five subdirectories: `Analysis/`, `Cli/` (incl. `Cli/OptionsResolvers/`), `Parser/`, `Runtime/`, `LanguageServer/`; shared base classes in each. `Superpower.Model` types available transitively — no direct package reference needed. Key test files: `Analysis/DocumentationCommentParserTests.cs` (23), `Analysis/TopsyTurvyCodeGeneratorTests.cs` (72 — round-trip coverage for all type keywords, operators, statement forms, block constructs, array operations, expressions, and programme structure), `Cli/ReplHighlighterTests.cs` (20), `Cli/OptionsResolvers/*Tests.cs` (20 across five classes — see the `Cli/OptionsResolvers/` file inventory entry below; `ConsoleCaptureCollection.cs` defines a `[CollectionDefinition("ConsoleCapture", DisableParallelization = true)]` all five apply via `[Collection("ConsoleCapture")]`, since each test redirects the process-global `Console.Out` to capture STDOUT and xUnit parallelizes different test classes by default — a real cross-test contamination bug was caught and fixed this way), `Parser/TopsyTurvyParser*Tests.cs` (array, ternary, guard, assert, primitive types, span coverage, and others), `Runtime/TopsyTurvyInterpreter*Tests.cs` (array, ternary, guard, assert, primitive types, and others), `LanguageServer/*Tests.cs`. See AGENTS.md for test conventions. |
| `operetta/BWHazel.TopsyTurvy.Cli/` | CLI project. Binary: `operetta`. Version: `0.1.0`. Dependencies: `Spectre.Console 0.55.2`, `System.CommandLine 2.0.8`, `OmniSharp.Extensions.LanguageServer 0.19.9`. References `BWHazel.TopsyTurvy.LanguageServer` (class library) and, as of the `sorcerer` redesign below, all five UtopIR projects (`UtopIR.Ast`, `UtopIR.Analysis`, `UtopIR.Parser`, `UtopIR.Transformer`, `UtopIR.Emitters.Cil`). Published as a single self-contained executable. **`sorcerer` redesign (breaking, acceptable pre-1.0)**: `sorcerer` (now aliased `compile`, `dev` alias dropped) is no longer a dev-tools container — the former `cue`/`promptbook` subcommands are removed and folded into a single `--emit`/`-e <format>` option (`preprocess`/`p`/`cue`/`c`, `ast`/`a`/`promptbook`/`b`, `utopir`/`u`, `utopir-ast`/`s`, `dotnet-cil`/`d`), which prints plain text (or JSON for `ast`/`utopir-ast`) to STDOUT. A `--target`/`-t <target>` option (only `dotnet`, the default when neither flag is given) builds a real artifact instead — a runnable executable (`CilOutputKind.Executable`, with `runtimeconfig.json`) written to `--output`/`-o` (default: input filename with its extension changed to `.dll`). If both `--emit` and `--target` are given, `--emit` wins (order-independent). `--abridged`/`-a` and `--chromatic`/`-c` (JSON formatting) and `--tiptoe` (suppress panels/colours) carry over from the old `promptbook`/`cue` commands and apply to `ast`/`utopir-ast`. File extension is validated per format/target: `.topsy` only for `preprocess`/`ast`/`utopir`; `.topsy` or `.utopir` for `utopir-ast`/`dotnet-cil`/`target dotnet`. **UtopIR source support (added with the `UtopIR.Parser` project)**: `.topsy` inputs follow the existing parse/type-check/`TopsyTurvyToUtopIRTransformer` path; `.utopir` inputs are read and parsed via `UtopIRParser.TryParse`, reporting each `UtopIRDiagnostic` via `PanelHelper.ReportUserError` (see `ToolchainOperations.cs` and the `Cli/OptionsResolvers/` entry below) on failure — `.utopir` input to `utopir-ast`/`dotnet-cil`/`target dotnet` is now genuinely parsed and compiled, no "not yet supported" rejection. `incantation` (alias `lsp`) is promoted from a `sorcerer` subcommand to a root-level command — this changes the actual LSP launch invocation, so `extensions/vscode/topsy-turvy/src/extension.ts` was updated from `['sorcerer', 'incantation']` to `['incantation']`, and its Cue/Promptbook command handlers from `['sorcerer', 'cue', ...]`/`['sorcerer', 'promptbook', ...]` to `['sorcerer', ..., '--emit', 'preprocess'/'ast', ...]`. New files: `UtopIRInstructionJsonConverter.cs`/`UtopIROperandJsonConverter.cs` (mirror `NodeJsonConverter.cs`'s reflective `$type`-discriminator pattern for the UtopIR AST's two separate abstract hierarchies, `UtopIRInstruction` and `UtopIROperand`, since neither derives from the Topsy Turvy `Ast.Node` the existing converter targets). **Resolver-based rearchitecture (`--config`/`--pour`/`--philtre` follow-up)** — see the `Cli/OptionsResolvers/` entry below and the top-of-file dated entry: `SorcererCommandBuilder` itself now only checks the file exists, parses `--config`, switches on the `<str>` value to instantiate the matching resolver, and drives it through `RunResolver<TOptions>` — every format/target-specific concern (extension validation, file reading, parsing/transforming, writing output) moved into the resolvers themselves. |
| `operetta/BWHazel.TopsyTurvy.Cli/OptionsResolvers/` | One `IEmitterOptionsResolver<TOptions>` implementation per `--emit`/`--target` `<str>` value, each owning: `GetAllowedExtensions()`/the default `CanEmit(filename)` (extension validation), `Apply(baseOptions, config, tiptoe) : TOptions` (merges `--config` on top of CLI-derived base options — an unrecognized key is reported via `PanelHelper.ReportUserWarning` and ignored, not an error), and `Emit(filename, options, tiptoe) : int` (reads the file itself and performs the actual work). `NoOptions` — empty record satisfying `TOptions` for a resolver with no settings at all (`PreprocessResolver`). `JsonEmitOptions(bool Abridged, bool Chromatic, VariableNameFormat Format)` — shared `TOptions` for the two JSON-emitting resolvers (`AstResolver`, `UtopIrAstResolver`); `Abridged`/`Chromatic` come from dedicated `--abridged`/`--chromatic` CLI options, not `--config`; `AstResolver` (never transforms to UtopIR) ignores `Format` and still rejects/warns on any `--config` key at all. `DotNetCilOptionsResolver` — `TOptions` = new `CilTargetOptions(CilEmitOptions EmitOptions, VariableNameFormat Format)`, wrapping rather than modifying `CilEmitOptions` (which belongs to the separate `UtopIR.Emitters.Cil` project and shouldn't carry a transform-stage concern); backs both `--emit dotnet-cil` and `--target dotnet` via its own static `BuildEmitOptions(filename)`/`BuildTargetOptions(filename, output)` base-options factories (called by `SorcererCommandBuilder` before `Apply`/`Emit`, so all `CilEmitOptions` construction stays with the resolver responsible for it); its `Emit` owns the IL-text-to-stdout (`IlSourceOnly`) and success-panel (`Executable`) output that previously lived duplicated inline in the command. **`--config varFormat`** (`VariableNameFormat.cs`, `VariableFormatConfig.cs`) — the first genuinely recognized `--config` key, resolved by `UtopIrResolver` (`TOptions` = `VariableNameFormat` directly, no wrapper needed for a single value), `UtopIrAstResolver`, and `DotNetCilOptionsResolver`; `VariableFormatConfig.Resolve` parses `numeric`/`verbose` (case-insensitive), warns on any other key, and throws `ToolchainConfigException` for an invalid `varFormat` value itself — the first real use of that exception, since every other key is currently unconditionally unrecognized. `VariableFormatConfig.CreateFormatter` maps the resolved enum to an `ITemporaryVariableNameFormatter` (`UtopIR.Transformer.VariableNameFormatters`); `VariableFormatConfig.WarnIfIgnoredForUtopIrInput` reports (for `Verbose` only — `Numeric` is indistinguishable from the unset default) that `varFormat` had no effect for `.utopir` input, which has no transform step. `ToolchainConfigException` — thrown by `Apply` for an invalid *value* on a recognized key, caught in `SorcererCommandBuilder.RunResolver` and reported via `PanelHelper.ReportUserError`. Shared parsing/JSON-writing logic lives in `ToolchainOperations` (see its own file inventory entry below), not in this directory, since it's genuinely common infrastructure rather than per-resolver behaviour; `ToolchainOperations.GetUtopIrProgram` takes an `ITemporaryVariableNameFormatter` parameter, threaded through by `UtopIrAstResolver`/`DotNetCilOptionsResolver`. |
| `operetta/BWHazel.TopsyTurvy.Cli/ToolchainOperations.cs` | Shared toolchain logic used by more than one `IEmitterOptionsResolver<TOptions>`: `ParseAndCheck` (parses/type-checks a `.topsy` file), `GetUtopIrProgram` (dispatches on extension — `.topsy` via `ParseAndCheck` + `TopsyTurvyToUtopIRTransformer`, `.utopir` via `UtopIRParser.TryParse`), `WriteJson` (STDOUT, optionally syntax-highlighted). Public, at the `BWHazel.TopsyTurvy.Cli` project root (not nested under `OptionsResolvers/`), since it isn't resolver-specific. |
| `operetta/BWHazel.TopsyTurvy.UtopIR.Parser/` | Superpower-based parser for UtopIR *source text* (`.utopir` files), mirroring `BWHazel.TopsyTurvy.Parser`'s house style, scaled down to UtopIR's line-oriented grammar. References `UtopIR.Ast` only (kept dependency-free of `BWHazel.TopsyTurvy.Ast`, matching every other UtopIR project except `UtopIR.Transformer`, which legitimately converts *from* Topsy Turvy AST). `Lexer.cs`: atomic terminals — `Whitespace`/`WhitespaceRequired` (`Character.In(' ', '\t')`, deliberately excluding `\r`/`\n` since UtopIR is line-oriented and newlines are structurally significant), `Variable` (strips the `£` sigil), `StringLiteral`/`CharacterLiteral` (same `~`-escape rules as Topsy Turvy), `FloatLiteral`/`IntegerLiteral`/`BoxIntegerLiteralValue` (int-if-fits-else-long boxing, same ternary-cast fix as the Topsy Turvy lexer), `BooleanLiteral` (`verity`/`nay`), `Comment` (`@` to end of line), `Identifier` (a leading letter *or* underscore, then letters/digits/hyphens/underscores — matches `specifications/UtopIR.ebnf`'s `Identifier` production exactly), `Keyword(string)` (case-insensitive, canonical lowercase). `OperandParser.cs`: `Type`, `ArithmeticOp`, `Literal`, `Operand`. `InstructionParser.cs`: one parser per instruction form (`Welcome`/`Appoint`/`Were`/`Leave`/`Arithmetic` via `AssignmentInstruction`, `Prentice`/`Find` via `StandaloneInstruction`), plus `InstructionSequence(TextParser<Unit>? terminator = null)` — a single recursive combinator (not a flat C# line-loop), reserved-but-unused `terminator` parameter so a future nested block instruction's body can be parsed by recursively invoking the same combinator with a different terminator, additively. `UtopIRDiagnostic(string Message, int Line, int Column)`/`UtopIRParseResult(UtopIRProgram? Program, IReadOnlyList<UtopIRDiagnostic> Diagnostics)`/`UtopIRSyntaxException`/`UtopIRParser` (`TryParse`/`Parse`) mirror the equivalent Topsy Turvy parser types, independently (not shared), preserving the dependency separation. **No `Span` on UtopIR AST nodes** — deliberate, confirmed with the user, deferred to whenever LSP/syntax-highlighting support for UtopIR is built; diagnostics get line/column from Superpower's own `Result<T>.ErrorPosition`. |
| `operetta/BWHazel.TopsyTurvy.Cli/ProgramExecutionResult.cs` | Discriminated result: `Success()`, `Failure(msg)`, `SyntaxError(errors)`, `RuntimeError(diagnostics)`. |
| `operetta/BWHazel.TopsyTurvy.Cli/ProgramRunner.cs` | `Run`, `Check`, `ParseFile` — all accept optional `IFileSystem? fileSystem = null`. `ParseFile` returns `(ProgramExecutionResult, ParseResult?)`. |
| `operetta/BWHazel.TopsyTurvy.Cli/PanelHelper.cs` | All Spectre.Console panel construction. `WriteDefault`, `WriteSuccess`, `WriteWarning` (yellow border via `Color.Yellow`; message text also wrapped in `[yellow]...[/]` markup, not just the border), `WriteUserError`, `WriteSyntaxErrors`, `WriteRuntimeErrors`, `WriteVersionInfo`, `ReportErrors`, `ReportUserError`/`ReportUserWarning` (panel or `--tiptoe` plain STDERR — used by `SorcererCommandBuilder` and every `OptionsResolvers/` resolver). Every panel uses `Border = BoxBorder.Rounded`, matching the REPL's `TableBorder.Rounded` tables. |
| `operetta/BWHazel.TopsyTurvy.Cli/NodeJsonConverter.cs` | `JsonConverter<Node>` for polymorphic AST serialisation with `$type` discriminators. |
| `operetta/BWHazel.TopsyTurvy.Cli/FileManager.cs` | Shared file-creation and IO utilities. `BuildFileContent(title, subtitle?)` builds scaffold source. Three IO methods accepting `IFileSystem? fileSystem = null`: `TryReadSource`, `TryCreateProgrammeFile`, `TryMountProject`. |
| `operetta/BWHazel.TopsyTurvy.Cli/CommandBuilders/` | One builder per command — see §3. All scaffold content goes through `FileManager.BuildFileContent`. Includes `IncantationCommandBuilder` — starts the OmniSharp LSP server over stdio; `--stdio` accepted silently for vscode-languageclient compatibility (see §2). `CadenzaCommandBuilder` starts the interactive REPL. |
| `operetta/BWHazel.TopsyTurvy.Cli/Repl/` | Six types supporting the `cadenza` REPL. `ReplSession` — orchestrates the REPL loop; owns one `TopsyTurvyEnvironment` and one `Interpreter` instance shared across all turns; wraps each input as `HARK! "Cadenza"\n{input}\nFINALE.`; detects `Console.IsInputRedirected` for non-interactive mode; implements `WriteArmoury` for the `:armoury`/`:env` command (prints variables and functions tables; excludes `THE PROPS`; supports rich and tiptoe modes). `ReplIO` — `ITopsyTurvyIO` that buffers `BEHOLD` output lines for panel display; pass-through writes (`suppressNewline: true`) go directly to `Console.Write`. `ReplInputReader` — raw key-reading loop (`Console.ReadKey(intercept: true)`) with styled prompts, backspace, cursor navigation, ↑/↓ history, Ctrl+C/D/L control key commands (`GetControlKeyCommand`), block-continuation accumulation, and live syntax highlighting via `ReplHighlighter`; the "keeping the cursor in sync" invariant ensures `cursorPosition` always equals the terminal column at loop entry. `ReplHighlighter` — static class; `Highlight(string) → string` tokenises a single line with a 7-step greedy left-to-right scanner (comment → string → **character literal** → boolean → number → keyword longest-first → plain) and wraps tokens in Spectre.Console markup; character literals use `sandybrown`, matching string literals. `ReplPromptKind` — enum (`SingleLine`, `MultiLine`, `Continuation`) used by `ReplInputReader.ShowPrompt`. `ReplConstants` — `internal static` class defining all REPL command strings and aliases (includes `:armoury` and `:env`). |
| `operetta/BWHazel.TopsyTurvy.Cli.E2ETests/` | Black-box CLI E2E tests — 100/100 passing. `CliFixture` locates the binary from the Debug build output and creates a temp directory prefixed `topsyturvy-cli-e2e-` per run. `CliTestBase` creates a per-test subdirectory and exposes `RunAsync(args)` (10-second timeout, redirected stdout + stderr) and `RunWithStdinAsync(args, stdin)` (same but sets `RedirectStandardInput = true` for interactive command testing). Eight test classes: `CommissionCommandTests` (11), `MountCommandTests` (11), `RehearseCommandTests` (6), `PerformCommandTests` (11), `PanelOutputTests` (13), `SorcererCommandTests` (27 — replaced `SorcererCueCommandTests`; covers every `--emit` format, `--target dotnet` incl. `--output`, the `--emit`-wins-when-both-given rule, extension validation, genuine `.utopir` parsing/compilation success for `utopir-ast`/`dotnet-cil`/`target dotnet` plus a malformed-`.utopir`-file diagnostic test, and `--config varFormat` — numeric default, verbose, an invalid value's error, and the `.utopir`-input warning), `PlaybillCommandTests` (6), `CadenzaCommandTests` (15). |
| `operetta/BWHazel.TopsyTurvy.LanguageServer/` | OmniSharp LSP **class library** — see §3. Started by the CLI via `operetta incantation`. `LspUtilities` — shared `GetSymbolDefinitionLocation` (builds an LSP `Location` for a symbol, converting 1-based to 0-based coordinates) and `MapSymbolKind` (maps `Analysis.SymbolKind` to LSP `SymbolKind`). `DocumentStateManager` — singleton; exposes `FindSymbolInOtherDocuments`, `FindSymbolWithUriInOtherDocuments`, and `GetImportedFunctionSymbols` for cross-document lookups. |
| `extensions/vscode/topsy-turvy/` | VS Code extension: LSP client, TextMate grammar, run/stop commands, panel and file icons. LSP server launched as `operetta incantation`. CLI binary bundled under `bin/` via `scripts/publish-cli.mjs`. `src/paths.ts` exports `resolveCliPath` — defaults to bundled binary when present, falls back to dev Debug layout. Tests: grammar (4), unit (4), integration (9). **UtopIR preview commands** (`.topsy`-only for now; `.utopir` has no registered language ID yet, deferred to a future syntax-highlighting task): `topsy-turvy.pourUtopIr` ("Pour UtopIR", `$(sparkle)`, `sorcerer <file> --emit utopir --tiptoe`, opens result as a new `plaintext` tab beside the source), `topsy-turvy.viewUtopIrAstPromptbook` ("View the UtopIR AST Promptbook", `$(list-tree)`, `--emit utopir-ast`, `json` tab), `topsy-turvy.pourDotNetCil` ("Pour .NET CIL", `$(circuit-board)`, `--emit dotnet-cil`, `plaintext` tab) — all three mirror the existing `cuePreProcessor`/`viewAstPromptbook` `execFileAsync` + `openTextDocument`/`showTextDocument(..., { preview: false, viewColumn: Beside })` pattern exactly, always passing `--tiptoe` unconditionally (not a user toggle, same as the two existing preview commands). `topsy-turvy.prepareDotNetAssembly` ("Prepare .NET Assembly Philtre", `$(package)`) differs — prompts via `showSaveDialog` (default filename derived from the source file, `.dll` extension) for the output path, then runs `sorcerer <file> --target dotnet --output <path>` (deliberately no `--tiptoe`, unlike the other three — this one runs visibly in a shared terminal panel via `vscode.Task`/`ProcessExecution`, not `execFileAsync` with captured output, mirroring `runFile`'s task pattern, since a compile can take a moment and benefits from visible progress and full panel/colour output); a one-off `vscode.tasks.onDidEndTaskProcess` listener (scoped to that specific `TaskExecution`, not the shared `runTaskExecution` used for the run/stop toggle) resolves the exit code, then shows a success notification with an "Open File Location" button (`revealFileInOS`) or an error notification pointing at the terminal output. All four are gated by a single new master toggle, `topsy-turvy.enableUtopirPreviewCommands` (boolean, default `false` — opt-in while UtopIR support is in preview), referenced directly in each menu contribution's `when` clause via `config.topsy-turvy.enableUtopirPreviewCommands` — the first use of a settings-gated (not just behavior-gated) `when` clause in this extension; no `setContext` plumbing needed since VS Code resolves `config.*` in `when` clauses natively. Added to `editor/title` (toolbar, each entry — old and new — now carrying an explicit `navigation@N` priority `@1`–`@10` rather than a bare `"navigation"` group, so default ordering is deterministic if the toolbar overflows; VS Code has no per-extension way to force a button to stay off the overflow menu, only user-controlled hide/show/reorder), `editor/context`, and `explorer/context` (continuing the existing `2_topsy-turvy@N` group ordering from `@4`–`@7`). |
| `operetta/BWHazel.TopsyTurvy.WebEditor/` | Blazor WASM standalone web editor. Toolbar: New Project, New File, Open, Save (left); Run (centre); Clear Output, Download Output, Light/Dark toggle, G&S Labels switch (right). BlazorMonaco editor with Monarch tokenizer, inline diagnostics, hover, completion, and go-to-definition. XtermBlazor output terminal. Three `[JSInvokable]` bridge methods: `GetHoverMarkdown`, `GetSymbolCompletions`, `GetDefinitionLocation` / `SwitchToFileForDefinitionAsync`. Per-file symbol state keyed by filename in `Dictionary<string, SymbolTable>`. See §2 for constraints. |
| `operetta/BWHazel.TopsyTurvy.WebEditor/Components/TopsyTurvyCodeEditor/` | `TopsyTurvyCodeEditor.razor` — wrapper component around `StandaloneCodeEditor`; exposes `Id`, `CssClass`, `ConstructionOptions`, `OnDidChangeModelContent`, and `SetValueAsync`. Namespace: `BWHazel.TopsyTurvy.WebEditor.Components.TopsyTurvyCodeEditor`. |
| `operetta/BWHazel.TopsyTurvy.WebEditor/Components/VisualEditor/` | `VisualEditor.razor` — hosts `DiagramCanvas` with `BlazorDiagram`; registers `TopsyTurvyVisualNodeModel → VisualNodeWidget`; handles right-click context menu state; subscribes to `diagram.Links.Added` and then `link.TargetAttached` for port-compatibility enforcement; Delete/Backspace removes selected nodes and links; `OnToggleReadOnly` and `OnTogglePanelOpen` `EventCallback` parameters; floating canvas controls. Phase D: `CreateSymbolNode(symbol, position)` helper — for `Function` kind creates a `FunctionReferenceNode` with named DataIn ports per `symbol.ParameterNames`; for `Parameter` kind creates an IdentifierNode with `Kind = VisualNodeKind.Parameter`; called from both `OnSymbolDoubleClickedAsync` and the drag-drop handler. `ExpressionNodeTypes` includes `"FunctionReferenceNode"`. `VisualNodeWidget.razor` — renders a single node as a vertical stack; `HasEditingControls` covers Program (excluding FINALE.), Declaration, Print, Literal, and Phase D: FunctionBodyOpener, ParameterNode, TryCatchOpener; FunctionBodyOpener section: name text field, void checkbox, return type dropdown, "+ Add Parameter" button (creates TERM node of `VisualNodeKind.Parameter`, links DataOut→"Param N" DataIn, updates subtitle via `UpdateFunctionSubtitle`); ParameterNode section: name text field + type dropdown; TryCatchOpener section: "Caught Variable" text field → SymbolIdentifierNodeName + subtitle `catch: {v}`; `UpdateLoopCondPort` adds "Cond" DataIn if non-Infinite and "Variable" DataIn if Ascending/Descending; `OnPropertyChanged` calls `this.Node.Refresh()`; CSS classes use the `visual-node-` prefix. `VisualContextMenu.razor` — absolutely-positioned context menu; CSS prefix `visual-context-menu-`. `VisualSymbolPanel.razor` — togglable left panel with `MudExpansionPanels`; Phase D: "Parameters" expansion panel added between Variables and Functions, each entry shows `(ownerName)` suffix; panel is resizable via a `visual-symbol-panel-resize-handle` div + `[JSInvokable] SetPanelWidth(int width)` (clamped 160–480 px) + `window.visualSymbolPanel.startResize(startX, initialWidth, dotNetRef)` JS; `panelWidth` state field controls the `width` style. Namespace: `BWHazel.TopsyTurvy.WebEditor.Components.VisualEditor`. |
| `operetta/BWHazel.TopsyTurvy.WebEditor/Visual/` | Model and builder types for the visual editor (non-Razor). `EditorMode.cs` (`Code`/`Visual` enum). `VisualNodeKind.cs` (semantic colour category enum; Phase D: `Parameter` value added, dark blue `#1565c0`). `VisualPortRole.cs` (`FlowIn`/`FlowOut`/`BranchOut`/`DataIn`/`DataOut` enum). `TopsyTurvyVisualNodeModel.cs` (extends `NodeModel`; adds `Title`, `Subtitle`, `Kind`; editable properties `AstNode`, `NodeName`, `NodeType`, `IsConstant`, `LiteralValue`, `SuppressNewline`; Phase B additions: `StatementType`, `PairedCloserId`, `PairedOpenerId`, `SymbolIdentifierNodeName`, `NodeLiteralType`, `IsIdentifierConstant`, `PrintSuppressNewline`; Phase C addition: `ArrayElementLiteralType: LiteralType?` — element type for array declaration nodes, separate from `NodeLiteralType` which is set to `LiteralType.Array` as a type marker). `TopsyTurvyVisualPortModel.cs` (extends `PortModel`; adds `Label`, `Role`). `NodeLayoutContext.cs` (mutable cursor; `NextPrimaryPosition()`, `AdvancePrimaryYTo(double)`). `VisualGraphBuilder.cs` (entry point `Build(ProgramNode, bool isReadOnly = true) : BlazorDiagram`; sets `StatementType`, `PairedCloserId`/`PairedOpenerId` on every node; Phase C: `CreateTargetIdentifierNode` helper; Phase D: `CreateFunctionBodySubgraph` creates TERM parameter nodes of `VisualNodeKind.Parameter` connected via "Param N" DataIn ports instead of DataOut ports on the opener; `ContinueNode` has `flowOut: false`; see §2 for port naming conventions and layout rules). `VisualGraphToAstConverter.cs` (entry point `Convert(BlazorDiagram) : ProgramNode`; fully flow-graph-based; `WalkFlowStatements(startNode, stopAtId, diagram, skipFirst = true)` — `skipFirst` param added; `IsBranchHeaderSentinel` returns true for all 8 header types; all builder-backed `Reconstruct*` methods use `WalkBranchBody`/`WalkFlowStatements` instead of the retired `ReconstructBodyStatements`; `ReconstructFunctionDefinition` reads "Param N" DataIn→TERM nodes for parameters; `ReconstructSummonFromNode` follows "Function" DataIn → FunctionReferenceNode, then reads FunctionReferenceNode's DataIn ports for arguments). `VisualNodeFactory.cs` (creates node clusters for all statement/expression types without AST inputs; Phase D: `CreateTryCatchBlock` pre-creates WITH GRATITUDE and MODIFIED RAPTURE, header nodes; `CreateSummonExpressionNode` has FlowIn + DataIn "Function" + DataOut "Out" + FlowOut; `FunctionReferenceNode` factory helper; `ContinueNode` has `shouldFlowOut: false`). `VisualTypeMaps.cs` (maps `LiteralType` → Topsy Turvy keyword string using `Keywords.TypeNames.*` constants — never raw string literals; includes `[LiteralType.Array] = "LITTLE LIST"`). `VisualSymbolEntry.cs` (Phase C record; Phase D: `string? OwnerName = null` optional property added for parameter owner function name). `VisualSymbolKind.cs` (Phase C: `Variable`/`Function`; Phase D: `Parameter` added). `VisualSymbolScanner.cs` (Phase C: scans declaration and function-body-opener nodes; Phase D: pre-builds `termOwner` dict mapping TERM node IDs → function name; emits `ParameterNode` as `VisualSymbolKind.Parameter` with `ownerName`; emits TryCatchOpener caught variable as Variable; pre-seeds `THE PROPS` built-in as Variable). |
| `operetta/BWHazel.TopsyTurvy.WebEditor/IO/BufferedWebIO.cs` | `ITopsyTurvyIO` for Blazor WASM — stdin from `Queue<string>`, output lines to `List<string>`. |
| `operetta/BWHazel.TopsyTurvy.WebEditor/IO/VirtualFile.cs` | Tracks a filename and content in the in-memory virtual file system used for tab management and ZIP export. Carries `[JsonConstructor]` on its constructor and `[JsonPropertyName("name"/"content")]` on its properties so it can be serialised/deserialised directly via Blazor JS interop without a separate DTO. `Name` has no setter — `[JsonConstructor]` is required for `System.Text.Json` to deserialise into it. |
| `operetta/BWHazel.TopsyTurvy.WebEditor/IO/EditorPreferences.cs` | Internal DTO used to read/write the `topsy-turvy-editor` Local Storage key via Blazor JS interop. All properties are nullable to distinguish "not yet saved" from "saved as false/empty". Properties: `List<VirtualFile>? Files` (`"files"`), `string? ActiveFile` (`"activeFile"`), `string? CommandLineArguments` (`"commandLineArguments"`), `string? StandardInput` (`"standardInput"`), `bool? AreGsLabelsEnabled` (`"areGsLabelsEnabled"`), `bool? IsDarkMode` (`"isDarkMode"`), `string? TerminalOutput` (`"terminalOutput"`). |
| `operetta/BWHazel.TopsyTurvy.WebEditor/Dialogs/NewFileDialog.razor` | `MudDialog` for entering a new file name; appends `.topsy` automatically. |
| `operetta/BWHazel.TopsyTurvy.WebEditor/Dialogs/OpenFilesDialog.razor` | `MudDialog` for opening multiple `.topsy` files via drag-and-drop or browse. |
| `operetta/BWHazel.TopsyTurvy.WebEditor/wwwroot/js/topsy-turvy-language.js` | Monaco Monarch tokenizer, keyword completion provider, and language configuration. Defines `window.topsyTurvy`. Extended by `web-editor.js`. |
| `operetta/BWHazel.TopsyTurvy.WebEditor/wwwroot/js/web-editor.js` | Monaco/xterm JS interop — hover, completion, definition providers, terminal fit, download utilities, semantic token provider (`registerSemanticTokensProvider`, `setSemanticTokens`, `enableSemanticHighlighting`), pane drag (`initPaneDrag`), localStorage persistence (`loadPreferences`, `savePreferences`), terminal theme updates (`setTerminalTheme`). Extends `window.topsyTurvy` via `Object.assign`. All nested functions in `initPaneDrag` use `const name = () =>` arrow syntax; drag state is consolidated into a single `dragConfig` object. |
| `operetta/BWHazel.TopsyTurvy.WebEditor.E2ETests/` | Playwright xUnit E2E tests — 6/6 passing. Three test classes: `EditorExecutionTests`, `EditorDiagnosticsTests`, `EditorToolbarTests`. `WebEditorAppFixture` starts the Blazor app and launches headless Chromium. |
| `extensions/vscode/topsy-turvy/scripts/publish-cli.mjs` | Node.js ESM script that cross-compiles the CLI for the current (or `DOTNET_RID`-specified) platform into `bin/`. |
| `docs/topsy-turvy/` | Docusaurus v3 documentation site. `npm run start` serves locally; `npm run build` produces `build/`. `npm run api:generate` (`api:clean` + `dotnetmd --config dotnet-md.json`) generates Markdown API reference from XML comments into `docs/api/dotnet/`. Requires `DOTNET_ROLL_FORWARD=LatestMajor` (set in the npm script) because `dotnetmd` targets .NET 8. `docs/guide/`, `docs/concepts/`, and `docs/tutorials/` each contain only a placeholder `index.md` ("Coming Soon") — no content pages yet. `docs/api/` holds the top-level API index. Generated output lands in `docs/api/dotnet/`; `docs/api/dotnet/index.md` is a placeholder with `unlisted: true` to prevent a build error when generated content is absent. Do not delete this placeholder. Sidebar convention: each section uses `link: {type: 'doc', id: 'section/index'}` to make the category label clickable; `items: []` while the section has no content pages, switching to `{type: 'autogenerated', dirName: 'section'}` once pages are added (Docusaurus excludes `index.md` from autogenerated child items automatically). |
| `.claude/commands/implement-language-feature.md` | Project-level Claude Code skill — invoked as `/implement-language-feature`. Guides full-stack implementation of a new language feature across all layers in sequential step order 3a–3l (grammar → KeywordData → parser → runtime → analysis → LSP → Monaco → TextMate → visual graph builder → visual supporting files → code generator → REPL). |
| `specifications/UtopIR.ebnf` | Formal EBNF grammar for UtopIR v0.0.1-preview1. Key productions: `Programme`, `Line`, `Instruction`, `AssignmentInstruction`, `WelcomeRhs`, `AppointRhs`, `ArithmeticRhs`, `LeaveRhs`, `StandaloneInstruction`, `PrenticeInstruction`, `FindInstruction`, `ArithmeticOp`, `Operand`, `Type`, `Variable` (prefixed with `£`). Boolean literals in UtopIR are `verity` (true) and `nay` (false) — not Topsy Turvy's `VERITY`/`NAY`. |
| `operetta/BWHazel.TopsyTurvy.UtopIR.Ast/` | UtopIR instruction node types. Abstract base: `UtopIRInstruction`. Concrete positional records: `WelcomeInstruction(UtopIRVariable Target, UtopIRType Type)`, `AppointInstruction(UtopIRVariable Target, UtopIROperand Value)`, `ArithmeticInstruction(UtopIRArithmeticOperation Operation, UtopIRVariable Target, UtopIROperand Operand1, UtopIROperand Operand2)`, `PrenticeInstruction(UtopIROperand Value)`, `LeaveInstruction(UtopIRVariable Target)`, `FindInstruction(UtopIROperand? Value)`, `WereInstruction(UtopIRVariable Target, UtopIROperand Value, UtopIRType Type)` (a type cast, e.g. `£Lords = were £LovesickMaidens, chancellor`). Supporting types: `UtopIRVariable(string Name)` — the `£` sigil is a display concern and is NOT stored in `Name`; `UtopIROperand` (abstract base), `VariableOperand(UtopIRVariable Variable)`, `LiteralOperand(object Value)`; `UtopIRType` enum (Peer, Chancellor, Pirate, SausageRoll, StandingPeer, StandingChancellor, StandingPirate, StandingSausageRoll, Fathom, Foot, Decree, Stitch, Yarn); `UtopIRArithmeticOperation` enum (Sum, Diff, Prod, Quot, Rem, Max, Min); `UtopIRProgram(IReadOnlyList<UtopIRInstruction> Instructions)`. `UtopIRKeywords` — static class with nested `Literals` (`Verity`, `Nay`), `TypeNames` (all 13 type keywords as constants), and `Instructions` (all 13 instruction/mnemonic keywords as constants, including `Were = "were"`). |
| `operetta/BWHazel.TopsyTurvy.UtopIR.Transformer/` | Walks a Topsy Turvy `ProgramNode` and produces a flat `UtopIRProgram`. Entry point: `TopsyTurvyToUtopIRTransformer(ITemporaryVariableNameFormatter? formatter = null).Transform(ProgramNode)`. Uses type-switching (consistent with `TypeCheckVisitor` and `Interpreter`). Nested arithmetic expressions are flattened into sequential `ArithmeticInstruction`s using auto-generated temporary registers whose names are delegated to the constructor-supplied `ITemporaryVariableNameFormatter` (`VariableNameFormatters/` subfolder — see the Known Gaps entry below for the full design): default `IncrementingIntVariableFormatter` (`_0`, `_1`, ...), or `InstructionDetailVariableFormatter` (`_prod_a_b`, `_sum__prod_a_b_c`) for the original descriptive scheme. Statement dispatch covers: `PrincipalBlockNode` (recurse into declarations), `DeclarationNode` (→ `WelcomeInstruction` + optional `AppointInstruction`), `AssignmentNode` (→ `AppointInstruction`), `ProgrammeReturnNode` (→ `FindInstruction`). `TransformExpression` also handles `ExpressionCastNode` (Topsy Turvy's `AS IT WERE ... AS A <type>`) via `TransformCast`, which flattens the source expression, maps the cast's `LiteralType` through `MapType`, and emits a `WereInstruction` into a temporary register named by the same formatter (e.g. `_were_Lords_chancellor` under `InstructionDetailVariableFormatter`) — the same auto-generated-temp pattern arithmetic uses. **Automatic widening**: a `Dictionary<string, UtopIRType> declaredTypes` is threaded through every transform method (populated by `DeclarationNode`, `TransformCast`, and `TransformArithmetic`'s own result register), so `TransformArithmetic` can call `InferOperandType` on both operands, compute the wider type via `Widen`/`IndexOfNumericWidening` (a third independent copy of the widening-order table, after `TypeCheckVisitor` and `Interpreter` — consistent with this project's existing pattern of per-layer copies rather than a shared utility), and insert a `WereInstruction` on the narrower operand via `CastOperandIfNeeded` whenever the two operand types differ, before the `ArithmeticInstruction` is emitted — so every `ArithmeticInstruction` this Transformer produces is guaranteed to have matching operand types, regardless of operand order (`PEER + CHANCELLOR` and `CHANCELLOR + PEER` both widen the `PEER` side). Same-type operands emit no extra `WereInstruction`. **Declaration/assignment type-matching**: `TransformDeclaration`/`TransformAssignment` reuse the same `CastOperandIfNeeded` helper — when the declared/target type is an integer type (`IsIntegerType`) and the value operand's inferred type differs (most commonly because integer literals default to `Peer`/`Chancellor` regardless of the variable they're being appointed to), a widening `WereInstruction` is inserted before the `AppointInstruction`, so `AppointInstruction` never stores a mismatched-width value directly. Since both of these now require knowing every referenced variable's declared type, hand-built `ProgramNode`s that reference an undeclared identifier in arithmetic or assignment (previously tolerated by the transformer) now throw `KeyNotFoundException` — real Topsy Turvy source can never trigger this, since `TypeCheckVisitor` already enforces declare-before-use ahead of the Transformer running. `CONSERVATIVE` declarations are currently lowered identically to mutable declarations — constant inlining is deferred (see §4). |
| `operetta/BWHazel.TopsyTurvy.UtopIR.Analysis/` | Emits human-readable UtopIR source text from a `UtopIRProgram`. Entry point: `UtopIRCodeGenerator.Generate(UtopIRProgram) : string`. Uses `UtopIRKeywords` constants throughout. `FormatLiteral` handles: `bool` → `verity`/`nay`; `string` → `"..."` with `~` escaping (`~` → `~~`, `"` → `~"`, `\n` → `~n`, `\t` → `~t`); `float`/`double` → ensures at least one decimal point (appends `.0` when no `.` or `E` is present); `char` → `'...'` with the same `~` escaping. `WereInstruction` renders as `£<target> = were <value>, <type>`, reusing the same `FormatOperand`/`TypeKeyword` helpers as `AppointInstruction`/`WelcomeInstruction`. |
| `operetta/BWHazel.TopsyTurvy.UtopIR.Emitters.Cil/` | Emits a .NET assembly from a `UtopIRProgram` using `PersistedAssemblyBuilder` (`System.Reflection.Emit`, .NET 10 native). Entry point: `CilEmitter.Emit(UtopIRProgram, CilEmitOptions)`. Scaffolds `AssemblyBuilder` → `ModuleBuilder` → `TypeBuilder "Opera"` (`TypeAttributes.Public \| Abstract \| Sealed` — the exact metadata shape the C# compiler emits for a `static class`; correct because `Main` is `static` and no instance is ever needed) → `MethodBuilder "Main(string[]):int"` → `ILGenerator`. Virtual registers (`£name`) map to `LocalBuilder` slots keyed by name (without `£`) in a `Dictionary<string, LocalBuilder>`; every `DeclareLocal` call is paired with a `CilGenerator.RegisterLocalName` call so the name survives into the IL text (see the `CilGenerator.cs` entry below). `CilEmitOptions` carries `AssemblyName`, `OutputPath`, and `OutputKind` (`CilOutputKind.Library`, `Executable`, or `IlSourceOnly` — **required, no default**; every call site must specify one explicitly). **`Library`**: `PersistedAssemblyBuilder.Save(Stream)` to an in-memory buffer, then that buffer is written verbatim to `OutputPath` — no CLR entry point set; loadable via `Assembly.LoadFrom` + reflection-invoke `Opera.Main` (used by all round-trip tests). **`Executable`**: follows the MS-documented "Set entry point for an executable" pattern — `PersistedAssemblyBuilder.GenerateMetadata(out ilStream, out fieldData)` + `ManagedPEBuilder(header: PEHeaderBuilder.CreateExecutableHeader(), entryPoint: MetadataTokens.MethodDefinitionHandle(mainMethod.MetadataToken))`, serialized to an in-memory buffer and written to `OutputPath`; also writes a companion `<name>.runtimeconfig.json` (minimal, `net10.0` TFM, `Microsoft.NETCore.App` framework at `Environment.Version`) so the output can be launched via `dotnet <path>`. **`IlSourceOnly`**: `OutputPath` is ignored and nothing is written to disk — but the type is still finalized and the assembly still serialized to an in-memory buffer internally, since that buffer is what feeds the IL-text disassembly (see below); "no output" means no disk/caller-visible side effect, not that no assembly is ever built. **Critical gotcha**: `PersistedAssemblyBuilder` throws `"Cannot populate assembly metadata multiple times"` if `Save`/`GenerateMetadata` is called more than once on the same builder — the in-memory buffer for each `OutputKind` must be produced exactly once and reused for both the on-disk write and the IL-text disassembly (`BuildLibraryPeBytes`/`BuildExecutablePeBytes` each call their respective builder method exactly once). **Verified cross-platform**: the emitted PE is a managed assembly (IL, not native machine code) — the PE container format itself is not Windows-specific, only the filename convention (`.exe`) suggests it. Confirmed working end-to-end on macOS: emitted a `SUM OF 21 AND 21` programme as `CilOutputKind.Executable`, ran `dotnet Opera.dll` directly (no Windows/Wine/emulation involved), got exit code `42`. `file` reports the PE as `"for MS Windows"` — this is `file`'s generic PE-format heuristic; it does not inspect the CLR header and is not evidence the assembly is Windows-only. The PE cannot be double-clicked or executed directly (`./Opera.dll`) on any OS — it always requires `dotnet <path>` (see the AppHost known gap below for what a true native double-clickable binary would require). UtopIR → CIL mapping: `welcome <type>` → `DeclareLocal`; `appoint <lit\|var>` → `ldc.*`/`ldloc` + `stloc`; `sum/diff/prod/quot/rem` → `add/sub/mul/div/rem` (`div.un`/`rem.un` for unsigned types — CIL's evaluation stack has no distinct unsigned int32/int64 type, so signed/unsigned division needs the explicit opcode variant while add/sub/mul are bit-identical either way); `max/min` → narrow to the true CLR width (`conv.i2`/`i1`/`u4`/`u2`/`u1` as needed, since the stack always widens `short`/`sbyte`/`ushort`/`byte` to `int32` on load) then `call Math.Max`/`Math.Min`; `were <value>, <type>` → `EmitWere`: loads the source operand, emits a single `EmitConversion(targetType)` (always the target-type `conv.*` opcode — this works regardless of the source type, since `conv.*` converts whatever is currently on the stack), then auto-declares the target local with the destination CLR type if not already declared and stores; `prentice`/`leave` → push/`stloc`; `find <value>` → `ldloc`/`ldc.*` + `ret` (64-bit types narrowed via `conv.i4` since `Main` always returns `int32`); `find` (void) → `ldc.i4.0` + `ret`. `EmitConversion` (renamed and generalised from the old `EmitNarrowConversion`, which only handled the narrow cases `EmitMaxMin` needed) covers all eight integer `UtopIRType` variants and is shared by both `EmitMaxMin` and `EmitWere`. `EmitLoadLiteralValue` infers the `ldc` opcode from the literal's actual CLR runtime type (not from a separate "expected type" — there is no such parameter; it was removed as dead code, see git history). Unsigned literal casts (`(int)someUint`, `(long)someUlong`) are same-width bit-reinterprets, not value conversions — the bit pattern round-trips correctly once stored into a genuinely `uint`/`ulong`-typed local; `byte`/`ushort` casts are safe zero-extending widens. **Scope**: all eight integer types (`peer`, `chancellor`, `pirate`, `sausageroll` and unsigned variants). Floating-point, boolean, character, and string types throw `NotSupportedException`. |
| `operetta/BWHazel.TopsyTurvy.UtopIR.Emitters.Cil/CilGenerator.cs` | Produces the readable, **bare** IL instruction listing returned via `CilEmitResult.IlSource` — nothing but the disassembled instruction lines, no `.locals init (...)` block and no `.assembly`/`.class`/`.method` wrapper. First external NuGet dependency in the UtopIR project line: **`AsmResolver.DotNet`** (`6.0.0`) — used purely for *reading*; `CilEmitter` still builds the assembly with `System.Reflection.Emit`/`PersistedAssemblyBuilder` unchanged. Two-phase contract, always used in this order by `CilEmitter`: (1) during emission, `RegisterLocalName(LocalBuilder, string)` records each local's UtopIR name against its index at declaration time — no text exists yet, this only remembers names; (2) after emission, `ToIlText(byte[] assemblyBytes)` is called exactly once, reading the already-persisted bytes via `AssemblyDefinition.FromBytes`, locating `Opera.Main`, and disassembling `CilMethodBody.Instructions`. **Nothing hand-composed**: AsmResolver has no facility anywhere in the library to render a `.assembly`/`.class`/`.method` wrapper *or* a `.locals init (...)` text block — only per-instruction disassembly and metadata reading exist (confirmed directly against AsmResolver's source) — and no platform in scope for v0.0.1-preview1 builds executable IL from source text, so neither had a consumer; two earlier versions of this file hand-composed first the full wrapper, then just the locals block, from real metadata — both were abandoned once it became clear either was still exactly the boilerplate AsmResolver was meant to eliminate. Local names/types are still recoverable inline: each `ldloc`/`stloc` line shows `'£name'` via `UtopIrCilInstructionFormatter` — see its own entry below. Retired the `ilasm`-based self-verification test (`ILASM_PATH`-gated) that existed only to compensate for the old hand-authored-text approach's risk — no longer needed once disassembly comes from a real, independent 3rd-party library reading real bytes; the fully-derived instruction output and the macro-operand fix (see `UtopIrCilInstructionFormatter.cs` below) were directly verified by compiling and running actual, unmodified `CilEmitter` output (temporarily wrapped for compilation purposes only) with the real Microsoft `ilasm`. |
| `operetta/BWHazel.TopsyTurvy.UtopIR.Emitters.Cil/UtopIrCilInstructionFormatter.cs` | A `CilInstructionFormatter` subclass (AsmResolver's own supported extension point for per-instruction text — `AsmResolver.PE.DotNet.Cil.ICilInstructionFormatter`) that substitutes registered UtopIR names for local variable operands, replacing an earlier post-processing approach that string-split AsmResolver's default `ToString()` output. Overrides `FormatVariable` for the general case (any instruction whose operand is a real `CilLocalVariable`, e.g. `ldloc.s V_4` beyond the macro range). **Non-obvious**: AsmResolver reports a `null` `Operand` for macro-form `ldloc`/`stloc` opcodes (`ldloc.0`…`ldloc.3`, `stloc.0`…`stloc.3`) — the local index is only recoverable from `CilInstruction.OpCode.Code` (a `CilCode` enum comparison), not from `Operand`; the public `Format(CilInstruction)` method special-cases this. **Second non-obvious gotcha, previously a real bug**: macro-form opcodes take no operand at all in ILASM syntax — `stloc.0 '£a'` is invalid, ilasm rejects it with a syntax error (confirmed directly). Once a name is being substituted in, `Format` always emits the base `ldloc`/`stloc` mnemonic with the name as an explicit operand (e.g. `stloc '£a'`), never the macro form, regardless of which form the real instruction used. **Why `Format` and not an override of `FormatInstruction`**: `CilInstructionFormatter.FormatInstruction` is not `virtual` in C# source, despite reflection reporting `IsVirtual = true` — that flag is an IL-level artifact of implementing the `ICilInstructionFormatter` interface, not a genuine `virtual` keyword in the base class (confirmed by reading AsmResolver's actual source on GitHub); `Format` is therefore a new method, not an `override`, that delegates to the inherited `FormatInstruction` for the general case. Every instruction not needing name substitution defers entirely to AsmResolver's own resolution (`call` targets, string/type operands, real `IL_XXXX:` byte offsets, etc.), matching real `ildasm` output. |
| `operetta/BWHazel.TopsyTurvy.UtopIR.Tests/` | xUnit suite — 169/169 passing. Five subdirectories: `Parser/` (`LexerTests`, `InstructionParserTests`, `UtopIRParserTests` — 43 tests, incl. a `UtopIRCodeGenerator`/`UtopIRParser` round-trip test — see the `UtopIR.Parser` file inventory entry above), `Analysis/UtopIRCodeGeneratorTests.cs` (42 tests, incl. `were` with variable and literal operands), `Transformer/TopsyTurvyToUtopIRTransformerTests.cs` (39 tests, incl. `AS IT WERE` → `WereInstruction`, `PEER + CHANCELLOR`/`CHANCELLOR + PEER` order-independent widening, same-type arithmetic emitting no spurious `WereInstruction`, and mismatched-literal-type declaration/assignment inserting a widening `WereInstruction`), `Emitters/CilEmitterTests.cs` (39 tests — every arithmetic opcode, all integer types, `prentice`/`leave`, nested arithmetic, implicit zero return, unsigned round-trips at the `byte`/`uint` boundary, `Executable`/`IlSourceOnly` output kinds, undeclared-temp-target auto-declare, unsupported-type cases, and `were` widen/narrow/unsigned-conversion/undeclared-target cases). The `were` tests assert on `CilEmitResult.IlSource` directly for the exact `conv.*` opcode (e.g. `conv.i8`, `conv.i4`, `conv.u4`), not just the resulting exit code — a widen followed immediately by `FindInstruction`'s own return-narrowing `conv.i4` truncates back to the same low 32 bits regardless of whether the widen sign- or zero-extended, so exit-code-only assertions cannot distinguish `conv.i8` from a wrongly-emitted `conv.u8`; confirmed by deliberately swapping the opcode and observing the IL-text assertion (not the exit-code assertion) fail. The narrowing test uses a source value outside the `int32` range so the result is only correct if genuine truncation occurred, not a coincidental pass-through. Round-trip tests emit to a temp path, `Assembly.LoadFrom`, reflection-invoke `Opera.Main`, assert the exit code. Placeholder span helper: `private static readonly SourceSpan PlaceholderSpan = new(new SourceLocation(0, 0), new SourceLocation(0, 0))`. Does **not** reference `BWHazel.TopsyTurvy.Parser` — the real-source-text pipeline test moved to `BWHazel.TopsyTurvy.UtopIR.E2ETests` (see below). `TopsyTurvyToUtopIRTransformerTests` explicitly constructs `new TopsyTurvyToUtopIRTransformer(new InstructionDetailVariableFormatter())`, since its assertions target descriptive temp names, not the transformer's own default. `Transformer/InstructionDetailVariableFormatterTests.cs`/`IncrementingIntVariableFormatterTests.cs` (6 tests) test the `VariableNameFormatters/` classes directly — sanitisation, de-duplication, and the incrementing counter. |
| `operetta/BWHazel.TopsyTurvy.UtopIR.E2ETests/` | xUnit suite — 7/7 passing. `TopsyTurvyUtopirCilPipelineIntegrationTests.cs` — end-to-end tests driving real Topsy Turvy source text through the full pipeline: `TopsyTurvyParser` → `TopsyTurvyToUtopIRTransformer` → `UtopIRCodeGenerator` (inspected as text) → `CilEmitter` → a loaded, reflection-invoked .NET assembly whose exit code is asserted. Distinct from `UtopIR.Tests`, whose Transformer/Analysis/Emitters tests all construct their inputs by hand rather than parsing real source. Follows the same project shape as `BWHazel.TopsyTurvy.Cli.E2ETests`. Includes a mixed-type-arithmetic test (`PEER + CHANCELLOR`) and a dedicated literal-appointed-to-wider-declared-type test (`PRAY WELCOME big_number AS A CHANCELLOR` then bare `big_number IS APPOINTED 200`) — both were originally bug reports, now fixed by the Transformer's automatic widening/type-matching casts. `UtopIrSourcePipelineIntegrationTests.cs` — the equivalent pipeline starting from hand-written **UtopIR** source text instead of Topsy Turvy: `UtopIRParser.TryParse` → `CilEmitter` → loaded/invoked/exit-code-asserted assembly, including a `were`-cast widening case (asserts both the emitted `conv.i8` opcode and the exit code), plus a malformed-source test asserting `TryParse` returns a diagnostic rather than throwing. |

---

## 2. Non-Obvious Constraints

A new session must know these before touching the relevant code.

**`BWHazel.TopsyTurvy.WebEditor.E2ETests` can silently hang against a stale, orphaned dev server on port 5188**: `WebEditorAppFixture.InitializeAsync` starts `dotnet run --project BWHazel.TopsyTurvy.WebEditor --launch-profile http` and polls `http://localhost:5188` until it gets any successful HTTP response, then proceeds. If a *previous* test run was killed or crashed rather than completing normally, its `dotnet run` child process (and the `blazor-devserver.dll` process it spawns) is never torn down by `DisposeAsync` and is reparented to `init` (PPID 1) — it keeps running and keeps the port bound indefinitely (observed alive for ~4h45m in one session). The new test run's `dotnet run` then fails to bind port 5188, but the fixture's health check still gets a 200 from the *orphaned* process and proceeds — against a server that may be serving stale/rebuilt-out-from-under-it `wwwroot` content. Symptom: every test in the run fails identically at `WaitForAppReadyAsync` with `TimeoutException: Timeout 30000ms exceeded — waiting for Locator("button:has-text(\"Perform\")")`, because the stale server's Blazor bundle never becomes interactive (or serves content inconsistent with the current build). Diagnose with `lsof -i :5188 -sTCP:LISTEN` and `ps -p <pid> -o pid,ppid,etime,command` — a `PPID` of `1` and a large `ELAPSED` confirms an orphan; `kill <pid>` (and its `blazor-devserver.dll` child if a separate PID) to free the port. A `dotnet build --no-incremental` on the whole solution can independently leave the WebEditor's `wwwroot/_framework/` directory with duplicate content-hashed `.wasm`/`.pdb` files from multiple builds (`dotnet run` does not always clean `_framework/` between builds) — if the port is confirmed free but the timeout persists, `rm -rf BWHazel.TopsyTurvy.WebEditor/bin BWHazel.TopsyTurvy.WebEditor/obj` and rebuild before re-running the E2E suite.

**Visual editor — port alignment is derived from `VisualPortRole`, never set explicitly**: `VisualGraphBuilder.MakePort` converts `VisualPortRole` → `PortAlignment` internally (`FlowIn` → Top, `FlowOut`/`BranchOut` → Bottom, `DataIn` → Left, `DataOut` → Right). Do not pass an alignment argument; there is no overload that accepts one.

**Visual editor — block closer pattern**: every block-level `Create*` method (Conditional, Loop, TryCatch, Switch, Guard) must (1) build all branches first while collecting each branch's tail node, (2) find `maxTailY = branches.Max(tail => tail.Position.Y)`, (3) place the closer node at `new Point(openerX, maxTailY + NodeLayoutContext.RowSpacing)`, (4) call `layout.AdvancePrimaryYTo(closerY + NodeLayoutContext.RowSpacing)`. Violating this order puts the closer at the same depth as mid-branch nodes.

**Visual editor — branch centering formula**: for `N` branches, branch `i` (0-based) is placed at `openerX + (i - (N-1)/2.0) * spacing`. Spacing constants: 660 px for Conditional and TryCatch (accommodates expression trees on ElseIf branches); 440 px for Switch. This keeps the opener visually centred above its branches.

**Visual editor — expression anchoring**: `CreateExpressionNode` places expression nodes at `(anchor.X - ExprColumnWidth, anchor.Y + portIndex * ExprPortSpacingY)` where `ExprColumnWidth = 210.0` and `ExprPortSpacingY = 80.0`. `portIndex` is the 0-based port slot on the consumer node. Always pass a non-null `anchor` equal to the consuming statement node's position.

**Visual editor — CSS class prefix**: all CSS classes for visual node rendering use the `visual-node-` prefix (e.g. `visual-node`, `visual-node-header`, `visual-node-flow-top`). Do not introduce `vn-` prefixed classes — that prefix was retired in the 2026-06-27 session.

**Visual editor — `VisualGraphBuilder` is a single file**: it is large (~1 100 lines) but splitting it into partial classes is deferred. When splitting, use: `VisualGraphBuilder.Statements.cs` and `VisualGraphBuilder.Expressions.cs`.

**Visual editor — `VisualGraphToAstConverter` reads only from the live diagram**: `Convert()` makes no reference to `originalProgram`. Sidebar nodes (floating declarations/imports with no incoming FlowIn link) are detected by `HasIncomingFlowLink(node)` which checks whether the node's `FlowIn` port has any links. Do not reintroduce an AST-backbone approach — factory-created nodes have no `AstNode` and would be silently lost.

**Visual editor — branch header `StatementType` sentinels must be skipped in `WalkFlowStatements`**: all 8 branch-header types are in the skip list: `"ConditionalTrueBranch"`, `"ConditionalElseIfBranch"`, `"ConditionalElseBranch"`, `"TryCatchSuccessBranch"`, `"TryCatchErrorBranch"`, `"SwitchCaseBranch"`, `"SwitchDefaultBranch"`, `"GuardElseBranch"`. When adding a new block type, add its branch-header sentinel here. `WalkFlowStatements` starts from `NextFlowNode(startNode)`, so passing a branch-header node as `startNode` is also safe — the first skipped `continue` handles it, but the skip list is a belt-and-suspenders guard for cases where the header ends up mid-walk.

**Visual editor — `VisualNodeFactory` must use `Keywords.TypeNames.*` constants, not raw strings**: `VisualTypeMaps.TypeToKeyword` uses `Keywords.TypeNames.*` throughout. New entries in `VisualTypeMaps` must follow the same rule — never write a Topsy Turvy type keyword as a string literal in this file.

**Visual editor — context menu CSS prefix is `visual-context-menu-`**: classes follow BEM: `visual-context-menu` (container), `visual-context-menu-section-label`, `visual-context-menu-item`, `visual-context-menu-item--danger` (modifier). Do not use the old `vcm-` prefix.

**Visual editor — `Node.Refresh()` is required for ZBD to re-render after property mutations**: Z.Blazor.Diagrams does not watch individual model properties. After any mutation of `TopsyTurvyVisualNodeModel` properties (title, subtitle, port list, etc.) via an editing control, call `this.Node.Refresh()` before `await OnNodeChanged.InvokeAsync()`. Without it, the diagram canvas will not re-render the node widget — changes appear to have no effect until the user interacts with the canvas.

**Visual editor — subscribe to `link.TargetAttached`, not `diagram.Links.Added`, for bidirectional validation**: at the point `Links.Added` fires, the user-drawn link's target is still a floating `PointAnchor`, not a `SinglePortAnchor`. Checking `link.Target is not SinglePortAnchor` will always return true and cause the validation to immediately reject (or skip) every new link. Subscribe to `link.TargetAttached` inside the `Links.Added` handler to validate only after the user completes the connection: `link.TargetAttached += (_, _) => { /* validate port role pair */ }`.

**Visual editor — Blazor CSS isolation does not apply to child component elements**: `VisualEditor.razor.css` is scoped to `VisualEditor.razor`'s own rendered elements only — it cannot style elements rendered by child components such as `VisualSymbolPanel`. To override MudBlazor styles inside a child component (e.g. `.mud-expand-panel-gutters` padding), add the rule to global `app.css` and qualify it with a CSS class on the parent wrapper (e.g. `.visual-symbol-panel .mud-expand-panel-gutters { padding: ... !important; }`). The `!important` is necessary because MudBlazor's component styles load with higher specificity than global rules.

**Visual editor — port naming conventions (Phase C)**: the following labels are canonical and must be used consistently across `VisualGraphBuilder`, `VisualNodeFactory`, `VisualGraphToAstConverter`, and `VisualNodeWidget`: `"Variable"` for the target-identifier DataIn port on IS APPOINTED, VICTIM IS APPOINTED, and PRAY TELL; `"Victim"` for the array-index DataIn port on VICTIM IS APPOINTED; `"Victim X"` (1-based, e.g. `"Victim 1"`, `"Victim 2"`) for array initial-value DataIn ports on PRAY WELCOME array nodes; `"Value"` for expression-value DataIn ports; `"Out"` for Identifier node DataOut ports. `HasArrayElementPorts` in `VisualNodeWidget` and the scanner both check for the `"Victim "` prefix (with trailing space) — maintain this prefix exactly.

**Visual editor — `ArrayElementLiteralType` vs `NodeLiteralType` on array declaration nodes**: `NodeLiteralType` is set to `LiteralType.Array` on array declaration nodes as a type marker (indicating "this is an array node"). `ArrayElementLiteralType` holds the actual element type (e.g. `LiteralType.Integer`). Code that displays the element type (scanner, widget element-type dropdown, converter) must read `ArrayElementLiteralType`, not `NodeLiteralType`. Reading `NodeLiteralType` on an array node always returns `Array` and will produce "LITTLE LIST" instead of the element keyword.

**Visual editor — `HasEditingControls` excludes `FINALE.` explicitly**: the editing-controls section is shown when `HasEditingControls` is true. The `VisualNodeKind.Program` check is guarded by `this.Node.Title != "FINALE."` because both `HARK!` (which has editable title/subtitle fields) and `FINALE.` (which has nothing editable) share `VisualNodeKind.Program`. Never remove this guard — adding edit fields to FINALE. would produce spurious converter input.

**Visual editor — `lastBuiltVisualSource` prevents feedback loops**: `Editor.razor` stores the last source string produced by the visual-to-code pipeline in `lastBuiltVisualSource`. `OnContentChangedAsync` compares incoming Monaco content against this field and skips `RebuildVisualGraphAsync` when they match, preventing the visual editor from re-building from its own output. Always set `lastBuiltVisualSource` before updating Monaco when triggering from the visual path.

**Documentation comment association rule**: `SymbolTable.FindDocumentationComment` scans backwards from the symbol's definition line to find an `END OF ASIDE.)` marker on the first non-blank line above the declaration. Blank lines between the block and the declaration are allowed; any non-blank, non-comment line breaks the association. Concretely: a doc comment inside a `PRINCIPALS` block must appear immediately before the `PRAY WELCOME` line it documents, not before the `PRINCIPALS` keyword itself.

**Documentation comment keywords in `KeywordData` (90 total)**: the 8 documentation tag keywords (`LEGEND:`, `RECITATIVE:`, `ARTICLE`, `CONSEQUENCE`, `CURSES`, `CHORUS:`, `ENSEMBLE:`, `STATUTORY:`) appear in the completion list but have no parser or runtime representation — they exist solely within `(ASIDE, AT SOME LENGTH: ... END OF ASIDE.)` blocks and are stripped by the `CommentsPreProcessor`. Do not add them to `Keywords.cs` or any parser combinator.

**`STATUTORY:` emits the `deprecated` semantic token modifier**: `SemanticTokensHandler` checks `symbol.Documentation?.IsDeprecated` and adds `"deprecated"` to the modifier list. The `Legend` now declares both `"readonly"` and `"deprecated"` modifiers. In VS Code, the `deprecated` modifier maps to strikethrough rendering via the `semanticTokenModifiers` contribution in `package.json` — add a `"deprecated"` entry there if the VS Code extension is updated to advertise the modifier.

**Web editor — semantic token provider uses a push model**: After each successful analysis pass, `ComputeSemanticTokenData` in `Editor.razor` encodes all symbol occurrences as a flat `int[]` (5 ints per token in Monaco's delta encoding: `deltaLine`, `deltaStartChar`, `length`, `tokenType`, `tokenModifiersBitmask`) and pushes it to JS via `topsyTurvy.setSemanticTokens(data)`. JS stores the array in `_semanticTokensData` and fires `_semanticTokensListeners`, causing Monaco to immediately call `provideDocumentSemanticTokens`, which returns the cached data as a `Uint32Array`. Token type indices: `variable`=0, `parameter`=1, `function`=2. Modifier bitmask: `readonly`=1, `deprecated`=2. Symbols whose names contain a space (e.g. `THE PROPS`) are skipped by the same guard used in `SemanticTokensHandler`.

**Web editor — semantic token scope hierarchy must use `variable.*`**: Monaco's `getTokenStyleMetadata` builds the TextMate lookup scope as `[tokenTypeName].concat(modifierNames).join('.')`. The built-in `vs-dark` TextMate trie has entries for `variable` and `variable.parameter` but not for bare `function`. Using `['variable', 'parameter', 'function']` as legend type names results in `function` tokens receiving no colour. All three legend type names therefore use the `variable.*` namespace (`variable`, `variable.parameter`, `variable.function`) so the trie resolves them correctly. The C# encoding does not change — it only emits integer indices.

**Web editor — semantic token theme rules must live in `enableSemanticHighlighting`**: `topsy-turvy-language.js` (Monarch tokenizer + base theme skeleton) can be served from the browser's HTTP cache between releases. `web-editor.js` is reliably fresh. `enableSemanticHighlighting` calls `monaco.editor.defineTheme` for both `topsy-turvy-dark` and `topsy-turvy-light` before enabling semantic highlighting. Redefining an already-registered Monaco theme also invalidates its internal `_tokenTheme` cache (Monaco calls `notifyBaseUpdated()`, which sets `_tokenTheme = null`), so the colours rebuild immediately from the new rules. `registerThemes` in `topsy-turvy-language.js` defines both themes with `rules: []`; the semantic token rules in `enableSemanticHighlighting` are always applied last and win.

**Ternary expression left-recursion guard — `NonTernaryExpression`**: `ExpressionParser` contains a `NonTernaryExpression` static field that is identical to `Expression` minus the `TernaryExpression` alternative. Both `TrueValue` and `Condition` in the ternary parser use `NonTernaryExpression`, not `Expression`. This prevents left-recursion (if `TrueValue` could itself be a ternary, the `TernaryExpression` parser would loop on itself) and prevents `OTHERWISE,` being consumed ambiguously mid-parse (a ternary in the condition position could consume `OTHERWISE,` before the outer ternary sees it). Only `FalseValue` uses full `Expression` via `Parse.Ref(() => Expression!)`, enabling right-chaining. Do not add `TernaryExpression` to `ExpressionStatementParser` — a bare ternary as a top-level statement would conflict with the `Conditional` statement parser, which also starts with `SHOULD IT TRANSPIRE THAT`.

**Guard clause formatter depth — `PostIncrease2` / `PreDecrease2`**: `YEOMAN` is in `SourceFormatter.PostIncrease2Keywords` (depth +2 after the line) and `UNDER ORDERS.` is in `PreDecrease2Keywords` (depth -2 before the line). This produces the same two-level indentation pattern as `IN WHICH CAPACITY?` / `NOTHING COULD BE MORE SATISFACTORY.`. `OTHERWISE,` inside a guard is already a `MidBlockKeyword` (depth -1 before, +1 after), matching its behaviour in `Conditional`.

**Assert `THAT` keyword is NOT in `KeywordData` but IS highlighted**: `THAT` is a structural separator within the `Assert` parser (`THE LAW IS <condition> THAT <errorMessage>`). It is intentionally absent from `KeywordData.Keywords` to avoid polluting the LSP completion list with an ambiguous fragment that also appears in `THAT WILL DO.` and `THAT CONCLUDES THE MATTER.`. The `Assert` parser matches it via `Lexer.Keyword("THAT")` inline — the keyword is only reachable after `THE LAW IS` has already matched. `THAT` is not added to `CanonicalKeywords` in `SourceFormatter`. It IS highlighted for syntax colouring in all three layers (REPL, Monaco, TextMate); in each case, all multi-word `THAT`-containing keywords are tried before the bare `THAT` rule (longest-match in REPL, rule order in Monaco and TextMate), so those longer keywords always win.

**`Keywords` class scope**: `Keywords.cs` in `BWHazel.TopsyTurvy.Ast` covers only strings used in code logic across multiple projects — literals (`VERITY`, `NAY`, `NAUGHT`), type names (`PEER`, `CHANCELLOR`, `PIRATE`, `SAUSAGE-ROLL`, `STANDING`, `FATHOM`, `FOOT`, `YARN`, `STITCH`, `DECREE`), and special names (`THE PROPS`). Single-site parser grammar strings such as `Lexer.Keyword("BEHOLD")` are intentionally left inline. `KeywordData.Keywords` in Analysis remains the authoritative 96-entry list for LSP/completion and must not be duplicated in `Keywords.cs`.

**`PhraseContext.Get` (`BWHazel.TopsyTurvy.Embedded/Analysis/`) is a deliberate duplicate of `CompletionHandler.GetPhraseContext` (`BWHazel.TopsyTurvy.LanguageServer/`)**: the Embedded project must not reference `LanguageServer`, since that project depends on OmniSharp, which Native AOT cannot carry across the C ABI boundary. The two implementations are otherwise identical (trim phrase up to cursor, find the last space-delimited word). If the phrase-parsing rule ever changes — how the phrase or last word is derived, or the 0-indexed line/character contract either method assumes — both call sites must be updated together; each method's XML doc carries a `<b>Constraint:</b>` note pointing at the other.

**`STANDING` modifier is parsed inside `TypeKeyword`, not at the declaration level**: `ExpressionParser.TypeKeyword` tries `STANDING <base-integer-type>` as a `.Try()` alternative before any other type keyword. This means `STANDING` works automatically everywhere a type annotation is accepted: declarations, `IS HENCEFORTH A`, and `AS IT WERE … AS A`. `STANDING` followed by a non-integer type keyword (e.g. `STANDING FATHOM`) will silently fail the `.Try()` and fall through — it will NOT produce a parse error. `STANDING` alone as a type name is also not valid; the parser backtracks and reports a missing type keyword error.

**YARN string indexing — no parser changes**: `VICTIM n ON yarn` and `RECKONING OF yarn` reuse the `ArrayIndexNode` and `ArrayLengthNode` AST nodes unchanged. The runtime dispatches on `LiteralType.String` before the `LiteralType.Array` path in `EvaluateArrayIndex` and `EvaluateArrayLength`. `ExecuteArrayElementAssignment` checks `LiteralType.String` first and throws immediately — strings are always immutable by position in Topsy Turvy.

**Arithmetic widening uses `long` for all integer-family arithmetic**: `ApplyArithmetic` converts both operands to `long` via `ToLong()` before applying the integer operation, then converts the `long` result back to the widest operand type via `CreateIntegerFromLong`. This means unsigned arithmetic for `STANDING CHANCELLOR` (`ulong`) passes through `long`, potentially losing precision for values > `long.MaxValue`. Fix requires a separate unsigned arithmetic path if full `ulong` range arithmetic is ever needed.

**`ApplyArithmetic` signature changed from `Func<int,int,int>` to `Func<long,long,long>`**: all call sites in `EvaluateBinaryOperator` use lambda syntax `(a, b) => a + b` etc. which are type-inferred and compile for `long` without changes. If a future operator needs genuine `int` arithmetic, wrap the lambda explicitly.

**`FOOT` arithmetic result type**: `FOOT` (Single) + any integer type → result is `FOOT`. `FOOT` + `FATHOM` → result is `FATHOM` (the `LiteralType.Double` check fires before the `LiteralType.Single` check in `ApplyArithmetic`). Two `FOOT` operands → `FOOT`. The float path always computes through `double` internally; the `FOOT` path casts the `double` result back to `float`.

**`TopsyTurvyValue.CastTo` for new types routes through `long`/`ulong`/`double`/`char` helpers**: `CastTo` dispatches to private `ToInt64()`, `ToUInt64()`, `ToDouble()`, and `ToChar()` helper methods (on the value instance, not the static `Interpreter` helpers). The `Interpreter` has its own separate static `ToDouble(TopsyTurvyValue)` and `ToLong(TopsyTurvyValue)` helpers. The two sets are independent; keep them in sync if the type list changes.

**`SourceSpan` is a half-open interval**: `Start` is inclusive, `End` is exclusive — `End` points to the column one past the final character of the region, i.e. `[Start, End)`. This matches the LSP range convention used by the web editor. Confirmed by the error-span construction in `TryParse` (`endColumn + 1`).

**`PlaceholderSpan` is a fallback, not the default**: `ParserHelpers.PlaceholderSpan` (`(0,0)-(0,0)`) is returned by `BuildSpan` only when `ActiveSourceMap` is `null` — for example in isolated unit tests that call a parser combinator directly. During normal parsing via `TopsyTurvyParser.Parse`/`TryParse`, `ActiveSourceMap` is always set before the parse and all AST nodes receive real spans mapped from the pre-processed text back to the original source.

**`TypeCheckVisitor` pre-seeds `THE PROPS` in the root scope**: the `Visit` method pushes the root `scopeStack` frame pre-populated with `[Keywords.SpecialNames.TheProps] = LiteralType.Array` and the root `arrayElementTypeStack` frame pre-populated with `[Keywords.SpecialNames.TheProps] = LiteralType.String`.  Without this, valid programmes that reference `THE PROPS` (which is never declared via `PRAY WELCOME`) would receive an undeclared-name error from `InferIdentifier`.

**`TypeCheckVisitor` undeclared-name errors**: `InferIdentifier` emits an error and returns `null` when `LookupSymbol` finds no entry for the referenced name.  `CheckAssignment` emits an error and returns early when the assignment target is not in any scope frame (rather than silently skipping the type-compatibility check as it did before).  `CheckInput` emits an error and returns early when `PRAY TELL`'s target is undeclared.  `InferSummon` was already correct (it emits an error for unknown function names).

**`THE PROPS` exclusion**: `THE PROPS` contains a space, so the existing space-in-name check in `RenameHandler` and `PrepareRenameHandler` excludes it from rename automatically — no explicit check needed. It is pre-populated in `SymbolTable.Build()` with `IsConstant = true` and `TypeDisplayName = "CONSERVATIVE LITTLE LIST OF YARN"`, and excluded from semantic token colouring by the same space check in `SemanticTokensHandler`. Attempting to re-declare it with `PRAY WELCOME` is a runtime error (variable already declared).

**`THE PROPS` parser handling**: `THE PROPS` is a two-word token and cannot be matched by `Lexer.Identifier`. `ExpressionParser.ArrayNameParser` matches it explicitly (trying `Lexer.Keyword("THE PROPS")` first, then falling back to `Lexer.Identifier`) and is used wherever an array name appears in the parser — `ArrayIndexExpression` and `StatementParser.ArrayElementAssignment`. `ThePropsExpression` in the `Expression` chain handles bare references to `THE PROPS` (e.g. `BEHOLD THE PROPS`) and must appear before `IdentifierExpression` so that `THE` is not consumed as a plain identifier.

**`THE PROPS` CONSERVATIVE enforcement**: element assignment on `THE PROPS` follows the same path as any other `CONSERVATIVE` array — `ExecuteArrayElementAssignment` calls `environment.IsConstantInChain("THE PROPS")`, which returns `true`, and a `TopsyTurvyRuntimeException` is thrown. No special-casing is needed in the interpreter.

**LSP stdio**: `.ClearProviders()` must be called before `.AddLanguageProtocolLogging()` when configuring OmniSharp logging. Without it, the default console logger writes to stdout and corrupts the JSON-RPC stream. Configured in `IncantationCommandBuilder.HandleIncantationAsync()`.

**`--stdio` hidden option**: `vscode-languageclient` unconditionally appends `--stdio` to the process args when `TransportKind.stdio` is used. The `incantation` command registers `--stdio` as a hidden `Option<bool>` so `System.CommandLine` accepts it silently. Without this, the CLI rejects it as an unrecognised argument and the LSP server fails to start.

**`OmniSharpServer` alias in `IncantationCommandBuilder`**: `BWHazel.TopsyTurvy.LanguageServer` is both a project namespace (imported via `using`) and the name of OmniSharp's static server class. The alias `using OmniSharpServer = OmniSharp.Extensions.LanguageServer.Server.LanguageServer;` is required to disambiguate. Do not remove it.

**Bundled binary takes precedence**: `resolveCliPath` in `paths.ts` calls `fs.existsSync` on `bin/operetta` at activation time. Once `npm run prebuild:dotnet` has been run, the bundled binary is always used — re-run `npm run prebuild:dotnet` to refresh it, or delete `bin/` to force the dev-layout fallback.

**`DocumentStateManager`**: singleton; rebuilds `SymbolTable` only on a successful parse, preserving the last good table during syntax errors.

**`TryParse` reserved-word errors**: returns `ParseResult(program, errors)` with non-null `program` so the symbol table still builds while squiggle diagnostics are published. `Parse()` (used by the CLI) throws `TopsyTurvySyntaxException` instead.

**Visual editor — `ExpressionNodeTypes` HashSet controls drag and double-click routing**: `VisualEditor.razor` contains a `private static readonly HashSet<string> ExpressionNodeTypes` that lists every `StatementType` string that should be created as an expression node (via `VisualNodeFactory.CreateExpression`). Any type string **not** in this set is silently routed to `CreateStatement`, which returns an empty list for unknown types — the node simply does not appear on the canvas. When adding a new expression node type to the symbol panel or context menu, add its type string to `ExpressionNodeTypes`. Current contents: `"LiteralNode"`, `"IdentifierNode"`, `"OperatorNode"`, `"ArithmeticNode"`, `"BitwiseNode"`, `"LogicalNode"`, `"VariadicNode"`, `"WovenNode"`, `"TernaryNode"`, `"SummonNode"`, `"ArrayIndexNode"`, `"ArrayLengthNode"`, `"ExpressionCastNode"`.

**Visual editor — SUMMON node has both data ports and flow ports**: every SUMMON node (built by `VisualGraphBuilder` or `VisualNodeFactory`) has FlowIn "In", FlowOut "Out", DataIn "Function", DataIn "Arg X" ports, and DataOut "Out". When used as an expression, DataOut is wired to the consumer's DataIn and the flow ports are left disconnected. When used as a statement (for void-return function calls), FlowIn is wired from the previous flow node and FlowOut to the next — `WalkFlowStatements` then encounters it and `ReconstructSingleStatement` wraps it in `ExpressionStatement`. Both use cases call `ReconstructSummonFromNode` internally.

**Visual editor — ArrayIndex and ArrayLength use "Variable" DataIn port for the array name**: both `ArrayIndexNode` and `ArrayLengthNode` express the target array as an Identifier node connected to a DataIn port labelled "Variable" — the same pattern as IS APPOINTED and PRAY TELL. `GetTargetNameFromPort` is the canonical helper for reading the array name from this port in the converter. Factory-created nodes have the "Variable" port but no pre-wired Identifier node; the user drags a symbol from the Symbols panel and connects it. The `ArrayIndexNode` title is "VICTIM" (subtitle "at index"), not the array name.

**Visual editor — SUMMON argument ports live on the SUMMON node, not the function-name node**: the SUMMON sub-graph has two nodes — the outer SUMMON node and an inner SummonFunctionNode (the function name). Arg DataIn ports (`"Arg 1"`, `"Arg 2"`, …) are added to the **SUMMON node**; the SummonFunctionNode has only a DataOut. `VisualGraphToAstConverter.ReconstructSummonFromNode` reads the SUMMON node's DataIn ports, filters out `"Function"`, orders by label, and treats each as an argument. Do not add arg ports to the SummonFunctionNode.

**Visual editor — SUMMON architecture (Phase D)**: SUMMON has FlowIn "In", DataIn "Function", DataOut "Out", FlowOut "Out". The "Function" DataIn accepts a FunctionReferenceNode's DataOut. FunctionReferenceNode (created when dragging a function from the Symbols panel) has one named DataIn port per parameter and a single DataOut "Out". Arguments are wired into FunctionReferenceNode's DataIn ports, not SUMMON's. `ReconstructSummonFromNode` follows the "Function" DataIn → FunctionReferenceNode, then reads that node's DataIn ports for argument expressions. Do not add argument ports to SUMMON itself.

**Visual editor — FunctionReferenceNode is expression-typed**: `"FunctionReferenceNode"` is in `ExpressionNodeTypes` in `VisualEditor.razor`. It is routed through `VisualNodeFactory.CreateExpression`, not `CreateStatement`. Omitting it from `ExpressionNodeTypes` silently prevents the node from appearing on canvas when dragged from the Symbols panel.

**Visual editor — TERM parameter nodes use `VisualNodeKind.Parameter`, not `VisualNodeKind.Declaration`**: parameter TERM nodes are rendered in dark blue (`#1565c0`), distinct from green Declaration nodes. Both `VisualNodeWidget.AddParameter()` and `VisualEditor.CreateSymbolNode` (for `VisualSymbolKind.Parameter`) must set `Kind = VisualNodeKind.Parameter`. `VisualGraphBuilder.CreateFunctionBodySubgraph` also uses `VisualNodeKind.Parameter`. Using `Declaration` gives the wrong colour and may confuse the scanner.

**Visual editor — body-sync bug is fixed; never re-introduce `ReconstructBodyStatements`**: the retired helper `ReconstructBodyStatements(original.Body, diagram)` iterated the frozen original AST, ignoring all live graph edits made after the last parse. It has been removed. All builder-backed `Reconstruct*` methods now use `WalkBranchBody` or `WalkFlowStatements` to follow live port links. Do not re-introduce this pattern.

**Visual editor — `WalkFlowStatements` `skipFirst` semantics**: `skipFirst = true` (default) means the first node visited is `NextFlowNode(startNode)` — i.e. the node after `startNode` in the flow chain. `skipFirst = false` means `startNode` itself is the first statement to reconstruct — used when a branch-entry sentinel node IS the structural start of the body (no header node above it). `IsBranchHeaderSentinel` determines which case applies: it returns `true` for all 8 branch-header types, meaning those headers need `skipFirst = false` when passed as `startNode` to `WalkBranchBody`.

**Visual editor — symbol panel resize JS uses `initialWidth` parameter**: `window.visualSymbolPanel.startResize(startX, initialWidth, dotNetRef)` computes `initialWidth + (e.clientX - startX)` on each `mousemove`. The `initialWidth` argument must be the panel's current pixel width at the time the drag starts — captured from `this.panelWidth` in `OnResizeHandleMouseDown`. Do not hardcode the initial width to a constant inside the JS function.

**Visual editor — operator node port count on unary/binary switch**: `VisualNodeWidget.AdjustOperatorPortCount` only *adds* a missing second DataIn port when switching from a unary to a binary operator. It cannot *remove* extra ports — ZBD's `Ports` collection has no single-item `Remove` overload. The converter's `ReconstructOperatorFromNode` null-filters unconnected DataIn ports (lines where `GetExpressionSourceNode` returns null are skipped), so leftover ports are harmless.

**`EITHER` uses `OR` separator, not `AND`**: `EITHER x OR y` is the correct syntax per SPEC.md. The parser uses `SingleOrExpression` (a private parser that matches ` OR <expression>`) for the second operand of `EITHER`; all other non-variadic binary operators use `SingleAndExpression` (` AND <expression>`). The code generator emits `EITHER x OR y`. Any new test using `EITHER` must use `OR` as the separator.

**`SUMMON` call syntax**: `SUMMON name WITH arg1 AND arg2 IF YOU PLEASE.` / `SUMMON name WITH NOTHING IF YOU PLEASE.` — uses `WITH` as the argument separator, not `AND`.

**Blazor WASM timeout**: `CancellationTokenSource(TimeSpan)` relies on a timer callback that cannot fire while `Interpreter.Execute()` is blocking the single WASM thread. Set `ExecutionTimeout = TimeSpan.FromSeconds(10)` in `InterpreterExecutionOptions` instead — this stores a `DateTime` deadline checked by `CheckCancellation()` inside every loop body via `DateTime.UtcNow`.

**`PRAY ADMIT` path resolution**: `ExecuteImport` resolves relative import paths against the directory of the *importing* file, not the process CWD. `Interpreter.Execute()` accepts an optional `sourceFilePath` parameter; when `null` (e.g. Blazor WASM), the raw path from the AST is used unchanged.

**Web editor virtual FS resolver**: `Interpreter.Execute()` accepts an optional `Func<string, string?> fileResolver`. When supplied, `ExecuteImport` calls this delegate instead of `File.ReadAllText`, enabling `PRAY ADMIT` to resolve imports from the in-memory virtual filesystem. Import paths must be bare filenames — no directory prefix.

**Cross-document LSP awareness**: `DocumentStateManager.AllDocuments()` returns a snapshot of all open documents. Attempt lookup in the current document's `SymbolTable` first; on failure, iterate `AllDocuments()` skipping the current URI. For `DefinitionHandler`, the URI returned by the cross-file fallback must be used as `Location.Uri` in the response. Only `Function` kind symbols are pulled from other documents for hover, completion, and semantic tokens. `WorkspaceSymbolHandler` and `ReferencesHandler` aggregate across all documents unconditionally.

**Multi-word keyword completion**: `CompletionHandler` uses server-side phrase filtering, `FilterText = lastWord`, `InsertText = keyword[insertOffset..]`, `isIncomplete = true`, and a space trigger character to keep the list live as the user types through multi-word phrases.

**Web editor — localStorage single-key merge pattern**: all editor preferences are stored under one key (`topsy-turvy-editor`) as a merged JSON object. `savePreferences(preferences)` always reads the existing object first and spreads the incoming partial update over it (`{ ...existing, ...incoming }`). This lets JS-managed keys (split ratios `horizontalSplit`, `verticalSplit`) and C#-managed keys (files, active file, dark mode, etc.) be written independently without clobbering each other. The C# side calls `topsyTurvy.loadPreferences()` (returns the full object) and `topsyTurvy.savePreferences(EditorPreferences)` via `JSRuntime`. Blazor's JS interop layer serialises `EditorPreferences` to/from JSON using `System.Text.Json`, respecting `[JsonPropertyName]` attributes on the class and `[JsonConstructor]`/`[JsonPropertyName]` on `VirtualFile`.

**Web editor — `[JsonConstructor]` on `VirtualFile` is required for round-trip deserialisation**: `VirtualFile.Name` has no setter (it is set only via the constructor). Without `[JsonConstructor]`, `System.Text.Json` cannot deserialise JSON into a `VirtualFile` instance and throws. The attribute must remain on the constructor. Parameter names in the constructor must match the `[JsonPropertyName]` values on the corresponding properties (case-insensitive) — `System.Text.Json` matches by convention.

**Web editor — terminal theme not applied dynamically by XtermBlazor**: XtermBlazor reads `Options` only once at initialisation. Two mechanisms are therefore needed: (1) `terminalOptions` in `Editor.razor` is an expression-bodied property (not a `readonly` field) that reads `this.IsDarkMode`, ensuring the correct theme is applied on first render since the `IsDarkMode` cascading parameter is set before the first `OnAfterRenderAsync`; (2) `topsyTurvy.setTerminalTheme(isDarkMode)` is called from `OnParametersSetAsync` whenever the theme changes, updating `term.options.theme` directly on the xterm instance. Terminal colours: dark mode — background `#1e1e1e`, foreground `#d4d4d4`; light mode — background `#f5f5f5`, foreground `#1e1e1e`.

**Web editor — terminal output persistence via `lastRunOutput`**: `lastRunOutput` accumulates output lines joined by `\n`. It is saved as `TerminalOutput` in `EditorPreferences` after every run (in the `finally` block of `RunProgramAsync`) and after clear (`ConfirmClearAsync`). It is reset to `string.Empty` at the start of `RunProgramCoreAsync` (right after `terminal.Clear()`) so a failed run does not persist stale output from a previous successful run. On restore, `TerminalOutput` is split on `'\n'` and each line is replayed via `terminal.WriteLine`.

**Web editor — preference save triggers**: `SavePreferencesAsync` is called from: `OnContentChangedAsync` (the 400 ms debounce path, after syncing the active file's `Content`), `@bind-Value:after` on both `MudTextField` elements (CLI args and stdin), `OnGsLabelsToggledAsync` (G&S labels switch), `OnParametersSetAsync` when dark mode changes, the `finally` block of `RunProgramAsync` (after every run including failures), and `ConfirmClearAsync` (after terminal is cleared).

**Web editor — preference restore order in `RestorePreferencesAsync`**: G&S labels → CLI args → stdin → files (clear list, add each, build symbol table) → active file (set `activeFileName`, call `monacoEditor.SetValue`) → terminal output (replay lines, set `lastRunOutput`) → dark mode (call `ToggleDarkMode?.Invoke()` only when stored value differs from current). `StateHasChanged()` is called at the end. Restore is invoked from `OnAfterRenderAsync` on first render, after `initPaneDrag` and before `editorReady = true`.

**Web editor — terminal fit private API**: `fitTerminal` reads `term._core._renderService.dimensions.css.cell.height`. This is an internal xterm.js property — if the terminal stops filling its pane after an XtermBlazor upgrade, check this path first.

**Web editor — editor content API**: per-file content is stored in `VirtualFile.Content` and loaded/saved via `monacoEditor.SetValue` / `blazorMonaco.editor.getValue`. URI-based Monaco models were considered but abandoned — replacing BlazorMonaco's default model breaks the `onDidChangeModelContent` event system.

**Web editor — output download**: `Editor.razor` stores the last run's output in `lastRunOutput`. `DownloadTerminalAsync` uses this field directly rather than reading the xterm buffer. `lastRunOutput` is reset when the terminal is cleared via `ConfirmClearAsync`.

**Web editor — open file replace**: `OpenSingleFileAsync` handles the case where the opened file shares a name with an already-open tab. When the replaced file is the active tab, call `monacoEditor.SetValue(content)` directly — do not call `SwitchFileAsync`, which would re-sync old content back over `VirtualFile.Content`.

**`CONSERVATIVE` constant enforcement**: `IS APPOINTED` (assignment) and `PRAY TELL` (input) both ultimately call `TopsyTurvyEnvironment.Assign`, which checks `this.constants.Contains(name)` before allowing the write — the constraint is enforced centrally. `AS IT WERE` does not need a guard because it produces a new value without mutating the source variable.

**`LITTLE LIST OF` in the parser, not `A LITTLE LIST OF`**: In the source text `AS A LITTLE LIST OF YARN`, the phrase `AS A` is consumed by `Lexer.Keyword("AS A")` — this matches exactly 4 characters `A-S-space-A`, positioning the cursor immediately after the final `A`. The remaining token stream therefore starts with ` LITTLE LIST OF`, not ` A LITTLE LIST OF`. The array-type combinator must match `Lexer.Keyword("LITTLE LIST OF")`. Using `Lexer.Keyword("A LITTLE LIST OF")` will fail to match because the `A` was already consumed. This applies in `StatementParser.ArrayDeclaration`, `ExpressionParser.ExpressionCast`, and anywhere else `AS A` is followed by an array type. `GRAMMAR.ebnf` carries an explanatory comment on the `ArrayDeclaration` production.

**`RECKONING OF` is an expression, not a statement, and must appear in `ExpressionStatementParser`**: `ExpressionStatementParser` in `StatementParser.cs` has its own explicit list of expression alternatives (distinct from `ExpressionParser.Expression`). Any new expression that can appear as a standalone statement must be added to **both** lists. `ArrayLengthExpression` is included in both. Omitting it from `ExpressionStatementParser` causes a `TopsyTurvySyntaxException` when parsing a bare `RECKONING OF arr` as a statement, even though the parser itself is correctly wired. `RECKONING OF` is read-only — it does not trigger `IsConstantInChain` and works on `CONSERVATIVE` arrays without error.

**Array element assignment bypasses `TopsyTurvyEnvironment.Assign`**: `VICTIM n ON arr IS APPOINTED val` mutates the `List<TopsyTurvyValue>` stored inside the array value directly — it does not call `Assign`. The standard CONSERVATIVE guard in `Assign` is therefore never triggered. Instead, `ExecuteArrayElementAssignment` calls `environment.IsConstantInChain(arrName)` (which walks the enclosing scope chain) before mutating the list. Do not rely on `Assign` for constant enforcement in array element mutations.

**Sized array size+BEING mutual exclusivity is a runtime error, not a parse error**: When `ArrayDeclarationNode.Size` is non-null and `InitialValues.Count > 0`, `ExecuteArrayDeclaration` throws a `TopsyTurvyRuntimeException`. The parser does not prevent this combination — it happily parses `PRAY WELCOME arr AS A LITTLE LIST OF 3 PEER BEING 1 AND 2 AND 3 IF YOU PLEASE.` into an `ArrayDeclarationNode` with both `Size = 3` and three `InitialValues`. The guard lives exclusively in the runtime. Similarly, a negative size is caught only at runtime.

**Array reference semantics**: `PRAY WELCOME alias AS A LITTLE LIST OF PEER` followed by `alias IS APPOINTED original` makes both variables point to the same `List<TopsyTurvyValue>` instance. Mutating an element via `alias` is visible via `original` and vice versa. There is no copy-on-assign. This is consistent with how .NET reference types work and is intentional — value semantics for arrays are deferred to a future struct/class type system.

**1-based array indexing and ascending loops**: `VICTIM 1 ON arr` accesses the first element. The ascending loop (`BY A LEGAL FICTION ASCENDING x UNTIL n`) resets its counter to 0 at the start of each iteration — using it to index into an array would start at `VICTIM 0`, which is out-of-range. Use a WHILST loop for array traversal: `count IS APPOINTED 1`, then `BY A LEGAL FICTION WHILST HARDLY EVER PRE-ADAMITE count AND length`, incrementing `count` manually inside the loop body. See `examples/language/arrays.topsy` for the canonical pattern.

**`AS IT WERE` is an expression, not a statement**: `AS IT WERE <expr> AS A <type>` is parsed as an `ExpressionCastNode` (extends `Expression`).  When used as a standalone statement it is wrapped in an `ExpressionStatement`; the type checker emits a warning for the discarded return value.  There is no `ExecuteExpressionCast` method — cast evaluation is handled entirely inside `EvaluateExpression`.

**Try/catch inline expression**: `WITH THE GREATEST RESPECT, <expression>` requires the expression on the *same line* as the keyword. Writing it on the following line causes a parse failure. The formatter's indentation rules for this construct diverge from valid parser input by design.

**`MODIFIED RAPTURE` catch binding is mandatory and creates a child scope**: the parser requires `MODIFIED RAPTURE, Name` — the binding is not optional.  The interpreter calls `environment.CreateNested()` and declares `Name` as a `YARN` variable in that child scope.  The binding is inaccessible outside the exception block — accessing it after `THAT CONCLUDES THE MATTER.` produces a runtime error.  The comma delimiter (`MODIFIED RAPTURE, Name`) is required to avoid ambiguity with statement-starting identifiers (e.g. `result IS APPOINTED ...`) that the parser would otherwise greedily consume as the binding name.

**Web editor — MudTabs API (MudBlazor 9.x)**: `ScrollButtons` was renamed to `AlwaysShowScrollButtons`. `PanelClass` belongs on `MudTabPanel`, not `MudTabs`. Using the old names produces MUD0002 analyzer warnings.

**Web editor — auto-indent**: Monaco's `indentationRules` only take effect when `autoIndent` is `'full'` (numeric `4`). This is a construction-time option — set via `AutoIndent = "full"` in `StandaloneEditorConstructionOptions`; `updateOptions` has no effect on it.

**CLI version string**: `AssemblyInformationalVersionAttribute` = `{Version}+{SourceRevisionId}`. `PedigreeCommandBuilder` splits on `+` and holds `SpecVersion = "0.2.0"` as a private constant.

**`--tiptoe` output routing**: with `--tiptoe`, errors go to stderr and program output to stdout. Without it, all output — including error panels — goes to stdout via `AnsiConsole.Write`. E2E tests that assert on error messages must use `--tiptoe`.

**Spectre.Console panel header truncation in non-TTY mode**: when stdout is redirected, Spectre.Console truncates panel headers to fit the content width with a Unicode ellipsis (`…`). A short body (e.g. a short filename) produces a narrow panel. Workaround: use a long filename to widen the panel, or assert only the unambiguous prefix.

**SignatureHelp two-update pattern**: signature help tests for in-progress `SUMMON` calls (where `IF YOU PLEASE.` has not been typed yet) must seed `DocumentStateManager` with a successful parse before updating with the incomplete source, so the last-good symbol table is preserved and available to the handler.

---

## 3. LSP Handlers & CLI Commands

### LSP Handlers (`operetta/BWHazel.TopsyTurvy.LanguageServer/` — class library, started via `operetta incantation`)

| Handler | Purpose |
|---|---|
| `TextDocumentSyncHandler` | `didOpen`/`didChange`/`didSave`/`didClose`. Calls `TryParse`, publishes diagnostics, updates `DocumentStateManager`. |
| `HoverHandler` | Markdown tooltip for variables, functions, and parameters. |
| `DefinitionHandler` | Go-to-definition using `DefinitionLine` recovered from source scan. |
| `CompletionHandler` | Symbols + 74 keywords. Server-side phrase filtering for multi-word keywords. Known edge case: variable names that are keyword prefixes may trigger false keyword-context mode. |
| `SemanticTokensHandler` | Colours variables, parameters, functions by whole-word line scan. Emits the `readonly` modifier for `CONSERVATIVE` constant variables, mapping to the `variable.other.constant` TextMate scope via `semanticTokenScopes` in `package.json`. Excludes symbols whose names contain a space (e.g. `THE PROPS`). |
| `RenameHandler` | Whole-word case-insensitive rename; skips comments and strings. Single-file only. Excludes multi-word built-ins via space-in-name check (covers `THE PROPS`). |
| `PrepareRenameHandler` | Pre-validates rename targets; returns placeholder range for input box. Same space-in-name exclusion as `RenameHandler`. |
| `DocumentSymbolHandler` | Populates VS Code Outline panel from `SymbolTable`. |
| `ReferencesHandler` | Find All References; respects `IncludeDeclaration`. |
| `SignatureHelpHandler` | Parameter hints inside `SUMMON` calls; counts `AND` tokens for active parameter. |
| `FoldingRangeHandler` | Stack-based fold regions for all block constructs and comments. |
| `DocumentFormattingHandler` | Normalises keyword casing (74 keywords) and applies 2-space libretto indentation. |
| `CodeLensHandler` | Inline reference-count annotations above declarations; links to Find All References. |
| `WorkspaceSymbolHandler` | Handles `workspace/symbol` requests; searches all open documents by query string. |

### CLI Commands (`operetta/BWHazel.TopsyTurvy.Cli/CommandBuilders/`)

| Command | Alias(es) | Options | Notes |
|---|---|---|---|
| `perform <file>` | `stage`, `run` | `--tiptoe` | Runs a `.topsy` file. |
| `mount <project>` | `init` | `--title`, `--or`, `--hollow`, `--tiptoe` | Creates a project directory. Without `--hollow`, also scaffolds `<project>/<project>.topsy`. |
| `commission <file>` | `new` | `--title`, `--or`, `--tiptoe` | Scaffolds a Hello World `.topsy` file. UTF-8, no BOM. Both commands delegate content to `FileManager.BuildFileContent`. |
| `rehearse <file>` | `check` | `--tiptoe` | Syntax-checks without executing via `ProgramRunner.Check`. |
| `sorcerer <file>` | `compile` | `--emit`/`-e`/`--pour`/`-p`, `--target`/`-t`/`--philtre`/`-f`, `--output`/`-o`, `--abridged`/`-a`, `--chromatic`/`-c`, `--config`/`-g`/`--tablet`/`-l`, `--tiptoe` | Compiles a Topsy Turvy programme or a UtopIR programme. `--emit` (wins if both given) prints an intermediate representation to stdout: `preprocess`/`p`/`cue`/`c` (former `cue` behaviour), `ast`/`a`/`promptbook`/`b` (former `promptbook` behaviour, `NodeJsonConverter`), `utopir`/`u` (UtopIR source text), `utopir-ast`/`s` (UtopIR AST as JSON, `UtopIRInstructionJsonConverter`/`UtopIROperandJsonConverter`), `dotnet-cil`/`d` (real CIL disassembly via `CilOutputKind.IlSourceOnly`). `--target` (only `dotnet`, the default) builds a runnable executable assembly (`CilOutputKind.Executable`) to `--output` (default: input filename with `.dll` extension). Extensions are validated per format/target: `.topsy` only for `preprocess`/`ast`/`utopir`; `.topsy` or `.utopir` for `utopir-ast`/`dotnet-cil`/`target dotnet` — a `.utopir` input is genuinely parsed via `UtopIRParser` (`BWHazel.TopsyTurvy.UtopIR.Parser`), with malformed source reporting a real syntax-error diagnostic rather than "not yet supported". `--config key:value` (repeatable) is resolved via each `<str>`'s own `OptionsResolvers/IEmitterOptionsResolver<TOptions>` implementation — an unrecognized key produces a non-fatal warning (panel, or a `Warning:` line in `--tiptoe`) and is ignored rather than blocking the operation; a malformed `key:value` token (no `:`) is still rejected outright before dispatch. `varFormat` (`verbose`/`numeric`, case-insensitive) is the first genuinely recognized `--config` key, accepted by `utopir`/`utopir-ast`/`dotnet-cil`/`target dotnet` to choose the `ITemporaryVariableNameFormatter` used when transforming Topsy Turvy source — an invalid value (not `verbose`/`numeric`) is a real error, unlike an unrecognized key; ignored (with a warning) for `.utopir` input, which has no transform step. Supersedes the former `sorcerer promptbook`/`sorcerer cue` subcommands, which are removed. |
| `incantation` | `lsp` | `--stdio` (hidden) | Starts the OmniSharp LSP server over stdio. Invoked by the VS Code extension. Promoted from a `sorcerer` subcommand to root level. |
| `playbill <file>` | `docs` | `--chromatic`, `--tiptoe` | Generates Markdown documentation for all documented symbols and prints to stdout. `--chromatic` applies Spectre.Console markup for titles, bold, italic, and inline code. |
| `cadenza` | `interactive` | `--tiptoe` | Interactive REPL session. Variables and function definitions persist across turns. Styled `In`/`Out` panels in rich mode. REPL commands: `:exit`/`:quit`, `:begone`/`:clear`, `:entracte`/`:help`, `:multiline`/`:madrigal`, `:singleline`/`:patter`, `:armoury [all|variables|functions]`/`:env`. |
| `pedigree` | `info` | `--tiptoe` | Shows CLI version, commit SHA, and language spec version. |

---

## 4. Known Gaps

**`PrincipalBlockNode.Declarations` uses a weak base type instead of a discriminated union**: `Declarations` is typed as `IReadOnlyList<Statement>` rather than a union of `DeclarationNode | ArrayDeclarationNode`, because C# does not yet provide first-class discriminated-union syntax. Callers must pattern-match at runtime to determine the concrete type. This will be revisited when discriminated unions land in C# / .NET 11.

**Ascending loop always starts from 0 (spec-level limitation)**: `ASCENDING <var>` resets the loop variable to `0` at the start of every loop per the spec (`<var>` begins at `0`). There is no syntax to specify a different starting value. Descending loops have no such constraint — they use the variable's current declared value. Fix requires a spec change (remove the "begins at `0`" constraint, or add `FROM <n>` syntax) and removing the hardcoded `Assign(..., Integer(0))` in `ExecuteAscendingLoop`.

**Monarch tokenizer keyword duplication**: the keyword list appears in `topsy-turvy-language.js` (Monarch tokenizer) and `topsy-turvy.tmLanguage.json` (TextMate grammar). The JS and TextMate copies cannot consume the C# library and remain separate. When adding a language feature, update both in the same change.

**Web editor interactive stdin (deferred)**: `BufferedWebIO` reads from a pre-supplied `Queue<string>`. True interactive stdin requires making `ITopsyTurvyIO` async — a new `InteractiveWebIO` implementation and an XtermBlazor input handler.

**`BWHazel.TopsyTurvy.WebEditor.E2ETests` requires PowerShell Core locally**: the Playwright browser binaries are installed via `pwsh <output-dir>/playwright.ps1 install chromium --with-deps` (see `.github/workflows/build-test.yml` line ~27, which already runs this as an explicit CI step). `pwsh` is not installed by default on macOS — install once with `brew install --cask powershell`, then run the install command locally against the built test project's output directory. Without `pwsh` present, all 11 `WebEditorAppFixture`-based tests fail at `InitializeAsync` with `PlaywrightException: Executable doesn't exist`; this is an environment-setup gap, not a code defect, and does not indicate a regression. A `.csproj` `AfterTargets="Build"` MSBuild target that shells out to `pwsh` was considered and rejected — it would hard-fail every local build on a machine without `pwsh`, which is worse than the current explicit-step approach.

**LSP squiggle accuracy — Option 1 done, Option 2 deferred**: syntax errors now report at the correct line. The `WsMany<T>` combinator in `ParserHelpers` replaced all 12 `Ws(X).Try().Many()` block-body sites; it separates whitespace consumption from the statement parse and propagates partial-match errors (where an opening keyword was consumed but the body failed) at the actual failure position rather than resetting the cursor. `TryParse()` still stops at the **first** syntax error — there is no error-recovery or multi-error reporting. Option 2 (deferred): full multi-error recovery requires a hand-written recovery loop that records the error, advances the cursor to the next statement-opening keyword, and continues; this is significant additional work and should be considered after static typing is introduced (more complex constructs = larger recovery surface area).

**`ValidateSymbolNames` only checks top-level statements**: `TopsyTurvyParser.ValidateSymbolNames` walks only the top-level statement list of `ProgramNode`. Declarations nested inside function bodies, conditionals, loops, or try-catch blocks are not validated — a variable declared with a reserved-keyword name inside a nested block will not produce a diagnostic. Note that the parser may independently reject some of these cases (reserved words that cannot be lexed as identifiers), but the semantic check is incomplete by design. Fix requires recursing into all block-bearing statement types (`FunctionDefinitionNode`, `ConditionalNode`, `LoopNode`, `TryCatchNode`, etc.) or introducing an AST visitor.

**Web editor JS not linted or formatted**: `topsy-turvy-language.js` and `web-editor.js` have no ESLint or Prettier configuration.

**VS Code extension file icon**: the `icon` field on the `languages` contribution is only shown when no file icon theme is active. Long-term fix: submit a PR to `vscode-icons` adding a `.topsy` entry.

**`<example>` must be nested inside `<remarks>` for dotnetmd rendering**: the standard XML doc convention places `<example>` as a top-level sibling of `<summary>`, but dotnetmd only renders examples when `<example>` is nested inside `<remarks>`. Methods with no `<param>` tags (e.g. `TopsyTurvyValue.Null()`) and `override` methods (e.g. `TopsyTurvyValue.ToString()`) are additionally prone to their documentation being silently dropped. The non-standard placement must be maintained until dotnetmd is replaced or this behaviour changes upstream.


**Visual editor classes have no unit tests**: `VisualGraphBuilder`, `VisualGraphToAstConverter`, `VisualNodeFactory`, and `VisualTypeMaps` contain significant pure logic that is independently testable without a Blazor rendering context (Z.Blazor.Diagrams model types are plain C# objects). Tests should live in `Tests/Visual/VisualEditorTests.cs`; if the test project cannot reference the WASM-target WebEditor project, extract the `Visual/` classes into a non-Blazor class library `BWHazel.TopsyTurvy.WebEditor.Visual`. Coverage targets: `VisualGraphBuilder` (HARK! node presence, link wiring), `VisualGraphToAstConverter` (round-trip: parse → build → convert → generate → re-parse → diagnostics empty), disconnected DataIn port handling, `VisualNodeFactory` (correct `Kind`, `StatementType`, port roles; block types yield paired opener+closer). Follow the Shouldly assertion style from `TopsyTurvyCodeGeneratorTests`.

**RESOLVED — UtopIR temporary variable names now pluggable, no longer break for float/string literal operands**: `TopsyTurvyToUtopIRTransformer` no longer builds temp names itself — naming is delegated to an `ITemporaryVariableNameFormatter` (new `operetta/BWHazel.TopsyTurvy.UtopIR.Transformer/VariableNameFormatters/` folder), supplied via the constructor (`TopsyTurvyToUtopIRTransformer(ITemporaryVariableNameFormatter? formatter = null)`), matching option (1) from the list previously here. The transformer's three temp-generating call sites (arithmetic flattening, explicit `AS IT WERE` cast, implicit widening cast) now hand the raw ingredients (mnemonic/keyword + operand labels) to `formatter.CreateName(params string[] parts)` rather than composing the name themselves — the formatter owns composition entirely. Two implementations: **`IncrementingIntVariableFormatter`** (the new default) — LLVM-SSA-style `_0`, `_1`, `_2`, ... ignoring the parts entirely; short, and collision-free by construction. **`InstructionDetailVariableFormatter`** — the original descriptive scheme (`_sum_a_b`), extracted and strengthened: each part is sanitized independently before joining (a decimal point becomes `p`, any other non-alphanumeric/non-`-`/non-`_` character becomes `-`, so a future float/string literal's textual representation can no longer produce an invalid identifier) and de-duplicated within a formatter instance (a repeated composed name gets a `_2`, `_3`, ... suffix, addressing the name-clash risk two structurally identical expressions previously had). CLI wiring: new `--config varFormat` key (`numeric`/`verbose`, case-insensitive) recognized by `UtopIrResolver`, `UtopIrAstResolver`, and `DotNetCilOptionsResolver` (see the `Cli/OptionsResolvers/` entry above) — an invalid value throws `ToolchainConfigException` (a real error, the first genuine use of that path); an unrecognized key still just warns. `varFormat:verbose` given alongside `.utopir` input (no transform step, so nothing to format) is reported as ignored via a warning, not silently dropped — `numeric` is not warned about there since it's indistinguishable from the unset default and has no practical effect either way. New tests: `InstructionDetailVariableFormatterTests`/`IncrementingIntVariableFormatterTests` (`UtopIR.Tests/Transformer/`, 6 total); existing `TopsyTurvyToUtopIRTransformerTests` pinned to `InstructionDetailVariableFormatter` explicitly, since its assertions target descriptive names, no longer the transformer's default; four new `SorcererCommandTests` covering both formats, the invalid-value error, and the `.utopir`-input warning.

**UtopIR — `CONSERVATIVE` declarations not inlined in v0.0.1-preview1**: `TopsyTurvyToUtopIRTransformer` lowers `CONSERVATIVE` (constant) declarations identically to mutable declarations — a `welcome` instruction followed by an `appoint`. True constant propagation (replacing every reference to the constant with its literal value and omitting the `welcome`/`appoint` pair) requires a symbol-table pass over the UtopIR instruction list. This will be implemented as an optimiser pass inside the `Transformer` project in a future version. No new UtopIR instruction or AST node is required — CIL and LLVM IR both handle constants through inlining rather than a dedicated local-variable instruction.

**RESOLVED — UtopIR mixed-type arithmetic used to produce invalid/unverifiable CIL**: Topsy Turvy legitimately permits arithmetic between operands of different numeric types (e.g. `SUM OF <peer> AND <chancellor>`) — `TypeCheckVisitor.Widen`/`Interpreter.GetWidestIntegerType` both confirm the language's own semantics: the result type is the *widest* of the two operand types. `TopsyTurvyToUtopIRTransformer.TransformArithmetic` previously had zero type awareness for its operands, so a genuinely mixed-type `ArithmeticInstruction` reached `CilEmitter` completely unmarked and produced `ldloc`/`ldloc`/`add` with no `conv.*` conversion between mismatched operand widths — invalid per ECMA-335, only silently tolerated by the .NET JIT. Fixed across two changes: (1) the `were` UtopIR instruction (`£<target> = were <value>, <type>`) — `WereInstruction`, `UtopIRCodeGenerator` rendering, `CilEmitter.EmitWere`/`EmitConversion` (covers all eight integer types), and the explicit `AS IT WERE` cast (`ExpressionCastNode` → `TransformCast`); (2) automatic widening in `TransformArithmetic` (see the `UtopIR.Transformer` file inventory entry above), which now inserts a `WereInstruction` on the narrower operand whenever two arithmetic operands' types differ, so every `ArithmeticInstruction` the Transformer produces is guaranteed to have matching operand types. `CilEmitter.EmitArithmetic` now validates this invariant defensively (throws `InvalidOperationException` if a hand-built or future non-Transformer producer of `ArithmeticInstruction` ever violates it) rather than blindly inferring the result type from `Operand1` alone as it used to. Verified end-to-end: a `PEER + CHANCELLOR` mixed-type programme run through the full parser → transformer → CIL emitter → loaded-and-invoked assembly produces the correct widened result, with the emitted `conv.i8` confirmed both via `dotnet test` and by directly inspecting `CilEmitResult.IlSource`.

**RESOLVED — `AppointInstruction` with a bare integer literal into a differently-typed declared variable used to produce invalid IL, tolerated only by the JIT**: found while writing an earlier mixed-type E2E test. `TopsyTurvyToUtopIRTransformer.TransformDeclaration`/`TransformAssignment` previously passed a `LiteralNode`'s value straight through as a `LiteralOperand` with no comparison against the target's declared type, so `PRAY WELCOME large_number AS A CHANCELLOR` followed by `large_number IS APPOINTED 200` produced an `AppointInstruction` whose `LiteralOperand` value was a genuine `int`, stored via plain `stloc` into a `long` local — an `int32`→`int64` stack-type mismatch with no `conv.i8`, invalid per ECMA-335, silently tolerated by the .NET JIT. **Fix**: both `TransformDeclaration` and `TransformAssignment` now infer the value operand's type via `InferOperandType` and, when the declared/target type is one of the eight integer `UtopIRType` variants (`IsIntegerType`) and differs from the operand's type, insert a `WereInstruction` cast via the (renamed) `CastOperandIfNeeded` — the same helper `TransformArithmetic` already used for widening, so no new emitter work was needed. Non-integer types (`Fathom`/`Foot`/`Decree`/`Stitch`/`Yarn`) are explicitly excluded from this check, since their literal values have no `InferOperandType` mapping and never needed a cast in the first place. Verified end-to-end: `PRAY WELCOME big_number AS A CHANCELLOR` then `big_number IS APPOINTED 200` now compiles to a genuine `conv.i8` before the `stloc`, confirmed both via `dotnet test` and by inspecting `CilEmitResult.IlSource` directly.

**RESOLVED — integer literals used to silently overflow/wrap to `Int32` regardless of declared target type or magnitude**: `Lexer.IntegerLiteral` was typed `TextParser<int>` (backed by Superpower's `Numerics.IntegerInt32`) — every integer literal was parsed directly to `System.Int32` at the lexer stage, before the parser even built the AST node, regardless of magnitude. `PRAY WELCOME big_number AS A CHANCELLOR` followed by `big_number IS APPOINTED 5000000000` used to silently produce `705032704` (`5000000000 mod 2^32`). **Fix**: `Lexer.IntegerLiteral` now returns `long` (`Numerics.IntegerInt64`). `ExpressionParser.LiteralExpression`'s integer arm resolves the final `LiteralType` (`Integer` vs `Long`) from the parsed magnitude via a new shared helper, `Lexer.BoxIntegerLiteralValue(long)`, which boxes the value as `int` when it fits or `long` otherwise — also reused by `StatementParser.SwitchBody`'s case-literal parsing, so boxed case values still `Equals()` correctly against boxed switch-discriminant values of the same magnitude class. **Non-obvious bug found and fixed while implementing this**: the first version of `BoxIntegerLiteralValue` was `value is >= int.MinValue and <= int.MaxValue ? (int)value : value` — a classic C# ternary gotcha where the compiler unifies both branches to a common type (`long`, via the implicit `int→long` conversion) *before* boxing, so every literal was silently boxed as `long` regardless of the check, breaking `switch`/`case` matching (`2L.Equals(2)` is `false`) and every function-call-argument type check across the whole test suite (75 failures) until one branch was explicitly cast to `(object)` first. `StatementParser.ArrayDeclaration`'s array-size parsing now uses `checked((int?)value)` (already wrapped in `.Try()`) so an out-of-range array size fails that parser alternative rather than silently wrapping. Confirmed no other production call sites depend on `IntegerLiteral`'s old `int` return type (`grep`-verified across the whole repo). Verified end-to-end: the UtopIR text generated for the original reproduction case now reads `£big_number = appoint 5000000000`, with no truncation.

**UtopIR — `duty`/`discharged` function structure not in scope for v0.0.1-preview1**: All generated UtopIR code is top-level statements. The `duty`/`discharged` function block concept from `UtopIR.md` §1 is deferred. The CIL emitter will still require its boilerplate `AssemblyBuilder`/`TypeBuilder`/`MethodBuilder("Main")` shell (a `System.Reflection.Emit` concern), but that is not a UtopIR-level instruction — it is emitter scaffolding.

**UtopIR — `yarn` (string) and `stitch` (char) deferred from CIL emitter**: `Yarn` and `Stitch` types are defined in the UtopIR AST and `UtopIRKeywords.TypeNames` for forward compatibility, but the CIL emitter will not implement them initially. In CIL, strings are managed types and use `ldstr` — no pointer concerns. A future LLVM backend would require `ptr`/`getelementptr` for string data. No AST or code generator changes will be needed when strings are eventually implemented in CIL; only new emitter logic is required.

**CIL emitter — floating-point, boolean, character, and string types not yet supported**: `CilEmitter.MapToClrType` and `EmitLoadLiteralValue` cover only the eight integer `UtopIRType` values. `Fathom`, `Foot`, `Decree`, `Stitch`, and `Yarn` throw `NotSupportedException`. Extending coverage requires: `MapToClrType` gains `double`/`float`/`bool`/`char`/`string` branches; `EmitLoadLiteralValue` gains `Ldc_R8`/`Ldc_R4` (float) and `Ldstr` (string, no boxing) cases; boolean and char already reduce to integer-like CIL representations (`bool` as `int32` `0`/`1`, `char` as `uint16`) so those two are low-effort once the `NotSupportedException` guard is lifted — string is the one requiring real design work (see the `yarn`/`stitch` gap above).

**UtopIR Parser — no grammar support for narrow/unsigned-typed literal syntax**: `UtopIR.Parser`'s `OperandParser.Literal` can only ever produce a bare integer literal boxed as `int` or `long` (no literal-type-suffix syntax exists in the grammar). A hand-written `.utopir` file that appoints such a literal directly to a narrower or unsigned-typed variable (e.g. `£x = welcome pirate` then `£x = appoint 500000`, where `pirate` is `short`) can still reach `CilEmitter` with a mismatched-width `LiteralOperand` and produce invalid IL — this is the same class of "declared type vs. literal's own CLR type" gap already fixed once for the Topsy-Turvy-source path (see `TransformDeclaration`/`TransformAssignment`'s `CastOperandIfNeeded` in the `UtopIR.Transformer` entry above), but nothing in `UtopIR.Parser` inserts an equivalent widening/narrowing `WereInstruction` for hand-authored UtopIR — the author is expected to write an explicit `were` cast themselves. Deliberately out of scope for the parser project (confirmed with the user); fixing it would mean giving the parser type-inference responsibility that the Transformer currently owns exclusively.

**CIL emitter — `Executable` output is framework-dependent and managed-only, not a native single-file binary**: `CilOutputKind.Executable` (see `BWHazel.TopsyTurvy.UtopIR.Emitters.Cil` above) produces a managed PE with a real CLR entry point plus a companion `runtimeconfig.json`. This can only be launched via `dotnet <path>` — it is not a double-clickable native `.exe` on Windows or an executable-bit Unix binary, because no native AppHost stub has been generated or patched. Producing a true self-contained single-file executable (matching what `dotnet publish --self-contained -p:PublishSingleFile=true` produces for compiled C# projects) requires binary-patching the platform-specific `apphost`/`apphost.exe` template shipped in the .NET SDK — normally done via the `Microsoft.NET.HostModel` package's `HostWriter.CreateAppHost` API, which embeds the target DLL path into the native stub and (for single-file) bundles the runtime and dependencies into one binary. This is materially more work than the managed-PE approach used today and has not been started. If this is needed, `Microsoft.NET.HostModel` would become a second external NuGet dependency in the `Emitters.Cil` project, alongside `AsmResolver.DotNet` (see the `CilGenerator.cs` file inventory entry above).

**`dotnetmd` cannot document LSP handlers and has additional rendering defects**: `dotnetmd` silently skips any class whose base type cannot be resolved from locally available XML documentation files. All Language Server handlers inherit from OmniSharp base classes (e.g. `HoverHandlerBase`, `CodeLensHandlerBase`) in NuGet packages that do not ship XML docs, so `dotnetmd` drops all handler pages without warning — only `DocumentStateManager` (base type `Object`) and other non-handler types survive. Additional defects: `<c>` inline code tags are not converted to Markdown backticks (rendered as literal `<c>text</c>` in output); `<remarks>` content is run together without line breaks. `DefaultDocumentation` (`scripts/generate-api-docs.sh`) is a working alternative: it documents all public types including handlers and processes the full XML doc set correctly. However, switching requires migrating all XML documentation comments from the project's current inline Markdown style (raw `*` bullet syntax, backtick spans, and other Markdown embedded within `<remarks>`) to standard .NET XML doc tags (`<list type="bullet"><item>`, `<c>`, `<para>`, etc.), because `DefaultDocumentation` treats XML node content as plain text rather than Markdown. Namespace title formatting in `DefaultDocumentation` output also differs from the current Docusaurus page structure and would require post-processing refinement before the generated pages are publication-ready.

**`SymbolTable` uses a flat namespace — parameters from different functions collide**: `SymbolTable` is a single `Dictionary<string, SymbolInfo>` (case-insensitive). If two functions declare a parameter with the same name (e.g. both have a parameter called `name`), only the first is kept; the second is silently dropped by the `ContainsKey` guard in `AddFunction`. Consequences: hover, go-to-definition, and rename all use the first function's definition position for any subsequently declared parameter of the same name, and Find All References aggregates occurrences from both functions under one symbol. Fix requires scoped symbol tables — a global scope for variables and functions, plus a per-function scope for parameters — and updates to all LSP handlers and web editor bridge methods that call `TryGetSymbol`.

### Deferred LSP Features

| Feature | Blocker |
|---|---|
| Inlay Hints (`textDocument/inlayHint`) | Handler not yet written; `SymbolInfo.TypeDisplayName` already carries the data. |
| Call Hierarchy | Requires a call-graph not currently built by `SymbolTable`. |
| Hierarchical `DocumentSymbol` (`textDocument/documentSymbol`) | `DocumentSymbolHandler` currently returns flat `SymbolInformation` (the older LSP format), which lists every symbol at the same level in the Outline panel. The newer `DocumentSymbol` format supports nesting — parameters under their parent function, variables under the PRINCIPALS block. Upgrading requires building a tree during `SymbolTable.Build` and returning `DocumentSymbol` instances instead of wrapping `SymbolInformation` in `SymbolInformationOrDocumentSymbol`. The flat format is used because the current `SymbolTable` has no hierarchy, and the flat namespace Known Gap (parameter name collision) would need to be resolved first. |

**Imported functions are surfaced in the visual symbol panel but flagged as undeclared in Monaco**: When `PRAY ADMIT "file.topsy"` is in the diagram, `Editor.razor.GetImportedFunctionSymbols` parses the imported file and adds its functions to the symbol panel so users can drag them onto the canvas. However, when the generated source is analysed by the LSP / type checker, the function is genuinely undeclared in the current file's symbol table — the cross-document function lookup that works in the LSP does not apply during the visual editor's local analysis pass. Fix requires either (a) building a merged symbol table that includes imported file symbols before the visual-editor analysis pass, or (b) extending `TopsyTurvyTypeChecker` to resolve cross-file symbols via the virtual file system resolver already present in `Interpreter`. This is a type-checker / analysis-layer change, not a visual-editor change.

### Deferred Web Editor Features

| Feature | Blocker |
|---|---|
| Interactive stdin (line-by-line `PRAY TELL`) | `ITopsyTurvyIO` must become async; requires `InteractiveWebIO` + XtermBlazor input handler |

---

## 5. Next Steps

1. Read `AGENTS.md` and `DEVELOPMENT.md` before starting any work.
2. Run `dotnet build operetta/BWHazel.TopsyTurvy.slnx --no-incremental` and `dotnet test` to confirm baseline.
3. **VS Code Marketplace publication**: register a publisher at `marketplace.visualstudio.com/manage`, confirm `publisher` in `package.json` matches, then `vsce publish` from the publish workflow or locally.
4. **VS Code file icon**: submit a PR to `vscode-icons` adding a `.topsy` entry — see §4 Known Gaps.
5. **LSP multi-error recovery (Option 2, deferred — see §4)**: `TryParse()` currently stops at the first syntax error. Full recovery requires a hand-written loop that records the error, advances to the next statement-opening keyword, and continues. Consider after static typing is complete.
