# DEVELOPMENT.md
## Topsy Turvy Language — Technical Reference for Coding Agents

* **Last Updated:** 2026-06-25 (Documentation — committed parse semantics. `TopsyTurvyParser.cs` class remarks: `### Committed Parse Semantics` section added (placed between `### Source Span Lifecycle` and `### Parsing Process`) documenting that `ProgramParser` uses `WsMany<T>` for committed-parse behaviour and cross-referencing `StatementParser` for block-body details; "for brevity" note updated to also reference the new section. `StatementParser.cs` class remarks: `### Committed Parse Semantics` section added (placed between `### Source Spans` and `### Main Entry Point`) documenting that all block-body parsers — conditionals, loops, switch cases, try-catch, guard, function bodies, and principal block — use `WsMany<T>`, and that parsing stops at the first unrecoverable error; "for brevity" note updated to reference both `### Source Spans` and `### Committed Parse Semantics`. `docs/topsy-turvy/docs/concepts/parser.md`: `### Committed Parse Semantics` subsection added (typo in draft heading corrected: `Commited` → `Committed`). No code or test changes.) **Previously:** 2026-06-25 (Committed parsing — correct syntax error positions. `ParserHelpers.cs`: `WsMany<T>` static method added — custom `TextParser<T[]>` combinator that separates whitespace consumption from statement parsing in a hand-written loop; partial-match detection via `r.Remainder != wsResult.Remainder` propagates errors at the actual failure position rather than resetting to before the whitespace. 12 `Ws(X).Try().Many()` call sites replaced with `WsMany(X)` across `TopsyTurvyParser.cs` (`ProgramParser.body`) and `StatementParser.cs` (all 11 block-body loops: `ConditionalBody` true/else-if/else blocks, `SwitchBody` case/default bodies, `Loop`, `TryCatch` success/catch blocks, `Guard` body, `FunctionDefinition` body, `PrincipalBlock` declarations). `TopsyTurvyParserTests.cs`: 3 new `[Fact]` tests confirm diagnostic `Span.Start.Line` is correct for top-level errors, errors inside function bodies, and errors inside conditional blocks. Test total: 734/734. `DEVELOPMENT.md` §4: LSP squiggle accuracy entry updated — Option 1 done, Option 2 deferred. `docs/topsy-turvy/docs/concepts/ast.md`: stale `:::warning` span block removed; replaced with two paragraphs documenting `SourceMap`-based span population and `PlaceholderSpan (0,0)-(0,0)` fallback. `docs/topsy-turvy/docs/concepts/parser.md`: `### Source Span Capture` subsection added.) **Previously:** 2026-06-24 (Code review and refinements. `SPEC.md`: 17 section headings renumbered (§8 through §24) to account for the inserted §6 Bitwise Operators; 4 body cross-references updated (`§6` → `§7` ×2 for String Operations refs; `§9` → `§10`; `§15` → `§16`). `Interpreter.cs`: `ApplyUnaryBitwise` private static helper added alongside `ApplyBitwise` — takes `Func<long, long>` and preserves operand type rather than widening; the 3 unary bitwise cases in `EvaluatePrefix` replaced with one-line calls to this helper, matching the style of the binary cases. `TopsyTurvyParserExpressionTests`: 4 unary-operator `[Fact]` tests (`HARDLY EVER`, `INVERSION OF`, `TRANSPOSITION UP`, `TRANSPOSITION DOWN`) consolidated into one `[Theory]` with 4 `[InlineData]` rows, mirroring the binary operator Theory. Test total unchanged: 692/692.) **Previously:** 2026-06-24 (Phase 2 — Bitwise Operators. Six new operators added across all layers: `CHORD OF` (bitwise AND, binary), `HARMONY OF` (bitwise OR, binary), `DISCORD OF` (bitwise XOR, binary), `INVERSION OF` (bitwise NOT, unary), `TRANSPOSITION UP` (left shift by 1, unary), `TRANSPOSITION DOWN` (right shift by 1, unary). All require integer operands; applying to floats, strings, characters, booleans or null is a runtime error. `Operator` enum: 6 new members (`ChordOf`, `HarmonyOf`, `DiscordOf`, `InversionOf`, `TranspositionUp`, `TranspositionDown`). `ExpressionParser.OperatorToken`: 6 new entries; `TRANSPOSITION DOWN` placed before `TRANSPOSITION UP` (longest-first). `Interpreter.EvaluatePrefix`: 3 new unary cases (`InversionOf`, `TranspositionUp`, `TranspositionDown`) — same pattern as `HardlyEver`. `Interpreter.EvaluateBinaryOperator`: 3 new cases delegating to new `ApplyBitwise` helper. `ApplyBitwise`: private static helper (same shape as `ApplyRemainder`) — `IsIntegerType` guard, `GetWidestIntegerType`, `ToLong` → operation → `CreateIntegerFromLong`. `KeywordData`: 6 new entries (122 total). `ReplHighlighter`: 6 new entries in `orange1`. Monaco tokenizer: 2 new regex rules. TextMate grammar: 2 new pattern entries. `SPEC.md`: new §6 Bitwise Operators table (sections renumbered); 6 rows added to §17 keyword reference. `GRAMMAR.ebnf`: new `BitwiseOp` production added to `FixedOp`. Test total: 692/692 (678 + 14 new: 3 `[InlineData]` in binary operator Theory, 3 unary `[Fact]`s in expression parser tests, 6 `[Fact]`s in `TopsyTurvyInterpreterOperatorTests` — 4 correctness, 2 type-guard errors).) **Previously:** 2026-06-24 (Code review and test improvements. `LiteralType.Float` renamed to `LiteralType.Double` and `TopsyTurvyValue.Float()` renamed to `TopsyTurvyValue.Double()` throughout — no integer-value references exist, so reordering is safe. `TopsyTurvyEnvironment.GetVariables()` added returning `IReadOnlyDictionary<string, TopsyTurvyValue>` for the current scope. `Interpreter.Functions` property added (`IReadOnlyDictionary<string, FunctionDefinitionNode>`). `ResolveStringCharacter` extracted as a private helper in `Interpreter` (mirrors `ResolveArrayElement`). `IntegerTypeRank` promoted from a local function inside `GetWidestIntegerType` to a named `private static int` method; `GetWidestIntegerType` is now a one-line expression body. `ApplyArithmetic` XML `<remarks>` corrected: now states that `Double` wins over `Single` (two distinct tiers), not that any floating-point result is equivalent. `LiteralTypeToDisplayName` in `SymbolTable` changed from `private static` to `public static`. `:armoury` REPL command added (alias `:env`); takes optional `<kind>` parameter (`all` | `variables` | `functions`, default `all`); prints variables table (columns: identifier, type, constant ✓/✗, value) and functions table (columns: identifier, parameters); filtered to exclude `THE PROPS`; supports both rich and tiptoe modes. `ReplHighlighter` extended to highlight character literals (`'A'`, `'~n'`, etc.) in `sandybrown`, matching string literal colour; new step 3 in the greedy scanner. `TopsyTurvyValue.cs` class-level remarks converted from grouped bullet lists to a three-column Markdown table (Topsy Turvy keyword | `LiteralType` | .NET type); code examples added to all public methods. `TopsyTurvyParserPrimitiveTypeTests` reorganised: 5 char-literal `[Fact]` tests consolidated into one `[Theory]`; 9 individual declaration facts replaced by one `[Theory]` covering all 14 types; one `[Theory]` for in-place casts covering all 14 types added; "new"/"Phase 1" language removed. Type-keyword declaration `[Theory]` (PEER/FATHOM/YARN/DECREE/NAUGHT) removed from `TopsyTurvyParserStatementTests` and consolidated into `TopsyTurvyParserPrimitiveTypeTests`. Four YARN string indexing tests added to `TopsyTurvyInterpreterPrimitiveTypeTests`: last-character boundary, zero index error, non-integer index error, empty-YARN length. Test total: 678/678.) **Previously:** 2026-06-24 (Phase 1 — Extended Primitive Types. Five new scalar type keywords: `CHANCELLOR` (Int64/`long`), `PIRATE` (Int16/`short`), `SAUSAGE-ROLL` (Int8/`sbyte`), `FOOT` (Single/`float`), `STITCH` (Char/`char`). `STANDING` unsigned modifier for all four integer types (`STANDING PEER` → `uint`, `STANDING CHANCELLOR` → `ulong`, `STANDING PIRATE` → `ushort`, `STANDING SAUSAGE-ROLL` → `byte`). Single-quoted character literals (`'A'`, `~n`, `~t`, `~'`, `~~`). `RECKONING OF` and `VICTIM` extended to work on `YARN` strings (no parser changes — runtime dispatches on `LiteralType.String` before array path). `VICTIM` assignment on `YARN` is a runtime error (immutable). Arithmetic widening rules: `FATHOM` wins over all; `FOOT` wins over all integers; integer-family picks widest operand per rank `UnsignedLong > Long > UnsignedInteger > Integer > UnsignedShort > Short > Byte > SignedByte`. `AreEqual` now uses `Equals(RawValue, RawValue)` as default arm (correct value equality for all new boxed value types). `CaseMatches` extended with `char` arm. `LiteralType` enum: 6 new members (Long, Short, SignedByte, UnsignedInteger, UnsignedLong, UnsignedShort, Byte, Single, Char — 9 total). `Keywords.TypeNames`: 6 new constants. `KeywordData`: 6 new entries (96 total). `TopsyTurvyValue`: 9 new factory methods; updated `IsTruthy`, `CastTo`, `ToString`; added private helpers `ToInt64`, `ToUInt64`, `ToDouble`, `ToChar`. `Interpreter`: new helpers `IsIntegerFamily`, `ToLong`, `GetWidestIntegerType`, `CreateIntegerFromLong`; `IsNumeric` and `ToDouble` extended; `ApplyArithmetic` signature changed `Func<int,int,int>` → `Func<long,long,long>`; `ApplyQuotient`/`ApplyRemainder` extended for all integer types. Syntax highlighting updated in all three layers (REPL, Monaco, TextMate). New test files: `TopsyTurvyParserPrimitiveTypeTests.cs`, `TopsyTurvyInterpreterPrimitiveTypeTests.cs`. Test total: 659/659. `SPEC.md` updated to v0.5.0; `GRAMMAR.ebnf` updated to v0.4.0.) **Previously:** 2026-06-24 (Documentation and highlighting polish. **XML doc class-level sections**: `ExpressionParser.cs` gained `### Non-Ternary Expressions` (explains why `NonTernaryExpression` exists — left-recursion prevention and `OTHERWISE,` ambiguity — and notes that recursive sub-expressions within its alternatives still use the full `Expression`) and `### Ternary Expressions` (documents the five-step parse, `.Try()` backtracking, right-chaining, and code examples); both placed between `### Cast Expressions` and `### Expression Parser`. `StatementParser.cs` gained `### Guard Clauses` (documents `YEOMAN … OTHERWISE, … UNDER ORDERS.`, the truthy fall-through semantics, and an example) and `### Assert Statements` (documents `THE LAW IS … THAT …`, the no-effect/throw semantics, equivalence with `A HIDEOUS CURSE ON`, and two examples); placed between `### Loops`/`### Exception Handling` and `### Exception Handling`/`### Functions` respectively. **`GuardNode.cs` example simplified**: changed from a two-condition `BOTH PRE-ADAMITE … AND LOWER DEGREE …` expression to a single `PRE-ADAMITE score AND 0` condition, with the AST representation updated accordingly. **`THAT` syntax highlighting**: the assert separator `THAT` is now highlighted in all three layers — REPL highlighter (`deepskyblue1`, longest-match sort ensures all multi-word `THAT` keywords take priority), Monaco tokenizer (`[/\bTHAT\b/, 'keyword']` placed after all multi-word `THAT` rules so they win first-match), and TextMate grammar (`(?i)\\bTHAT\\b` in the `exceptions` repository after `THAT CONCLUDES THE MATTER.` so the more specific pattern wins on same-position ties). `THAT` remains absent from `KeywordData` (no LSP completion). **`SPEC.md` keyword reference table**: four new rows added — `YEOMAN <condition>` (guard clause start), `UNDER ORDERS.` (end guard clause), `THE LAW IS <condition> THAT <error-message>` (assert statement), `THAT` (assert separator, not in keyword completion list); guard keywords placed after `THE TERM EXPIRES.`, assert keywords placed after `THAT CONCLUDES THE MATTER.`. No functional code, parser, runtime, AST, or test changes in this session.) **Previously:** 2026-06-23 (Three new language features — Ternary Expression, Guard Clause, Assert Statement — implemented across all layers: SPEC, GRAMMAR, AST, Parser, Runtime, Analysis, LSP, Monaco, TextMate, REPL Highlighter, and tests. **Ternary Expression** (`TrueValue SHOULD IT TRANSPIRE THAT Condition OTHERWISE, FalseValue`): inline conditional expression; reuses existing `SHOULD IT TRANSPIRE THAT` and `OTHERWISE,` keywords; right-chainable in the false-value position; `NonTernaryExpression` introduced in `ExpressionParser` to avoid left-recursion and prevent `OTHERWISE,` being consumed in the condition position. **Guard Clause** (`YEOMAN Condition OTHERWISE, Block UNDER ORDERS.`): executes block only when condition is falsy; `YEOMAN` → `PostIncrease2Keywords`, `UNDER ORDERS.` → `PreDecrease2Keywords` (matching the `IN WHICH CAPACITY?` / `NOTHING COULD BE MORE SATISFACTORY.` pattern); both keywords added to `KeywordData` (89 entries). **Assert Statement** (`THE LAW IS Condition THAT ErrorMessage`): runtime invariant check; throws via the same `TopsyTurvyThrowException` mechanism as `A HIDEOUS CURSE ON`; `THE LAW IS` added to `KeywordData` (90 entries); `THAT` is a structural separator and intentionally NOT added to `KeywordData`. Test totals: 628/628 unit, 78/78 E2E. New AST files: `TernaryExpressionNode.cs`, `GuardNode.cs`, `AssertNode.cs`. New test files: `TopsyTurvyParserTernaryTests.cs`, `TopsyTurvyInterpreterTernaryTests.cs`, `TopsyTurvyParserGuardTests.cs`, `TopsyTurvyInterpreterGuardTests.cs`, `TopsyTurvyParserAssertTests.cs`, `TopsyTurvyInterpreterAssertTests.cs`. `SPEC.md` remains at v0.4.0; `GRAMMAR.ebnf` remains at v0.3.0.
* **Specification Version:** 0.5.0 — `SPEC.md` is the grammar source of truth
* **File Extension:** `.topsy`
* **Baseline:** `dotnet build interpreter/BWHazel.TopsyTurvy.slnx --no-incremental` — 0 errors, 0 warnings. `dotnet test interpreter/BWHazel.TopsyTurvy.Tests` — 734/734. `dotnet test interpreter/BWHazel.TopsyTurvy.Cli.E2ETests` — 78/78. `dotnet test interpreter/BWHazel.TopsyTurvy.WebEditor.E2ETests` — 6/6 Playwright. `npm run test:grammar` — 4 files/all passing. `npm run test:unit` — 4/4.

| Component | Status |
|---|---|
| AST (`BWHazel.TopsyTurvy.Ast`) | Complete |
| Parser (`BWHazel.TopsyTurvy.Parser`) | Complete |
| Runtime (`BWHazel.TopsyTurvy.Runtime`) | Complete |
| Analysis (`BWHazel.TopsyTurvy.Analysis`) | Complete |
| Integration — all 4 examples pass | Complete |
| LSP server + VS Code extension | Complete |
| CLI (`operetta`) | Complete |
| Web editor (`BWHazel.TopsyTurvy.WebEditor`) | Complete (Blazor WASM) |

---

## 1. File Inventory

| File / Directory | Role |
|---|---|
| `AGENTS.md` | Project rules and constraints — read before starting any work. |
| `.actrc` | Portable `act` configuration for local workflow testing. |
| `scripts/act.sh` | Wrapper for `act` — detects host architecture and Podman socket, then delegates all arguments to `act`. Run from repo root: `scripts/act.sh push`. |
| `.github/workflows/build-test.yml` | Reusable CI workflow — triggered on `push` and `workflow_call`. Two parallel jobs: `language-toolchain` (full .NET build + unit/CLI E2E/WebEditor E2E tests) and `vscode-extension` (CLI build + extension compile, grammar, unit, and integration tests via `coactions/setup-xvfb`). VS Code integration tests require `libgtk-3-0` and related libraries installed via `apt-get`. |
| `.github/workflows/publish.yml` | Manual publish workflow — calls `build-test.yml`, then cross-compiles for `win-x64`, `linux-x64`, `linux-arm64`, `osx-x64`, `osx-arm64`, producing CLI `.tar.gz` and `.vsix` artifacts per platform. |
| `SPEC.md` | Authoritative language specification v0.5.0. |
| `GRAMMAR.ebnf` | Formal EBNF grammar v0.4.0 — blueprint for the parser. |
| `examples/` | `hello_world.topsy`, `fizzbuzz.topsy`, `fibonacci.topsy`, `pirates_calculator.topsy` — all execute correctly. `examples/language/arrays.topsy` demonstrates array declaration, element access, element assignment, truthiness, and WHILST-loop traversal. |
| `interpreter/BWHazel.TopsyTurvy.slnx` | Solution file. |
| `interpreter/BWHazel.TopsyTurvy.Ast/` | AST node types. All inherit from `Node` (`required SourceSpan Span`). Also defines `Diagnostic`, `DiagnosticCollection`, `SourceSpan`, `SourceLocation`. `TryCatchNode` carries optional `CaughtValueName` (null = no binding) for `MODIFIED RAPTURE, Name` syntax. `InPlaceCastNode` extends `Statement` directly (no shared `TypeCastNode` base). `ExpressionCastNode` extends `Expression` — the non-mutating cast is an expression, not a statement. `TypeCastNode` has been removed. `ArrayDeclarationNode` — `PRAY WELCOME arr AS A [CONSERVATIVE] LITTLE LIST OF [<size>] <type> [BEING ...]`; carries `int? Size` (pre-allocation count, null = no size). `ArrayIndexNode` extends `Expression` — `VICTIM <index> ON <arr>`. `ArrayElementAssignmentNode` extends `Statement` — `VICTIM <index> ON <arr> IS APPOINTED <value>`. `ArrayLengthNode` extends `Expression` — `RECKONING OF <arr>`; carries only `string ArrayName`. `TernaryExpressionNode` extends `Expression` — `TrueValue SHOULD IT TRANSPIRE THAT Condition OTHERWISE, FalseValue`; carries `TrueValue`, `Condition`, `FalseValue` (all `Expression`). `GuardNode` extends `Statement` — `YEOMAN Condition OTHERWISE, Block UNDER ORDERS.`; carries `Condition: Expression` and `ElseBlock: IReadOnlyList<Statement>`. `AssertNode` extends `Statement` — `THE LAW IS Condition THAT ErrorMessage`; carries `Condition: Expression` and `ErrorMessage: Expression`. `FunctionDefinitionNode` carries a parallel `ParameterSpans: IReadOnlyList<SourceSpan>` alongside `Parameters: IReadOnlyList<string>` — both lists have the same length and are populated together by the parser; `ParameterSpans[i]` covers the identifier token of `Parameters[i]` in the original source. `GenerateDocumentationFile` is enabled; XML documentation comments are present on all types. |
| `interpreter/BWHazel.TopsyTurvy.Analysis/` | Shared analysis library. `SymbolTable`, `SymbolInfo` (now carries `IsConstant` and `Documentation`), `SymbolKind`, `DocumentState`, `DepthAction`. `SourceFormatter` — keyword normalisation + libretto indentation. `SourceAnalyser` — `FindSkipRanges`, `BuildLineOffsets`, `IsInSkipRange`, `FindWordOccurrences`, `CountOccurrences`. `KeywordData` — 96-entry keyword list (authoritative source of truth for LSP/completion — must not be duplicated in `Keywords.cs`; now includes `CONSERVATIVE`, `LIBERAL`, 8 documentation comment tag keywords, `YEOMAN`, `UNDER ORDERS.`, `THE LAW IS`, `CHANCELLOR`, `PIRATE`, `SAUSAGE-ROLL`, `STANDING`, `FOOT`, `STITCH`). `HoverMarkdownBuilder` — Markdown hover strings; shows `(constant)` for `CONSERVATIVE` variables; now also renders documentation comment sections (summary, remarks, parameters, return, exceptions, examples, see-also, deprecation). `DocumentationComment` — record holding parsed documentation comment data. `DocumentationCommentParser` — public static class that parses block comment content into a `DocumentationComment`. `DocumentationCommentParseState` — public sealed class holding all mutable state accumulated during a single `Parse` pass; lives in its own file. `LiteralTypeToDisplayName` is `public static` (used by `ReplSession.WriteArmoury` to display type names in the `:armoury` table). References: Ast, Parser. Referenced by: LanguageServer, Cli, WebEditor. `GenerateDocumentationFile` is enabled; XML documentation **complete** for all types. |
| `interpreter/BWHazel.TopsyTurvy.Parser/` | Superpower parser. Entry: `TopsyTurvyParser`. Pre-processors: `CommentsPreProcessor`, `VictorianFlourishPreProcessor`. `SourceMap`/`SourceMapping` for offset tracking (`SourceMapping.TransformedOffset` maps a position in the pre-processed text to its original line/column). Pre-processor output carried in `PreProcessResult(TransformedText, SourceMap)`. `TryParse` returns `ParseResult(ProgramNode?, IReadOnlyList<Diagnostic>)`. `TypeKeyword` lives in `ExpressionParser` (moved from `StatementParser` to avoid a circular initialisation dependency). `ExpressionParser.ExpressionCast` is wired into both `Expression` alternatives and `ExpressionStatementParser`. `StatementParser.TypeCast` now only handles in-place casts. `StatementParser.ArrayDeclaration` parses `PRAY WELCOME arr AS A [CONSERVATIVE] LITTLE LIST OF [<size>] <type> [BEING ...]` — after `AS A` is consumed (4 chars including trailing `A`), the remaining token is `LITTLE LIST OF` not `A LITTLE LIST OF` (see §2); the optional size is parsed with `Ws(Lexer.IntegerLiteral).Select(v => (int?)v).Try().OptionalOrDefault(null)` between the `LITTLE LIST OF` keyword and the type keyword. `StatementParser.ArrayElementAssignment` parses `VICTIM <index> ON <arr> IS APPOINTED <value>`. `ExpressionParser.ArrayIndexExpression` parses `VICTIM <index> ON <arr>`. `ExpressionParser.ArrayLengthExpression` parses `RECKONING OF <arr>` — wired into both `ExpressionParser.Expression` and `StatementParser.ExpressionStatementParser` (see §2). `PrincipalBlock` accepts `Ws(ArrayDeclaration.Or(Declaration)).Try().Many()` to handle mixed declaration lists. All AST nodes produced by the parser carry real source spans: `TopsyTurvyParser` sets `ParserHelpers.ActiveSourceMap` (thread-local) to the `SourceMap` from the pre-processor before calling any combinator, and clears it in a `finally` block; every LINQ parser chain captures `startOffset` and `endOffset` via `ParserHelpers.CurrentOffset` and calls `ParserHelpers.BuildSpan` at node construction time. `FunctionDefinition` additionally captures per-parameter offsets to populate `FunctionDefinitionNode.ParameterSpans`. `GenerateDocumentationFile` is enabled; XML documentation **complete** for all types. |
| `interpreter/BWHazel.TopsyTurvy.Parser/ParserHelpers.cs` | `internal static class` providing shared helpers used across `StatementParser`, `ExpressionParser`, and `TopsyTurvyParser`: `PlaceholderSpan` (the `(0,0)-(0,0)` fallback used in isolated unit tests where no `SourceMap` is active); `ActiveSourceMap` (`[ThreadStatic]` field set by `TopsyTurvyParser` before each parse and cleared in a `finally` block — the communication channel between the entry-point and the static parser combinators); `CurrentOffset` (a zero-consuming parser that returns the current absolute cursor offset); `BuildSpan(startOffset, endOffset)` (converts two pre-processed offsets to a `SourceSpan` via `ActiveSourceMap`, or falls back to `PlaceholderSpan`); `WithOffsets<T>(parser)` (wraps any parser to also return start/end offsets, used for branched `.Select()` chains where inserting `from` clauses would be unwieldy); and `Ws<T>(parser)` (wraps a parser with a required-whitespace prefix). All parser files import it via `using static`. |
| `interpreter/BWHazel.TopsyTurvy.Parser/ConditionalBodyInfo.cs` | `public record` carrying the parsed body of a conditional statement — `TrueBlock`, `ElseIfs`, and `ElseBlock`. Replaces the anonymous tuple previously returned by the `ConditionalBody` parser, following the pattern established by `LoopDefinition`. |
| `interpreter/BWHazel.TopsyTurvy.Parser/SwitchBodyInfo.cs` | `public record` carrying the parsed body of a switch statement — `Cases` and `DefaultBlock`. Replaces the anonymous tuple previously returned by the `SwitchBody` parser. |
| `interpreter/BWHazel.TopsyTurvy.Runtime/` | Tree-walking interpreter. Entry: `Interpreter`. Key types: `TopsyTurvyValue` (now supports `LiteralType.Array` backed by `List<TopsyTurvyValue>`; `Array(List<TopsyTurvyValue>)` factory; `IsTruthy()` returns `true` for non-empty arrays; `CastTo()` allows cast only to `YARN` or `DECREE`; `ToString()` renders `[elem1, elem2, ...]`; factory method `Double()` — previously `Float()`), `TopsyTurvyEnvironment` (tracks constants via `HashSet<string>`; `Declare` accepts optional `isConstant`; `Assign` enforces the constant constraint; `IsConstant(name)` query; `IsConstantInChain(name)` walks enclosing scope chain — required for array element mutation which bypasses `Assign`; `GetVariables()` returns `IReadOnlyDictionary<string, TopsyTurvyValue>` for the current scope), `ITopsyTurvyIO`/`ConsoleIO`. `Interpreter` exposes `Functions` (`IReadOnlyDictionary<string, FunctionDefinitionNode>`) for external inspection (used by `:armoury`). Private helpers: `ResolveStringCharacter` (mirrors `ResolveArrayElement` for YARN indexing); `IntegerTypeRank` (named `private static int` method, extracted from a local function in `GetWidestIntegerType`). Signal exceptions: `BreakSignalException`, `ContinueSignalException`, `ReturnSignalException`, `TopsyTurvyThrowException`. `Execute(program, cancellationToken, options?)` — pass `InterpreterExecutionOptions.ExecutionTimeout` for Blazor WASM timeout (see §2). `ExecuteTryCatch` creates a child scope via `CreateNested()` when `CaughtValueName` is set. `GenerateDocumentationFile` is enabled; XML documentation **complete** for all types. |
| `interpreter/BWHazel.TopsyTurvy.Runtime/InterpreterExecutionOptions.cs` | Positional record consolidating the optional `Execute` parameters: `ExecutionTimeout` (`TimeSpan?`), `SourceFilePath` (`string?`), `SourceFileResolver` (`Func<string, string?>?`), `ProgrammeArguments` (`IReadOnlyList<string>?`, default `null`). Pass `null` for any unused option. |
| `interpreter/BWHazel.TopsyTurvy.Tests/` | xUnit suite — 731/731 passing. Five subdirectories: `Analysis/`, `Cli/`, `Parser/`, `Runtime/`, `LanguageServer/`; shared base classes in each. `Superpower.Model` types available transitively — no direct package reference needed. Key test files: `Analysis/DocumentationCommentParserTests.cs` (23), `Cli/ReplHighlighterTests.cs` (20), `Parser/TopsyTurvyParser*Tests.cs` (array, ternary, guard, assert, primitive types, span coverage, and others), `Runtime/TopsyTurvyInterpreter*Tests.cs` (array, ternary, guard, assert, primitive types, and others), `LanguageServer/*Tests.cs`. See AGENTS.md for test conventions. |
| `interpreter/BWHazel.TopsyTurvy.Cli/` | CLI project. Binary: `operetta`. Version: `0.1.0`. Dependencies: `Spectre.Console 0.55.2`, `System.CommandLine 2.0.8`, `OmniSharp.Extensions.LanguageServer 0.19.9`. References `BWHazel.TopsyTurvy.LanguageServer` (class library). Published as a single self-contained executable. |
| `interpreter/BWHazel.TopsyTurvy.Cli/ProgramExecutionResult.cs` | Discriminated result: `Success()`, `Failure(msg)`, `SyntaxError(errors)`, `RuntimeError(diagnostics)`. |
| `interpreter/BWHazel.TopsyTurvy.Cli/ProgramRunner.cs` | `Run`, `Check`, `ParseFile` — all accept optional `IFileSystem? fileSystem = null`. `ParseFile` returns `(ProgramExecutionResult, ParseResult?)`. |
| `interpreter/BWHazel.TopsyTurvy.Cli/PanelHelper.cs` | All Spectre.Console panel construction. `WriteDefault`, `WriteSuccess`, `WriteUserError`, `WriteSyntaxErrors`, `WriteRuntimeErrors`, `WriteVersionInfo`, `ReportErrors`. |
| `interpreter/BWHazel.TopsyTurvy.Cli/NodeJsonConverter.cs` | `JsonConverter<Node>` for polymorphic AST serialisation with `$type` discriminators. |
| `interpreter/BWHazel.TopsyTurvy.Cli/FileManager.cs` | Shared file-creation and IO utilities. `BuildFileContent(title, subtitle?)` builds scaffold source. Three IO methods accepting `IFileSystem? fileSystem = null`: `TryReadSource`, `TryCreateProgrammeFile`, `TryMountProject`. |
| `interpreter/BWHazel.TopsyTurvy.Cli/CommandBuilders/` | One builder per command — see §3. All scaffold content goes through `FileManager.BuildFileContent`. Includes `IncantationCommandBuilder` — starts the OmniSharp LSP server over stdio; `--stdio` accepted silently for vscode-languageclient compatibility (see §2). `CadenzaCommandBuilder` starts the interactive REPL. |
| `interpreter/BWHazel.TopsyTurvy.Cli/Repl/` | Six types supporting the `cadenza` REPL. `ReplSession` — orchestrates the REPL loop; owns one `TopsyTurvyEnvironment` and one `Interpreter` instance shared across all turns; wraps each input as `HARK! "Cadenza"\n{input}\nFINALE.`; detects `Console.IsInputRedirected` for non-interactive mode; implements `WriteArmoury` for the `:armoury`/`:env` command (prints variables and functions tables; excludes `THE PROPS`; supports rich and tiptoe modes). `ReplIO` — `ITopsyTurvyIO` that buffers `BEHOLD` output lines for panel display; pass-through writes (`suppressNewline: true`) go directly to `Console.Write`. `ReplInputReader` — raw key-reading loop (`Console.ReadKey(intercept: true)`) with styled prompts, backspace, cursor navigation, ↑/↓ history, Ctrl+C/D/L control key commands (`GetControlKeyCommand`), block-continuation accumulation, and live syntax highlighting via `ReplHighlighter`; the "keeping the cursor in sync" invariant ensures `cursorPosition` always equals the terminal column at loop entry. `ReplHighlighter` — static class; `Highlight(string) → string` tokenises a single line with a 7-step greedy left-to-right scanner (comment → string → **character literal** → boolean → number → keyword longest-first → plain) and wraps tokens in Spectre.Console markup; character literals use `sandybrown`, matching string literals. `ReplPromptKind` — enum (`SingleLine`, `MultiLine`, `Continuation`) used by `ReplInputReader.ShowPrompt`. `ReplConstants` — `internal static` class defining all REPL command strings and aliases (includes `:armoury` and `:env`). |
| `interpreter/BWHazel.TopsyTurvy.Cli.E2ETests/` | Black-box CLI E2E tests — 73/73 passing. `CliFixture` locates the binary from the Debug build output and creates a temp directory prefixed `topsyturvy-cli-e2e-` per run. `CliTestBase` creates a per-test subdirectory and exposes `RunAsync(args)` (10-second timeout, redirected stdout + stderr) and `RunWithStdinAsync(args, stdin)` (same but sets `RedirectStandardInput = true` for interactive command testing). Eight test classes: `CommissionCommandTests` (11), `MountCommandTests` (11), `RehearseCommandTests` (6), `PerformCommandTests` (11), `PanelOutputTests` (13), `SorcererCueCommandTests` (5), `PlaybillCommandTests` (6), `CadenzaCommandTests` (10). |
| `interpreter/BWHazel.TopsyTurvy.LanguageServer/` | OmniSharp LSP **class library** — see §3. Started by the CLI via `operetta sorcerer incantation`. `LspUtilities` — shared `GetSymbolDefinitionLocation` (builds an LSP `Location` for a symbol, converting 1-based to 0-based coordinates) and `MapSymbolKind` (maps `Analysis.SymbolKind` to LSP `SymbolKind`). `DocumentStateManager` — singleton; exposes `FindSymbolInOtherDocuments`, `FindSymbolWithUriInOtherDocuments`, and `GetImportedFunctionSymbols` for cross-document lookups. |
| `extensions/vscode/topsy-turvy/` | VS Code extension: LSP client, TextMate grammar, run/stop commands, panel and file icons. LSP server launched as `operetta sorcerer incantation`. CLI binary bundled under `bin/` via `scripts/publish-cli.mjs`. `src/paths.ts` exports `resolveCliPath` — defaults to bundled binary when present, falls back to dev Debug layout. Tests: grammar (4), unit (4), integration (9). |
| `interpreter/BWHazel.TopsyTurvy.WebEditor/` | Blazor WASM standalone web editor. Toolbar: New Project, New File, Open, Save (left); Run (centre); Clear Output, Download Output, Light/Dark toggle, G&S Labels switch (right). BlazorMonaco editor with Monarch tokenizer, inline diagnostics, hover, completion, and go-to-definition. XtermBlazor output terminal. Three `[JSInvokable]` bridge methods: `GetHoverMarkdown`, `GetSymbolCompletions`, `GetDefinitionLocation` / `SwitchToFileForDefinitionAsync`. Per-file symbol state keyed by filename in `Dictionary<string, SymbolTable>`. See §2 for constraints. |
| `interpreter/BWHazel.TopsyTurvy.WebEditor/IO/BufferedWebIO.cs` | `ITopsyTurvyIO` for Blazor WASM — stdin from `Queue<string>`, output lines to `List<string>`. |
| `interpreter/BWHazel.TopsyTurvy.WebEditor/IO/VirtualFile.cs` | Tracks a filename and content in the in-memory virtual file system used for tab management and ZIP export. Carries `[JsonConstructor]` on its constructor and `[JsonPropertyName("name"/"content")]` on its properties so it can be serialised/deserialised directly via Blazor JS interop without a separate DTO. `Name` has no setter — `[JsonConstructor]` is required for `System.Text.Json` to deserialise into it. |
| `interpreter/BWHazel.TopsyTurvy.WebEditor/IO/EditorPreferences.cs` | Internal DTO used to read/write the `topsy-turvy-editor` Local Storage key via Blazor JS interop. All properties are nullable to distinguish "not yet saved" from "saved as false/empty". Properties: `List<VirtualFile>? Files` (`"files"`), `string? ActiveFile` (`"activeFile"`), `string? CommandLineArguments` (`"commandLineArguments"`), `string? StandardInput` (`"standardInput"`), `bool? AreGsLabelsEnabled` (`"areGsLabelsEnabled"`), `bool? IsDarkMode` (`"isDarkMode"`), `string? TerminalOutput` (`"terminalOutput"`). |
| `interpreter/BWHazel.TopsyTurvy.WebEditor/Dialogs/NewFileDialog.razor` | `MudDialog` for entering a new file name; appends `.topsy` automatically. |
| `interpreter/BWHazel.TopsyTurvy.WebEditor/Dialogs/OpenFilesDialog.razor` | `MudDialog` for opening multiple `.topsy` files via drag-and-drop or browse. |
| `interpreter/BWHazel.TopsyTurvy.WebEditor/wwwroot/js/topsy-turvy-language.js` | Monaco Monarch tokenizer, keyword completion provider, and language configuration. Defines `window.topsyTurvy`. Extended by `web-editor.js`. |
| `interpreter/BWHazel.TopsyTurvy.WebEditor/wwwroot/js/web-editor.js` | Monaco/xterm JS interop — hover, completion, definition providers, terminal fit, download utilities, semantic token provider (`registerSemanticTokensProvider`, `setSemanticTokens`, `enableSemanticHighlighting`), pane drag (`initPaneDrag`), localStorage persistence (`loadPreferences`, `savePreferences`), terminal theme updates (`setTerminalTheme`). Extends `window.topsyTurvy` via `Object.assign`. All nested functions in `initPaneDrag` use `const name = () =>` arrow syntax; drag state is consolidated into a single `dragConfig` object. |
| `interpreter/BWHazel.TopsyTurvy.WebEditor.E2ETests/` | Playwright xUnit E2E tests — 6/6 passing. Three test classes: `EditorExecutionTests`, `EditorDiagnosticsTests`, `EditorToolbarTests`. `WebEditorAppFixture` starts the Blazor app and launches headless Chromium. |
| `extensions/vscode/topsy-turvy/scripts/publish-cli.mjs` | Node.js ESM script that cross-compiles the CLI for the current (or `DOTNET_RID`-specified) platform into `bin/`. |
| `docs/topsy-turvy/` | Docusaurus v3 documentation site. `npm run start` serves locally; `npm run build` produces `build/`. `npm run api:generate` (`api:clean` + `dotnetmd --config dotnet-md.json`) generates Markdown API reference from XML comments into `docs/api/dotnet/`. Requires `DOTNET_ROLL_FORWARD=LatestMajor` (set in the npm script) because `dotnetmd` targets .NET 8. `docs/guide/`, `docs/concepts/`, and `docs/tutorials/` each contain only a placeholder `index.md` ("Coming Soon") — no content pages yet. `docs/api/` holds the top-level API index. Generated output lands in `docs/api/dotnet/`; `docs/api/dotnet/index.md` is a placeholder with `unlisted: true` to prevent a build error when generated content is absent. Do not delete this placeholder. Sidebar convention: each section uses `link: {type: 'doc', id: 'section/index'}` to make the category label clickable; `items: []` while the section has no content pages, switching to `{type: 'autogenerated', dirName: 'section'}` once pages are added (Docusaurus excludes `index.md` from autogenerated child items automatically). |
| `.claude/commands/implement-language-feature.md` | Project-level Claude Code skill — invoked as `/implement-language-feature`. Guides full-stack implementation of a new language feature across all layers. |

---

## 2. Non-Obvious Constraints

A new session must know these before touching the relevant code.

**Documentation comment association rule**: `SymbolTable.FindDocumentationComment` scans backwards from the symbol's definition line to find an `END OF ASIDE.)` marker on the first non-blank line above the declaration. Blank lines between the block and the declaration are allowed; any non-blank, non-comment line breaks the association. Concretely: a doc comment inside a `PRINCIPALS` block must appear immediately before the `PRAY WELCOME` line it documents, not before the `PRINCIPALS` keyword itself.

**Documentation comment keywords in `KeywordData` (90 total)**: the 8 documentation tag keywords (`LEGEND:`, `RECITATIVE:`, `ARTICLE`, `CONSEQUENCE`, `CURSES`, `CHORUS:`, `ENSEMBLE:`, `STATUTORY:`) appear in the completion list but have no parser or runtime representation — they exist solely within `(ASIDE, AT SOME LENGTH: ... END OF ASIDE.)` blocks and are stripped by the `CommentsPreProcessor`. Do not add them to `Keywords.cs` or any parser combinator.

**`STATUTORY:` emits the `deprecated` semantic token modifier**: `SemanticTokensHandler` checks `symbol.Documentation?.IsDeprecated` and adds `"deprecated"` to the modifier list. The `Legend` now declares both `"readonly"` and `"deprecated"` modifiers. In VS Code, the `deprecated` modifier maps to strikethrough rendering via the `semanticTokenModifiers` contribution in `package.json` — add a `"deprecated"` entry there if the VS Code extension is updated to advertise the modifier.

**Web editor — semantic token provider uses a push model**: After each successful analysis pass, `ComputeSemanticTokenData` in `Editor.razor` encodes all symbol occurrences as a flat `int[]` (5 ints per token in Monaco's delta encoding: `deltaLine`, `deltaStartChar`, `length`, `tokenType`, `tokenModifiersBitmask`) and pushes it to JS via `topsyTurvy.setSemanticTokens(data)`. JS stores the array in `_semanticTokensData` and fires `_semanticTokensListeners`, causing Monaco to immediately call `provideDocumentSemanticTokens`, which returns the cached data as a `Uint32Array`. Token type indices: `variable`=0, `parameter`=1, `function`=2. Modifier bitmask: `readonly`=1, `deprecated`=2. Symbols whose names contain a space (`JUST SO`, `THE PROPS`) are skipped by the same guard used in `SemanticTokensHandler`.

**Web editor — semantic token scope hierarchy must use `variable.*`**: Monaco's `getTokenStyleMetadata` builds the TextMate lookup scope as `[tokenTypeName].concat(modifierNames).join('.')`. The built-in `vs-dark` TextMate trie has entries for `variable` and `variable.parameter` but not for bare `function`. Using `['variable', 'parameter', 'function']` as legend type names results in `function` tokens receiving no colour. All three legend type names therefore use the `variable.*` namespace (`variable`, `variable.parameter`, `variable.function`) so the trie resolves them correctly. The C# encoding does not change — it only emits integer indices.

**Web editor — semantic token theme rules must live in `enableSemanticHighlighting`**: `topsy-turvy-language.js` (Monarch tokenizer + base theme skeleton) can be served from the browser's HTTP cache between releases. `web-editor.js` is reliably fresh. `enableSemanticHighlighting` calls `monaco.editor.defineTheme` for both `topsy-turvy-dark` and `topsy-turvy-light` before enabling semantic highlighting. Redefining an already-registered Monaco theme also invalidates its internal `_tokenTheme` cache (Monaco calls `notifyBaseUpdated()`, which sets `_tokenTheme = null`), so the colours rebuild immediately from the new rules. `registerThemes` in `topsy-turvy-language.js` defines both themes with `rules: []`; the semantic token rules in `enableSemanticHighlighting` are always applied last and win.

**Ternary expression left-recursion guard — `NonTernaryExpression`**: `ExpressionParser` contains a `NonTernaryExpression` static field that is identical to `Expression` minus the `TernaryExpression` alternative. Both `TrueValue` and `Condition` in the ternary parser use `NonTernaryExpression`, not `Expression`. This prevents left-recursion (if `TrueValue` could itself be a ternary, the `TernaryExpression` parser would loop on itself) and prevents `OTHERWISE,` being consumed ambiguously mid-parse (a ternary in the condition position could consume `OTHERWISE,` before the outer ternary sees it). Only `FalseValue` uses full `Expression` via `Parse.Ref(() => Expression!)`, enabling right-chaining. Do not add `TernaryExpression` to `ExpressionStatementParser` — a bare ternary as a top-level statement would conflict with the `Conditional` statement parser, which also starts with `SHOULD IT TRANSPIRE THAT`.

**Guard clause formatter depth — `PostIncrease2` / `PreDecrease2`**: `YEOMAN` is in `SourceFormatter.PostIncrease2Keywords` (depth +2 after the line) and `UNDER ORDERS.` is in `PreDecrease2Keywords` (depth -2 before the line). This produces the same two-level indentation pattern as `IN WHICH CAPACITY?` / `NOTHING COULD BE MORE SATISFACTORY.`. `OTHERWISE,` inside a guard is already a `MidBlockKeyword` (depth -1 before, +1 after), matching its behaviour in `Conditional`.

**Assert `THAT` keyword is NOT in `KeywordData` but IS highlighted**: `THAT` is a structural separator within the `Assert` parser (`THE LAW IS <condition> THAT <errorMessage>`). It is intentionally absent from `KeywordData.Keywords` to avoid polluting the LSP completion list with an ambiguous fragment that also appears in `THAT WILL DO.` and `THAT CONCLUDES THE MATTER.`. The `Assert` parser matches it via `Lexer.Keyword("THAT")` inline — the keyword is only reachable after `THE LAW IS` has already matched. `THAT` is not added to `CanonicalKeywords` in `SourceFormatter`. It IS highlighted for syntax colouring in all three layers (REPL, Monaco, TextMate); in each case, all multi-word `THAT`-containing keywords are tried before the bare `THAT` rule (longest-match in REPL, rule order in Monaco and TextMate), so those longer keywords always win.

**`Keywords` class scope**: `Keywords.cs` in `BWHazel.TopsyTurvy.Ast` covers only strings used in code logic across multiple projects — literals (`VERITY`, `NAY`, `NAUGHT`), type names (`PEER`, `CHANCELLOR`, `PIRATE`, `SAUSAGE-ROLL`, `STANDING`, `FATHOM`, `FOOT`, `YARN`, `STITCH`, `DECREE`), and the implicit variable (`JUST SO`). Single-site parser grammar strings such as `Lexer.Keyword("BEHOLD")` are intentionally left inline. `KeywordData.Keywords` in Analysis remains the authoritative 96-entry list for LSP/completion and must not be duplicated in `Keywords.cs`.

**Integer literal range is `int` only**: `Lexer.IntegerLiteral` delegates to `Numerics.IntegerInt32`, so literal values are capped at ±2,147,483,647. `CHANCELLOR` (`long`) and `STANDING CHANCELLOR` (`ulong`) variables cannot be initialised with out-of-range literals directly; assign a computed result or use casts. This is a Superpower limitation — adding a 64-bit literal parser would require a new combinator and changes to `ExpressionParser.LiteralExpression`.

**`STANDING` modifier is parsed inside `TypeKeyword`, not at the declaration level**: `ExpressionParser.TypeKeyword` tries `STANDING <base-integer-type>` as a `.Try()` alternative before any other type keyword. This means `STANDING` works automatically everywhere a type annotation is accepted: declarations, `IS HENCEFORTH A`, and `AS IT WERE … AS A`. `STANDING` followed by a non-integer type keyword (e.g. `STANDING FATHOM`) will silently fail the `.Try()` and fall through — it will NOT produce a parse error. `STANDING` alone as a type name is also not valid; the parser backtracks and reports a missing type keyword error.

**YARN string indexing — no parser changes**: `VICTIM n ON yarn` and `RECKONING OF yarn` reuse the `ArrayIndexNode` and `ArrayLengthNode` AST nodes unchanged. The runtime dispatches on `LiteralType.String` before the `LiteralType.Array` path in `EvaluateArrayIndex` and `EvaluateArrayLength`. `ExecuteArrayElementAssignment` checks `LiteralType.String` first and throws immediately — strings are always immutable by position in Topsy Turvy.

**Arithmetic widening uses `long` for all integer-family arithmetic**: `ApplyArithmetic` converts both operands to `long` via `ToLong()` before applying the integer operation, then converts the `long` result back to the widest operand type via `CreateIntegerFromLong`. This means unsigned arithmetic for `STANDING CHANCELLOR` (`ulong`) passes through `long`, potentially losing precision for values > `long.MaxValue`. Fix requires a separate unsigned arithmetic path if full `ulong` range arithmetic is ever needed.

**`ApplyArithmetic` signature changed from `Func<int,int,int>` to `Func<long,long,long>`**: all call sites in `EvaluateBinaryOperator` use lambda syntax `(a, b) => a + b` etc. which are type-inferred and compile for `long` without changes. If a future operator needs genuine `int` arithmetic, wrap the lambda explicitly.

**`FOOT` arithmetic result type**: `FOOT` (Single) + any integer type → result is `FOOT`. `FOOT` + `FATHOM` → result is `FATHOM` (the `LiteralType.Double` check fires before the `LiteralType.Single` check in `ApplyArithmetic`). Two `FOOT` operands → `FOOT`. The float path always computes through `double` internally; the `FOOT` path casts the `double` result back to `float`.

**`TopsyTurvyValue.CastTo` for new types routes through `long`/`ulong`/`double`/`char` helpers**: `CastTo` dispatches to private `ToInt64()`, `ToUInt64()`, `ToDouble()`, and `ToChar()` helper methods (on the value instance, not the static `Interpreter` helpers). The `Interpreter` has its own separate static `ToDouble(TopsyTurvyValue)` and `ToLong(TopsyTurvyValue)` helpers. The two sets are independent; keep them in sync if the type list changes.

**`SourceSpan` is a half-open interval**: `Start` is inclusive, `End` is exclusive — `End` points to the column one past the final character of the region, i.e. `[Start, End)`. This matches the LSP range convention used by the web editor. Confirmed by the error-span construction in `TryParse` (`endColumn + 1`).

**`PlaceholderSpan` is a fallback, not the default**: `ParserHelpers.PlaceholderSpan` (`(0,0)-(0,0)`) is returned by `BuildSpan` only when `ActiveSourceMap` is `null` — for example in isolated unit tests that call a parser combinator directly. During normal parsing via `TopsyTurvyParser.Parse`/`TryParse`, `ActiveSourceMap` is always set before the parse and all AST nodes receive real spans mapped from the pre-processed text back to the original source.

**`JUST SO` exclusion**: the implicit variable's name contains a space, so it cannot be matched with word-boundary regex. It is excluded from rename, references, and semantic token scanning, and is always added to `SymbolTable` manually.

**`THE PROPS` exclusion**: `THE PROPS` contains a space, so the existing space-in-name check in `RenameHandler` and `PrepareRenameHandler` excludes it from rename automatically — no explicit check needed. It is pre-populated in `SymbolTable.Build()` with `IsConstant = true` and `TypeDisplayName = "CONSERVATIVE LITTLE LIST OF YARN"`, and excluded from semantic token colouring by the same space check in `SemanticTokensHandler`. Like `JUST SO`, attempting to re-declare it with `PRAY WELCOME` is a runtime error (variable already declared).

**`THE PROPS` parser handling**: `THE PROPS` is a two-word token and cannot be matched by `Lexer.Identifier`. `ExpressionParser.ArrayNameParser` matches it explicitly (trying `Lexer.Keyword("THE PROPS")` first, then falling back to `Lexer.Identifier`) and is used wherever an array name appears in the parser — `ArrayIndexExpression` and `StatementParser.ArrayElementAssignment`. `ThePropsExpression` in the `Expression` chain handles bare references to `THE PROPS` (e.g. `BEHOLD THE PROPS`) and must appear before `IdentifierExpression` so that `THE` is not consumed as a plain identifier.

**`THE PROPS` CONSERVATIVE enforcement**: element assignment on `THE PROPS` follows the same path as any other `CONSERVATIVE` array — `ExecuteArrayElementAssignment` calls `environment.IsConstantInChain("THE PROPS")`, which returns `true`, and a `TopsyTurvyRuntimeException` is thrown. No special-casing is needed in the interpreter.

**LSP stdio**: `.ClearProviders()` must be called before `.AddLanguageProtocolLogging()` when configuring OmniSharp logging. Without it, the default console logger writes to stdout and corrupts the JSON-RPC stream. Configured in `IncantationCommandBuilder.HandleIncantationAsync()`.

**`--stdio` hidden option**: `vscode-languageclient` unconditionally appends `--stdio` to the process args when `TransportKind.stdio` is used. The `incantation` command registers `--stdio` as a hidden `Option<bool>` so `System.CommandLine` accepts it silently. Without this, the CLI rejects it as an unrecognised argument and the LSP server fails to start.

**`OmniSharpServer` alias in `IncantationCommandBuilder`**: `BWHazel.TopsyTurvy.LanguageServer` is both a project namespace (imported via `using`) and the name of OmniSharp's static server class. The alias `using OmniSharpServer = OmniSharp.Extensions.LanguageServer.Server.LanguageServer;` is required to disambiguate. Do not remove it.

**Bundled binary takes precedence**: `resolveCliPath` in `paths.ts` calls `fs.existsSync` on `bin/operetta` at activation time. Once `npm run prebuild:dotnet` has been run, the bundled binary is always used — re-run `npm run prebuild:dotnet` to refresh it, or delete `bin/` to force the dev-layout fallback.

**`DocumentStateManager`**: singleton; rebuilds `SymbolTable` only on a successful parse, preserving the last good table during syntax errors.

**`TryParse` reserved-word errors**: returns `ParseResult(program, errors)` with non-null `program` so the symbol table still builds while squiggle diagnostics are published. `Parse()` (used by the CLI) throws `TopsyTurvySyntaxException` instead.

**`SUMMON` call syntax**: `SUMMON name WITH arg1 AND arg2 IF YOU PLEASE.` / `SUMMON name WITH NOTHING IF YOU PLEASE.` — uses `WITH` as the argument separator, not `AND`.

**Blazor WASM timeout**: `CancellationTokenSource(TimeSpan)` relies on a timer callback that cannot fire while `Interpreter.Execute()` is blocking the single WASM thread. Set `ExecutionTimeout = TimeSpan.FromSeconds(10)` in `InterpreterExecutionOptions` instead — this stores a `DateTime` deadline checked by `CheckCancellation()` inside every loop body via `DateTime.UtcNow`.

**`PRAY ADMIT` path resolution**: `ExecuteImport` resolves relative import paths against the directory of the *importing* file, not the process CWD. `Interpreter.Execute()` accepts an optional `sourceFilePath` parameter; when `null` (e.g. Blazor WASM), the raw path from the AST is used unchanged.

**Web editor virtual FS resolver**: `Interpreter.Execute()` accepts an optional `Func<string, string?> fileResolver`. When supplied, `ExecuteImport` calls this delegate instead of `File.ReadAllText`, enabling `PRAY ADMIT` to resolve imports from the in-memory virtual filesystem. Import paths must be bare filenames — no directory prefix.

**Cross-document LSP awareness**: `DocumentStateManager.AllDocuments()` returns a snapshot of all open documents. Attempt lookup in the current document's `SymbolTable` first; on failure, iterate `AllDocuments()` skipping the current URI. For `DefinitionHandler`, the URI returned by the cross-file fallback must be used as `Location.Uri` in the response. Only `Function` kind symbols are pulled from other documents for hover, completion, and semantic tokens. `WorkspaceSymbolHandler` and `ReferencesHandler` aggregate across all documents unconditionally.

**Multi-word keyword completion**: `CompletionHandler` uses server-side phrase filtering, `FilterText = lastWord`, `InsertText = keyword[insertOffset..]`, `isIncomplete = true`, and a space trigger character to keep the list live as the user types through multi-word phrases.

**Web editor — localStorage single-key merge pattern**: all editor preferences are stored under one key (`topsy-turvy-editor`) as a merged JSON object. `savePreferences(preferences)` always reads the existing object first and spreads the incoming partial update over it (`{ ...existing, ...incoming }`). This lets JS-managed keys (split ratios `horizontalSplit`, `verticalSplit`) and C#-managed keys (files, active file, dark mode, etc.) be written independently without clobbering each other. The C# side calls `topsyTurvy.loadPreferences()` (returns the full object) and `topsyTurvy.savePreferences(EditorPreferences)` via `JSRuntime`. Blazor's JS interop layer serialises `EditorPreferences` to/from JSON using `System.Text.Json`, respecting `[JsonPropertyName]` attributes on the class and `[JsonConstructor]`/`[JsonPropertyName]` on `VirtualFile`.

**Web editor — `[JsonConstructor]` on `VirtualFile` is required for round-trip deserialisation**: `VirtualFile.Name` has no setter (it is set only via the constructor). Without `[JsonConstructor]`, `System.Text.Json` cannot deserialise JSON into a `VirtualFile` instance and throws. The attribute must remain on the constructor. Parameter names in the constructor must match the `[JsonPropertyName]` values on the corresponding properties (case-insensitive) — `System.Text.Json` matches by convention.

**Web editor — terminal theme not applied dynamically by XtermBlazor**: XtermBlazor reads `Options` only once at initialisation. Two mechanisms are therefore needed: (1) `terminalOptions` in `Editor.razor` is an expression-bodied property (not a `readonly` field) that reads `this.IsDarkMode`, ensuring the correct theme is applied on first render since the `IsDarkMode` cascading parameter is set before the first `OnAfterRenderAsync`; (2) `topsyTurvy.setTerminalTheme(isDarkMode)` is called from `OnParametersSetAsync` whenever the theme changes, updating `term.options.theme` directly on the xterm instance. Terminal colours: dark mode — background `#1e1e1e`, foreground `#d4d4d4`; light mode — background `#f5f5f5`, foreground `#1e1e1e`.

**Web editor — terminal output persistence via `lastRunOutput`**: `lastRunOutput` accumulates output lines joined by `\n`. It is saved as `TerminalOutput` in `EditorPreferences` after every run (in the `finally` block of `RunProgramAsync`) and after clear (`ConfirmClearAsync`). It is reset to `string.Empty` at the start of `RunProgramCoreAsync` (right after `terminal.Clear()`) so a failed run does not persist stale output from a previous successful run. On restore, `TerminalOutput` is split on `'\n'` and each line is replayed via `terminal.WriteLine`.

**Web editor — preference save triggers**: `SavePreferencesAsync` is called from: `OnContentChangedAsync` (the 400 ms debounce path, after syncing the active file's `Content`), `@bind-Value:after` on both `MudTextField` elements (CLI args and stdin), `OnGsLabelsToggledAsync` (G&S labels switch), `OnParametersSetAsync` when dark mode changes, the `finally` block of `RunProgramAsync` (after every run including failures), and `ConfirmClearAsync` (after terminal is cleared).

**Web editor — preference restore order in `RestorePreferencesAsync`**: G&S labels → CLI args → stdin → files (clear list, add each, build symbol table) → active file (set `activeFileName`, call `monacoEditor.SetValue`) → terminal output (replay lines, set `lastRunOutput`) → dark mode (call `ToggleDarkMode?.Invoke()` only when stored value differs from current). `StateHasChanged()` is called at the end. Restore is invoked from `OnAfterRenderAsync` on first render, after `initPaneDrag` and before `editorReady = true`.

**Web editor — terminal fit private API**: `fitTerminal` reads `term._core._renderService.dimensions.css.cell.height`. This is an internal xterm.js property — if the terminal stops filling its pane after an XtermBlazor upgrade, check this path first.

**Web editor — editor content API**: per-file content is stored in `VirtualFile.Content` and loaded/saved via `monacoEditor.SetValue` / `blazorMonaco.editor.getValue`. URI-based Monaco models were considered but abandoned — replacing BlazorMonaco's default model breaks the `onDidChangeModelContent` event system.

**Web editor — output download**: `Editor.razor` stores the last run's output in `lastRunOutput`. `DownloadTerminalAsync` uses this field directly rather than reading the xterm buffer. `lastRunOutput` is reset when the terminal is cleared via `ConfirmClearAsync`.

**Web editor — open file replace**: `OpenSingleFileAsync` handles the case where the opened file shares a name with an already-open tab. When the replaced file is the active tab, call `monacoEditor.SetValue(content)` directly — do not call `SwitchFileAsync`, which would re-sync old content back over `VirtualFile.Content`.

**`CONSERVATIVE` constant enforcement covers all three mutation paths**: `IS APPOINTED` (assignment), `IS HENCEFORTH A` (in-place cast), and `PRAY TELL` (input) all ultimately call `TopsyTurvyEnvironment.Assign`, which now checks `this.constants.Contains(name)` before allowing the write.  No additional guard in the interpreter is required for the cast and input paths — the constraint is enforced centrally in `Assign`.  The expression cast `AS IT WERE` does **not** need a guard because it produces a new value without mutating the source variable.

**`LITTLE LIST OF` in the parser, not `A LITTLE LIST OF`**: In the source text `AS A LITTLE LIST OF YARN`, the phrase `AS A` is consumed by `Lexer.Keyword("AS A")` — this matches exactly 4 characters `A-S-space-A`, positioning the cursor immediately after the final `A`. The remaining token stream therefore starts with ` LITTLE LIST OF`, not ` A LITTLE LIST OF`. The array-type combinator must match `Lexer.Keyword("LITTLE LIST OF")`. Using `Lexer.Keyword("A LITTLE LIST OF")` will fail to match because the `A` was already consumed. This applies in `StatementParser.ArrayDeclaration`, `ExpressionParser.ExpressionCast`, and anywhere else `AS A` is followed by an array type. `GRAMMAR.ebnf` carries an explanatory comment on the `ArrayDeclaration` production.

**`RECKONING OF` is an expression, not a statement, and must appear in `ExpressionStatementParser`**: `ExpressionStatementParser` in `StatementParser.cs` has its own explicit list of expression alternatives (distinct from `ExpressionParser.Expression`). Any new expression that can appear as a standalone statement must be added to **both** lists. `ArrayLengthExpression` is included in both. Omitting it from `ExpressionStatementParser` causes a `TopsyTurvySyntaxException` when parsing a bare `RECKONING OF arr` as a statement, even though the parser itself is correctly wired. `RECKONING OF` is read-only — it does not trigger `IsConstantInChain` and works on `CONSERVATIVE` arrays without error.

**Array element assignment bypasses `TopsyTurvyEnvironment.Assign`**: `VICTIM n ON arr IS APPOINTED val` mutates the `List<TopsyTurvyValue>` stored inside the array value directly — it does not call `Assign`. The standard CONSERVATIVE guard in `Assign` is therefore never triggered. Instead, `ExecuteArrayElementAssignment` calls `environment.IsConstantInChain(arrName)` (which walks the enclosing scope chain) before mutating the list. Do not rely on `Assign` for constant enforcement in array element mutations.

**Sized array size+BEING mutual exclusivity is a runtime error, not a parse error**: When `ArrayDeclarationNode.Size` is non-null and `InitialValues.Count > 0`, `ExecuteArrayDeclaration` throws a `TopsyTurvyRuntimeException`. The parser does not prevent this combination — it happily parses `PRAY WELCOME arr AS A LITTLE LIST OF 3 PEER BEING 1 AND 2 AND 3 IF YOU PLEASE.` into an `ArrayDeclarationNode` with both `Size = 3` and three `InitialValues`. The guard lives exclusively in the runtime. Similarly, a negative size is caught only at runtime.

**Array reference semantics**: `PRAY WELCOME alias AS A LITTLE LIST OF PEER` followed by `alias IS APPOINTED original` makes both variables point to the same `List<TopsyTurvyValue>` instance. Mutating an element via `alias` is visible via `original` and vice versa. There is no copy-on-assign. This is consistent with how .NET reference types work and is intentional — value semantics for arrays are deferred to a future struct/class type system.

**1-based array indexing and ascending loops**: `VICTIM 1 ON arr` accesses the first element. The ascending loop (`BY A LEGAL FICTION ASCENDING x UNTIL n`) resets its counter to 0 at the start of each iteration — using it to index into an array would start at `VICTIM 0`, which is out-of-range. Use a WHILST loop for array traversal: `count IS APPOINTED 1`, then `BY A LEGAL FICTION WHILST HARDLY EVER PRE-ADAMITE count AND length`, incrementing `count` manually inside the loop body. See `examples/language/arrays.topsy` for the canonical pattern.

**`AS IT WERE` standalone statement sets `JUST SO` via `ExpressionStatement`**: `AS IT WERE <expr> AS A <type>` is now an expression, not a dedicated statement.  When used as a standalone statement it is parsed as an `ExpressionStatement` wrapping the `ExpressionCastNode`.  The `ExpressionStatement` dispatch path in `Interpreter.Execute` sets `environment.JustSo` to the evaluated expression result.  There is no `ExecuteExpressionCast` method — cast evaluation is handled entirely inside `EvaluateExpression`.

**Try/catch inline expression**: `WITH THE GREATEST RESPECT, <expression>` requires the expression on the *same line* as the keyword. Writing it on the following line causes a parse failure. The formatter's indentation rules for this construct diverge from valid parser input by design.

**`MODIFIED RAPTURE` catch binding creates a child scope**: when `MODIFIED RAPTURE, Name` is used, the interpreter calls `environment.CreateNested()` and declares `Name` in that child scope.  The binding is inaccessible outside the exception block — accessing it after `THAT CONCLUDES THE MATTER.` produces a runtime error.  `JUST SO` is always set regardless of whether a binding is present (backward compatibility).  The comma delimiter (`MODIFIED RAPTURE, Name`) is required to avoid ambiguity with statement-starting identifiers (e.g. `result IS APPOINTED ...`) that the parser would otherwise greedily consume as the binding name.

**Web editor — MudTabs API (MudBlazor 9.x)**: `ScrollButtons` was renamed to `AlwaysShowScrollButtons`. `PanelClass` belongs on `MudTabPanel`, not `MudTabs`. Using the old names produces MUD0002 analyzer warnings.

**Web editor — auto-indent**: Monaco's `indentationRules` only take effect when `autoIndent` is `'full'` (numeric `4`). This is a construction-time option — set via `AutoIndent = "full"` in `StandaloneEditorConstructionOptions`; `updateOptions` has no effect on it.

**CLI version string**: `AssemblyInformationalVersionAttribute` = `{Version}+{SourceRevisionId}`. `PedigreeCommandBuilder` splits on `+` and holds `SpecVersion = "0.2.0"` as a private constant.

**`--tiptoe` output routing**: with `--tiptoe`, errors go to stderr and program output to stdout. Without it, all output — including error panels — goes to stdout via `AnsiConsole.Write`. E2E tests that assert on error messages must use `--tiptoe`.

**Spectre.Console panel header truncation in non-TTY mode**: when stdout is redirected, Spectre.Console truncates panel headers to fit the content width with a Unicode ellipsis (`…`). A short body (e.g. a short filename) produces a narrow panel. Workaround: use a long filename to widen the panel, or assert only the unambiguous prefix.

**SignatureHelp two-update pattern**: signature help tests for in-progress `SUMMON` calls (where `IF YOU PLEASE.` has not been typed yet) must seed `DocumentStateManager` with a successful parse before updating with the incomplete source, so the last-good symbol table is preserved and available to the handler.

---

## 3. LSP Handlers & CLI Commands

### LSP Handlers (`interpreter/BWHazel.TopsyTurvy.LanguageServer/` — class library, started via `operetta sorcerer incantation`)

| Handler | Purpose |
|---|---|
| `TextDocumentSyncHandler` | `didOpen`/`didChange`/`didSave`/`didClose`. Calls `TryParse`, publishes diagnostics, updates `DocumentStateManager`. |
| `HoverHandler` | Markdown tooltip for variables, functions, parameters, and `JUST SO`. |
| `DefinitionHandler` | Go-to-definition using `DefinitionLine` recovered from source scan. |
| `CompletionHandler` | Symbols + 74 keywords. Server-side phrase filtering for multi-word keywords. Known edge case: variable names that are keyword prefixes may trigger false keyword-context mode. |
| `SemanticTokensHandler` | Colours variables, parameters, functions by whole-word line scan. Emits the `readonly` modifier for `CONSERVATIVE` constant variables, mapping to the `variable.other.constant` TextMate scope via `semanticTokenScopes` in `package.json`. Excludes `JUST SO`. |
| `RenameHandler` | Whole-word case-insensitive rename; skips comments and strings. Single-file only. Excludes multi-word built-ins via space-in-name check (covers `JUST SO` and `THE PROPS`). |
| `PrepareRenameHandler` | Pre-validates rename targets; returns placeholder range for input box. Same space-in-name exclusion as `RenameHandler`. |
| `DocumentSymbolHandler` | Populates VS Code Outline panel from `SymbolTable`. |
| `ReferencesHandler` | Find All References; respects `IncludeDeclaration`. |
| `SignatureHelpHandler` | Parameter hints inside `SUMMON` calls; counts `AND` tokens for active parameter. |
| `FoldingRangeHandler` | Stack-based fold regions for all block constructs and comments. |
| `DocumentFormattingHandler` | Normalises keyword casing (74 keywords) and applies 2-space libretto indentation. |
| `CodeLensHandler` | Inline reference-count annotations above declarations; links to Find All References. |
| `WorkspaceSymbolHandler` | Handles `workspace/symbol` requests; searches all open documents by query string. |

### CLI Commands (`interpreter/BWHazel.TopsyTurvy.Cli/CommandBuilders/`)

| Command | Alias(es) | Options | Notes |
|---|---|---|---|
| `perform <file>` | `stage`, `run` | `--tiptoe` | Runs a `.topsy` file. |
| `mount <project>` | `init` | `--title`, `--or`, `--hollow`, `--tiptoe` | Creates a project directory. Without `--hollow`, also scaffolds `<project>/<project>.topsy`. |
| `commission <file>` | `new` | `--title`, `--or`, `--tiptoe` | Scaffolds a Hello World `.topsy` file. UTF-8, no BOM. Both commands delegate content to `FileManager.BuildFileContent`. |
| `rehearse <file>` | `check` | `--tiptoe` | Syntax-checks without executing via `ProgramRunner.Check`. |
| `sorcerer promptbook <file>` | `dev ast` | `--abridged`, `--chromatic`, `--tiptoe` | Prints AST as JSON via `NodeJsonConverter`. |
| `sorcerer cue <file>` | `preprocess` | `--tiptoe` | Runs the pre-processor pipeline and prints the transformed source to stdout. |
| `sorcerer incantation` | `dev lsp` | `--stdio` (hidden) | Starts the OmniSharp LSP server over stdio. Invoked by the VS Code extension. |
| `playbill <file>` | `docs` | `--chromatic`, `--tiptoe` | Generates Markdown documentation for all documented symbols and prints to stdout. `--chromatic` applies Spectre.Console markup for titles, bold, italic, and inline code. |
| `cadenza` | `interactive` | `--tiptoe` | Interactive REPL session. Variables and function definitions persist across turns. Styled `In`/`Out` panels in rich mode. REPL commands: `:exit`/`:quit`, `:begone`/`:clear`, `:entracte`/`:help`, `:multiline`/`:madrigal`, `:singleline`/`:patter`, `:armoury [all|variables|functions]`/`:env`. |
| `pedigree` | `info` | `--tiptoe` | Shows CLI version, commit SHA, and language spec version. |

---

## 4. Known Gaps

**`PrincipalBlockNode.Declarations` uses a weak base type instead of a discriminated union**: `Declarations` is typed as `IReadOnlyList<Statement>` rather than a union of `DeclarationNode | ArrayDeclarationNode`, because C# does not yet provide first-class discriminated-union syntax. Callers must pattern-match at runtime to determine the concrete type. This will be revisited when discriminated unions land in C# / .NET 11.

**Ascending loop always starts from 0 (spec-level limitation)**: `ASCENDING <var>` resets the loop variable to `0` at the start of every loop per the spec (`<var>` begins at `0`). There is no syntax to specify a different starting value. Descending loops have no such constraint — they use the variable's current declared value. Fix requires a spec change (remove the "begins at `0`" constraint, or add `FROM <n>` syntax) and removing the hardcoded `Assign(..., Integer(0))` in `ExecuteAscendingLoop`.

**Monarch tokenizer keyword duplication**: the keyword list appears in `topsy-turvy-language.js` (Monarch tokenizer) and `topsy-turvy.tmLanguage.json` (TextMate grammar). The JS and TextMate copies cannot consume the C# library and remain separate. When adding a language feature, update both in the same change.

**Web editor interactive stdin (deferred)**: `BufferedWebIO` reads from a pre-supplied `Queue<string>`. True interactive stdin requires making `ITopsyTurvyIO` async — a new `InteractiveWebIO` implementation and an XtermBlazor input handler.

**LSP squiggle accuracy — Option 1 done, Option 2 deferred**: syntax errors now report at the correct line. The `WsMany<T>` combinator in `ParserHelpers` replaced all 12 `Ws(X).Try().Many()` block-body sites; it separates whitespace consumption from the statement parse and propagates partial-match errors (where an opening keyword was consumed but the body failed) at the actual failure position rather than resetting the cursor. `TryParse()` still stops at the **first** syntax error — there is no error-recovery or multi-error reporting. Option 2 (deferred): full multi-error recovery requires a hand-written recovery loop that records the error, advances the cursor to the next statement-opening keyword, and continues; this is significant additional work and should be considered after static typing is introduced (more complex constructs = larger recovery surface area).

**`ValidateSymbolNames` only checks top-level statements**: `TopsyTurvyParser.ValidateSymbolNames` walks only the top-level statement list of `ProgramNode`. Declarations nested inside function bodies, conditionals, loops, or try-catch blocks are not validated — a variable declared with a reserved-keyword name inside a nested block will not produce a diagnostic. Note that the parser may independently reject some of these cases (reserved words that cannot be lexed as identifiers), but the semantic check is incomplete by design. Fix requires recursing into all block-bearing statement types (`FunctionDefinitionNode`, `ConditionalNode`, `LoopNode`, `TryCatchNode`, etc.) or introducing an AST visitor.

**Web editor JS not linted or formatted**: `topsy-turvy-language.js` and `web-editor.js` have no ESLint or Prettier configuration.

**VS Code extension file icon**: the `icon` field on the `languages` contribution is only shown when no file icon theme is active. Long-term fix: submit a PR to `vscode-icons` adding a `.topsy` entry.

**`<example>` must be nested inside `<remarks>` for dotnetmd rendering**: the standard XML doc convention places `<example>` as a top-level sibling of `<summary>`, but dotnetmd only renders examples when `<example>` is nested inside `<remarks>`. Methods with no `<param>` tags (e.g. `TopsyTurvyValue.Null()`) and `override` methods (e.g. `TopsyTurvyValue.ToString()`) are additionally prone to their documentation being silently dropped. The non-standard placement must be maintained until dotnetmd is replaced or this behaviour changes upstream.

**`dotnetmd` cannot document LSP handlers and has additional rendering defects**: `dotnetmd` silently skips any class whose base type cannot be resolved from locally available XML documentation files. All Language Server handlers inherit from OmniSharp base classes (e.g. `HoverHandlerBase`, `CodeLensHandlerBase`) in NuGet packages that do not ship XML docs, so `dotnetmd` drops all handler pages without warning — only `DocumentStateManager` (base type `Object`) and other non-handler types survive. Additional defects: `<c>` inline code tags are not converted to Markdown backticks (rendered as literal `<c>text</c>` in output); `<remarks>` content is run together without line breaks. `DefaultDocumentation` (`scripts/generate-api-docs.sh`) is a working alternative: it documents all public types including handlers and processes the full XML doc set correctly. However, switching requires migrating all XML documentation comments from the project's current inline Markdown style (raw `*` bullet syntax, backtick spans, and other Markdown embedded within `<remarks>`) to standard .NET XML doc tags (`<list type="bullet"><item>`, `<c>`, `<para>`, etc.), because `DefaultDocumentation` treats XML node content as plain text rather than Markdown. Namespace title formatting in `DefaultDocumentation` output also differs from the current Docusaurus page structure and would require post-processing refinement before the generated pages are publication-ready.

**`FunctionDefinitionNode` parameter representation is split across two parallel lists**: `Parameters` (names, `IReadOnlyList<string>`) and `ParameterSpans` (source positions, `IReadOnlyList<SourceSpan>`) are separate lists that must be kept in sync — they are always the same length and are populated together by `StatementParser.FunctionDefinition`. When static typing is introduced, both lists will be replaced by a single typed-parameter structure (e.g. `record TypedParameter(string Name, LiteralType Type, SourceSpan Span)`) so that name, type, and position travel together.

**`SymbolTable` uses a flat namespace — parameters from different functions collide**: `SymbolTable` is a single `Dictionary<string, SymbolInfo>` (case-insensitive). If two functions declare a parameter with the same name (e.g. both have a parameter called `name`), only the first is kept; the second is silently dropped by the `ContainsKey` guard in `AddFunction`. Consequences: hover, go-to-definition, and rename all use the first function's definition position for any subsequently declared parameter of the same name, and Find All References aggregates occurrences from both functions under one symbol. Fix requires scoped symbol tables — a global scope for variables and functions, plus a per-function scope for parameters — and updates to all LSP handlers and web editor bridge methods that call `TryGetSymbol`.

### Deferred LSP Features

| Feature | Blocker |
|---|---|
| Inlay Hints (`textDocument/inlayHint`) | Handler not yet written; `SymbolInfo.TypeDisplayName` already carries the data. |
| Call Hierarchy | Requires a call-graph not currently built by `SymbolTable`. |
| Hierarchical `DocumentSymbol` (`textDocument/documentSymbol`) | `DocumentSymbolHandler` currently returns flat `SymbolInformation` (the older LSP format), which lists every symbol at the same level in the Outline panel. The newer `DocumentSymbol` format supports nesting — parameters under their parent function, variables under the PRINCIPALS block. Upgrading requires building a tree during `SymbolTable.Build` and returning `DocumentSymbol` instances instead of wrapping `SymbolInformation` in `SymbolInformationOrDocumentSymbol`. The flat format is used because the current `SymbolTable` has no hierarchy, and the flat namespace Known Gap (parameter name collision) would need to be resolved first. |

### Deferred Web Editor Features

| Feature | Blocker |
|---|---|
| Interactive stdin (line-by-line `PRAY TELL`) | `ITopsyTurvyIO` must become async; requires `InteractiveWebIO` + XtermBlazor input handler |

---

## 5. Next Steps

1. Read `AGENTS.md` and `DEVELOPMENT.md` before starting any work.
2. Run `dotnet build interpreter/BWHazel.TopsyTurvy.slnx --no-incremental` and `dotnet test` to confirm baseline.
3. **VS Code Marketplace publication**: register a publisher at `marketplace.visualstudio.com/manage`, confirm `publisher` in `package.json` matches, then `vsce publish` from the publish workflow or locally.
4. **VS Code file icon**: submit a PR to `vscode-icons` adding a `.topsy` entry — see §4 Known Gaps.
5. **LSP multi-error recovery (Option 2, deferred — see §4)**: `TryParse()` currently stops at the first syntax error. Full recovery requires a hand-written loop that records the error, advances to the next statement-opening keyword, and continues. Consider after static typing is complete.
